引「../../src/持久化/内存仓储.yx」为 内存；

引「../../src/持久化/仓储协议.yx」为 协议；

引「../../src/实体/实体.yx」为 实体；

引「标准:测试」为 测试；

引「标准:文字」为 文字；

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.断言（命中，「应抛出指定仓储错误码」）；
终

定 仓储 为 内存.新仓储（）；

定 甲 为 实体.新实体（「测试:角色/甲」，「测试:原型/玩家」）；

甲.添加组件（「言域:组件/描述」，{「名称」：「甲」}，1）；

甲.添加标签（「玩家」）；

定 已写甲 为 仓储.写入实体快照（甲.快照（），空）；

测试.相等（已写甲.修订（），2）；

甲.添加标签（「外部修改」）；

测试.相等（仓储.读取实体（「测试:角色/甲」）.有标签（「外部修改」），假）；

定 读甲 为 仓储.读取实体（「测试:角色/甲」）；

读甲.添加标签（「未保存」）；

测试.相等（仓储.读取实体（「测试:角色/甲」）.有标签（「未保存」），假）；

定 乙 为 实体.新实体（「测试:角色/乙」，「测试:原型/访客」）；

乙.添加标签（「访客」）；

仓储.写入实体快照（乙.快照（），空）；

定 批量：列 为 仓储.批量读取（【「测试:角色/乙」，「测试:角色/不存在」，「测试:角色/甲」】）；

测试.相等（批量【0】.编号，「测试:角色/乙」）；

测试.相等（批量【1】，空）；

测试.相等（批量【2】.编号，「测试:角色/甲」）；

测试.相等（长度（仓储.查询实体（{「编号前缀」：「测试:角色/」}）），2）；

测试.相等（仓储.查询实体（{「标签」：「玩家」}）【0】.编号，「测试:角色/甲」）；

测试.相等（仓储.查询实体（{「组件」：【「言域:组件/描述」】}）【0】.编号，「测试:角色/甲」）；

测试.相等（长度（仓储.查询实体（{「最低修订」：2，「最高修订」：2}）），1）；

定 空仓储 为 内存.新仓储（）；

法 非法查询（） 则
    空仓储.查询实体（{「未知字段」：1}）；
终

应抛（非法查询，协议.参数无效错误）；

法 非法范围（） 则
    空仓储.查询实体（{「最低修订」：3，「最高修订」：1}）；
终

应抛（非法范围，协议.参数无效错误）；

法 非法集合（） 则
    空仓储.查询实体（{「标签」：【「玩家」，1】}）；
终

应抛（非法集合，协议.参数无效错误）；

法 非法精确编号（） 则
    空仓储.查询实体（{「编号」：「不是稳定编号」}）；
终

法 非法组件编号（） 则
    空仓储.查询实体（{「组件」：「坏组件」}）；
终

法 空白标签（） 则
    空仓储.查询实体（{「标签」：「   」}）；
终

应抛（非法精确编号，协议.参数无效错误）；

应抛（非法组件编号，协议.参数无效错误）；

应抛（空白标签，协议.参数无效错误）；

定 同修订：典 为 仓储.读取实体（「测试:角色/甲」）.快照（）；

置 同修订【「组件」】【「言域:组件/描述」】【「数据」】【「名称」】 为 「偷改」；

法 写同修订（） 则
    仓储.写入实体快照（同修订，2）；
终

应抛（写同修订，协议.参数无效错误）；

定 更新甲 为 仓储.读取实体（「测试:角色/甲」）；

更新甲.添加标签（「在线」）；

仓储.写入实体快照（更新甲.快照（），2）；

测试.相等（仓储.读取实体（「测试:角色/甲」）.修订（），3）；

定 变更甲 为 仓储.读取实体（「测试:角色/甲」）；

变更甲.添加标签（「已验证」）；

定 甲变更：典 为 变更甲.变更集（）；

仓储.保存变更集（甲变更）；

测试.相等（仓储.读取实体（「测试:角色/甲」）.有标签（「已验证」），真）；

法 重复保存旧变更（） 则
    仓储.保存变更集（甲变更）；
终

应抛（重复保存旧变更，协议.修订冲突错误）；

法 错修订删除（） 则
    仓储.删除实体（「测试:角色/甲」，1）；
终

应抛（错修订删除，协议.修订冲突错误）；

测试.相等（仓储.删除实体（「测试:角色/甲」，4），真）；

测试.相等（仓储.读取实体（「测试:角色/甲」），空）；

法 缺失实体仍带修订（） 则
    仓储.删除实体（「测试:角色/甲」，4）；
终

应抛（缺失实体仍带修订，协议.修订冲突错误）；

测试.相等（仓储.删除实体（「测试:角色/甲」，空），假）；

法 分数预期修订（） 则
    仓储.删除实体（「测试:角色/甲」，0.5）；
终

应抛（分数预期修订，协议.参数无效错误）；
