引「../../src/持久化/内存仓储.yx」为 内存；

引「../../src/持久化/仓储协议.yx」为 协议；

引「../../src/实体/实体.yx」为 实体；

引「标准:JSON」为 JSON；

引「标准:测试」为 测试；

定 仓储 为 内存.新仓储（）；

仓储.设置调用记录上限（0）；

定 初始状态：典 为 协议.空状态（）；

逐 序号：数 于 范围（0，100） 则
    定 编号：文 为 （「规模:实体/」 加 JSON.序列化（序号））；
    置 初始状态【「实体」】【编号】 为 {「协议」：实体.快照协议，「格式版本」：实体.格式版本，「编号」：编号，「原型编号」：「」，「组件」：{}，「标签」：【】，「修订」：0}；
终

仓储.提交事务（0，初始状态）；

定 导出：典 为 仓储.导出状态（）；

测试.相等（长度（导出【「实体」】），100）；

定 事务 为 仓储.开始事务（）；

事务.设置世界变量（「规模.事务」，{「完成」：真}）；

事务.提交（）；

测试.相等（仓储.读取世界变量（「规模.事务」）【「完成」】，真）；

仓储.创建快照（「大规模实体」，{「实体数」：100}）；

仓储.删除实体（「规模:实体/99.0」，0）；

测试.相等（仓储.读取实体（「规模:实体/99.0」），空）；

仓储.恢复快照（「大规模实体」）；

测试.相等（仓储.读取实体（「规模:实体/99.0」）.编号，「规模:实体/99.0」）；
