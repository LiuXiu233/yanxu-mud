引「../../src/持久化/内存仓储.yx」为 内存；

引「../../src/实体/实体.yx」为 实体；

引「包:言据」为 言据；

引「标准:JSON」为 JSON；

引「标准:测试」为 测试；

定 来源 为 内存.新仓储（）；

来源.写入实体快照（实体.新实体（「测试:实体/导入」，「」）.快照（），空）；

来源.设置世界变量（「测试.导入」，{「值」：1}）；

来源.保存计划任务（{「编号」：「测试:任务/导入」，「到期」：10，「间隔」：0，「载荷」：{}}，空）；

定 状态：典 为 来源.导出状态（）；

定 序列化状态：典 为 JSON.解析（来源.序列化状态（））；

测试.断言（言据.相同（序列化状态，状态），「序列化状态必须与隔离导出保持一致」）；

定 隔离状态：典 为 来源.导出状态（）；

置 隔离状态【「世界变量」】【「测试.导入」】【「值」】 为 2；

测试.相等（来源.读取世界变量（「测试.导入」）【「值」】，1）；

定 目标 为 内存.从状态（状态）；

测试.相等（目标.读取实体（「测试:实体/导入」）.编号，「测试:实体/导入」）；

测试.相等（目标.读取世界变量（「测试.导入」）【「值」】，1）；

测试.相等（目标.读取计划任务（「测试:任务/导入」）【「到期」】，10）；

测试.断言（言据.相同（目标.导出状态（），状态），「导入状态必须精确恢复协议状态」）；

来源.创建快照（「运行快照」，{「用途」：「重启恢复」}）；

来源.设置世界变量（「测试.导入」，{「值」：2}）；

定 运行状态：典 为 JSON.解析（来源.序列化运行状态（））；

测试.相等（运行状态【「协议」】，内存.运行状态协议）；

定 运行目标 为 内存.从运行状态（运行状态）；

测试.相等（运行目标.读取世界变量（「测试.导入」）【「值」】，2）；

测试.相等（长度（运行目标.各快照（）），1）；

运行目标.恢复快照（「运行快照」）；

测试.相等（运行目标.读取世界变量（「测试.导入」）【「值」】，1）；

测试.相等（运行目标.创建快照（「运行快照二」，{}）【「序号」】，2）；
