引「../../src/持久化/内存仓储.yx」为 内存；

引「../../src/持久化/仓储协议.yx」为 协议；

引「标准:测试」为 测试；

引「标准:文字」为 文字；

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.断言（命中，「应抛出指定仓储错误码」）；
终

定 仓储 为 内存.新仓储（）；

定 原变量：典 为 {「天气」：「晴」，「计数」：【1，2】}；

仓储.设置世界变量（「世界.天气」，原变量）；

置 原变量【「天气」】 为 「雨」；

测试.相等（仓储.读取世界变量（「世界.天气」）【「天气」】，「晴」）；

定 读变量：典 为 仓储.读取世界变量（「世界.天气」）；

置 读变量【「天气」】 为 「雪」；

测试.相等（仓储.读取世界变量（「世界.天气」）【「天气」】，「晴」）；

法 非普通变量（） 则
    法 回调（） 则
        归 空；
    终
    仓储.设置世界变量（「坏值」，{「回调」：回调}）；
终

应抛（非普通变量，协议.参数无效错误）；

定 后任务：典 为 仓储.保存计划任务（{「编号」：「后」，「到期」：20，「间隔」：0，「载荷」：{「值」：2}}，空）；

定 前任务：典 为 仓储.保存计划任务（{「编号」：「前」，「到期」：10，「间隔」：5，「载荷」：{「值」：1}}，空）；

测试.相等（前任务【「修订」】，0）；

测试.相等（仓储.各计划任务（）【0】【「编号」】，「前」）；

仓储.保存计划任务（{「编号」：「b」，「到期」：5，「间隔」：0，「载荷」：{}}，空）；

仓储.保存计划任务（{「编号」：「a」，「到期」：5，「间隔」：0，「载荷」：{}}，空）；

测试.相等（仓储.各计划任务（）【0】【「编号」】，「a」）；

测试.相等（仓储.各计划任务（）【1】【「编号」】，「b」）；

置 前任务【「到期」】 为 30；

定 已更新：典 为 仓储.保存计划任务（前任务，0）；

测试.相等（已更新【「修订」】，1）；

测试.相等（仓储.读取计划任务（「前」）【「到期」】，30）；

法 旧任务修订（） 则
    仓储.保存计划任务（前任务，0）；
终

应抛（旧任务修订，协议.修订冲突错误）；

法 空白任务编号（） 则
    仓储.保存计划任务（{「编号」：「   」，「到期」：1，「间隔」：0，「载荷」：{}}，空）；
终

法 前后空白任务编号（） 则
    仓储.保存计划任务（{「编号」：「 坏任务 」，「到期」：1，「间隔」：0，「载荷」：{}}，空）；
终

法 删除空白任务（） 则
    仓储.删除计划任务（「   」，空）；
终

应抛（空白任务编号，协议.参数无效错误）；

应抛（前后空白任务编号，协议.参数无效错误）；

应抛（删除空白任务，协议.参数无效错误）；

测试.相等（仓储.删除计划任务（「后」，后任务【「修订」】），真）；

法 缺失任务仍带修订（） 则
    仓储.删除计划任务（「后」，后任务【「修订」】）；
终

应抛（缺失任务仍带修订，协议.修订冲突错误）；

测试.相等（仓储.删除计划任务（「后」，空），假）；

定 迁移：典 为 仓储.记录迁移（「测试:迁移/初始化」，「1.0.0」，{「说明」：「完成」}）；

测试.相等（迁移【「序号」】，1）；

测试.相等（仓储.各迁移（）【0】【「版本」】，「1.0.0」）；

法 重复迁移（） 则
    仓储.记录迁移（「测试:迁移/初始化」，「1.0.1」，{}）；
终

应抛（重复迁移，协议.迁移错误）；

仓储.清调用记录（）；

仓储.读取世界变量（「世界.天气」）；

定 调用：列 为 仓储.调用记录（）；

测试.相等（调用【0】【「操作」】，「读取世界变量」）；

置 调用【0】【「操作」】 为 「篡改」；

测试.相等（仓储.调用记录（）【0】【「操作」】，「读取世界变量」）；

仓储.设故障（「读取世界变量」，「注入读取失败」，2）；

法 第一次故障（） 则
    仓储.读取世界变量（「世界.天气」）；
终

法 第二次故障（） 则
    仓储.读取世界变量（「世界.天气」）；
终

应抛（第一次故障，协议.测试故障错误）；

应抛（第二次故障，协议.测试故障错误）；

测试.相等（仓储.读取世界变量（「世界.天气」）【「天气」】，「晴」）；

仓储.清调用记录（）；

仓储.设置调用记录上限（2）；

仓储.读取世界变量（「世界.天气」）；

仓储.读取世界变量（「世界.天气」）；

仓储.读取世界变量（「世界.天气」）；

测试.相等（长度（仓储.调用记录（）），2）；

测试.相等（仓储.调用记录（）【0】【「序号」】 小于 仓储.调用记录（）【1】【「序号」】，真）；
