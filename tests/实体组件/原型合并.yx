引「../../src/实体/实体.yx」为 实体；

引「标准:测试」为 测试；

定 原型 为 实体.新实体（「测试:原型/木箱」，「」）；

原型.添加组件（「核心:组件/描述」，{「名称」：「旧木箱」}，1）；

原型.添加组件（「核心:组件/容器」，{「容量」：10}，1）；

原型.添加标签（「容器」）；

定 实例 为 实体.从原型快照（「测试:物品/仓库木箱」，原型.快照（），{「核心:组件/描述」：{「版本」：2，「数据」：{「名称」：「仓库木箱」}}，「核心:组件/持久化」：{「版本」：1，「数据」：{「范围」：「世界」}}}，【「容器」，「固定」】）；

测试.相等（实例.编号，「测试:物品/仓库木箱」）；

测试.相等（实例.原型编号（），「测试:原型/木箱」）；

测试.相等（实例.修订（），0）；

测试.相等（实例.取组件信封（「核心:组件/描述」）【「版本」】，2）；

测试.相等（实例.取组件（「核心:组件/描述」）【「名称」】，「仓库木箱」）；

测试.相等（实例.取组件（「核心:组件/容器」）【「容量」】，10）；

测试.相等（实例.取组件（「核心:组件/持久化」）【「范围」】，「世界」）；

测试.相等（实例.标签（）【0】，「固定」）；

测试.相等（实例.标签（）【1】，「容器」）；

实例.设置组件（「核心:组件/容器」，{「容量」：20}，1）；

测试.相等（原型.取组件（「核心:组件/容器」）【「容量」】，10）；
