引「../../src/组件/通用组件.yx」为 通用；

引「标准:测试」为 测试；

引「包:言据」为 言据；

定 注册 为 通用.建立注册表（）；

测试.相等（长度（注册.编号列（）），24）；

测试.相等（注册.已注册（「言域:组件/描述」），真）；

测试.相等（注册.已注册（「言域:组件/持久化」），真）；

测试.相等（注册.已注册（「言域:组件/角色」），真）；

测试.相等（注册.已注册（「言域:组件/物品」），真）；

逐 编号：文 于 注册.编号列（） 则
    定 定义 为 注册.取（编号）；
    定 信封：典 为 定义.新信封（{}）；
    测试.相等（信封【「版本」】，1）；
    测试.断言（言据.符合规则（信封【「数据」】，定义.Schema），「通用组件默认值必须符合自身 Schema」）；
    测试.断言（（长度（定义.文档） 大于 0），「通用组件必须有中文文档」）；
终
