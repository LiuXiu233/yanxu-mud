引「../../src/实体/值支持.yx」为 值；

引「标准:测试」为 测试；

引「标准:文字」为 文字；

引「标准:数学」为 数学；

令 自环典：典 为 {}；

置 自环典【「自己」】 为 自环典；

测试.相等（值.是普通值（自环典），假）；

令 自环列：列 为 【】；

追加（自环列，自环列）；

测试.相等（值.是普通值（自环列），假）；

令 拒绝深复制：理 为 假；

试 则
    值.深复制（自环典）；
救 所误 则
    置 拒绝深复制 为 文字.始于（所误.消息，值.值无效错误）；
终

测试.断言（拒绝深复制，「深复制必须稳定拒绝循环值」）；

令 根：典 为 {}；

令 当前：典 为 根；

逐 次：数 于 范围（0，70） 则
    令 下一层：典 为 {}；
    置 当前【「下一层」】 为 下一层；
    置 当前 为 下一层；
终

测试.相等（值.是普通值（根），假）；

令 共享：典 为 {「值」：1}；

定 无环共享：典 为 {「甲」：共享，「乙」：共享}；

测试.相等（值.是普通值（无环共享），真）；

定 副本：典 为 值.深复制典（无环共享）；

置 副本【「甲」】【「值」】 为 2；

测试.相等（副本【「乙」】【「值」】，1）；

测试.相等（共享【「值」】，1）；

定 非有限数：数 为 数学.幂（10，400）；

测试.相等（值.是普通值（非有限数），假）；
