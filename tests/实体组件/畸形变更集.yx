引「../../src/实体/实体.yx」为 实体；

引「../../src/实体/值支持.yx」为 值；

引「标准:测试」为 测试；

引「包:言据」为 言据；

法 去变更字段（来源：典，位置：数，字段：文）：典 则
    定 结果：典 为 值.深复制典（来源）；
    定 各变更：列 为 结果【「变更」】；
    定 所项：典 为 各变更【位置】；
    置 各变更【位置】 为 值.去键（所项，字段）；
    置 结果【「变更」】 为 各变更；
    归 结果；
终

法 改变更字段（来源：典，位置：数，字段：文，新值）：典 则
    定 结果：典 为 值.深复制典（来源）；
    定 各变更：列 为 结果【「变更」】；
    定 所项：典 为 各变更【位置】；
    置 所项【字段】 为 新值；
    置 各变更【位置】 为 所项；
    置 结果【「变更」】 为 各变更；
    归 结果；
终

法 应拒绝且原子（基础，畸形：典，说明：文）：空 则
    定 应用前：典 为 基础.快照（）；
    定 结果：典 为 实体.尝试应用变更集（基础，畸形）；
    测试.相等（结果【「成功」】，假）；
    测试.相等（结果【「错误」】【「代码」】，实体.变更集无效错误）；
    测试.相等（值.有键（结果【「错误」】，「消息」），假）；
    测试.相等（值.有键（结果【「错误」】，「位置」），假）；
    测试.断言（言据.相同（基础.快照（），应用前），说明）；
    归 空；
终

定 标签基础 为 实体.新实体（「测试:角色/畸形标签」，「测试:原型/玩家」）；

定 标签工作 为 实体.恢复实体（标签基础.快照（））；

标签工作.添加标签（「玩家」）；

定 标签添加：典 为 标签工作.变更集（）；

应拒绝且原子（标签基础，去变更字段（标签添加，0，「标签」），「标签添加缺字段不得修改原实体」）；

应拒绝且原子（标签基础，改变更字段（标签添加，0，「标签」，1），「标签添加字段类型错误不得修改原实体」）；

定 标签删除基础 为 实体.新实体（「测试:角色/畸形标签删除」，「测试:原型/玩家」）；

标签删除基础.添加标签（「玩家」）.确认变更（）；

定 标签删除工作 为 实体.恢复实体（标签删除基础.快照（））；

标签删除工作.移除标签（「玩家」）；

定 标签删除：典 为 标签删除工作.变更集（）；

应拒绝且原子（标签删除基础，去变更字段（标签删除，0，「标签」），「标签删除缺字段不得修改原实体」）；

应拒绝且原子（标签删除基础，改变更字段（标签删除，0，「标签」，{}），「标签删除字段类型错误不得修改原实体」）；

定 组件基础 为 实体.新实体（「测试:角色/畸形组件」，「测试:原型/玩家」）；

定 组件工作 为 实体.恢复实体（组件基础.快照（））；

组件工作.设置组件（「核心:组件/位置」，{「房间」：「测试:房间/南门」}，1）；

定 组件设置：典 为 组件工作.变更集（）；

应拒绝且原子（组件基础，去变更字段（组件设置，0，「组件编号」），「组件设置缺编号不得修改原实体」）；

应拒绝且原子（组件基础，改变更字段（组件设置，0，「组件编号」，1），「组件设置编号类型错误不得修改原实体」）；

应拒绝且原子（组件基础，去变更字段（组件设置，0，「信封」），「组件设置缺信封不得修改原实体」）；

应拒绝且原子（组件基础，改变更字段（组件设置，0，「信封」，【】），「组件设置信封类型错误不得修改原实体」）；

应拒绝且原子（组件基础，改变更字段（组件设置，0，「信封」，{「版本」：1}），「组件设置信封缺数据不得修改原实体」）；

应拒绝且原子（组件基础，改变更字段（组件设置，0，「信封」，{「版本」：「一」，「数据」：{}}），「组件设置信封版本类型错误不得修改原实体」）；

定 组件删除基础 为 实体.恢复实体（组件工作.快照（））；

组件删除基础.确认变更（）；

定 组件删除工作 为 实体.恢复实体（组件删除基础.快照（））；

组件删除工作.移除组件（「核心:组件/位置」）；

定 组件删除：典 为 组件删除工作.变更集（）；

应拒绝且原子（组件删除基础，去变更字段（组件删除，0，「组件编号」），「组件删除缺编号不得修改原实体」）；

应拒绝且原子（组件删除基础，改变更字段（组件删除，0，「组件编号」，【】），「组件删除编号类型错误不得修改原实体」）；

应拒绝且原子（标签基础，改变更字段（标签添加，0，「动作」，「未知」），「未知动作不得修改原实体」）；

应拒绝且原子（标签基础，改变更字段（标签添加，0，「类别」，「未知」），「未知类别不得修改原实体」）；

应拒绝且原子（标签基础，改变更字段（标签添加，0，「动作」，1），「动作字段类型错误不得修改原实体」）；

定 原型工作 为 实体.恢复实体（标签基础.快照（））；

原型工作.设置原型（「测试:原型/访客」）；

定 原型设置：典 为 原型工作.变更集（）；

应拒绝且原子（标签基础，去变更字段（原型设置，0，「新值」），「原型设置缺新值不得修改原实体」）；

应拒绝且原子（标签基础，改变更字段（原型设置，0，「新值」，1），「原型编号类型错误不得修改原实体」）；

定 两项工作 为 实体.恢复实体（标签基础.快照（））；

两项工作.添加标签（「第一项」）.添加标签（「第二项」）；

定 后项畸形：典 为 去变更字段（两项工作.变更集（），1，「标签」）；

应拒绝且原子（标签基础，后项畸形，「后项畸形时不得应用前项」）；
