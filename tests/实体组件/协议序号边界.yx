引「../../src/实体/实体.yx」为 实体；

引「标准:测试」为 测试；

引「标准:数学」为 数学；

定 基础 为 实体.新实体（「测试:角色/序号边界」，「」）；

法 非普通扩展（） 则
    归 空；
终

定 非普通快照：典 为 基础.快照（）；

置 非普通快照【「扩展」】 为 非普通扩展；

定 非普通快照结果：典 为 实体.尝试恢复实体（非普通快照）；

测试.相等（非普通快照结果【「错误」】【「代码」】，实体.快照无效错误）；

定 空白标签快照：典 为 基础.快照（）；

置 空白标签快照【「标签」】 为 【「 标签 」】；

定 空白标签结果：典 为 实体.尝试恢复实体（空白标签快照）；

测试.相等（空白标签结果【「错误」】【「代码」】，实体.快照无效错误）；

定 标签边界 为 实体.新实体（「测试:角色/标签边界」，「」）；

令 已拒绝空白标签：理 为 假；

试 则
    标签边界.添加标签（「 标签 」）；
救 所误 则
    置 已拒绝空白标签 为 真；
终

测试.相等（已拒绝空白标签，真）；

测试.相等（标签边界.修订（），0）；

测试.相等（长度（标签边界.标签（）），0）；

定 小数修订快照：典 为 基础.快照（）；

置 小数修订快照【「修订」】 为 0.5；

定 小数修订结果：典 为 实体.尝试恢复实体（小数修订快照）；

测试.相等（小数修订结果【「成功」】，假）；

测试.相等（小数修订结果【「错误」】【「代码」】，实体.快照无效错误）；

定 非有限修订快照：典 为 基础.快照（）；

置 非有限修订快照【「修订」】 为 数学.幂（10，400）；

定 非有限修订结果：典 为 实体.尝试恢复实体（非有限修订快照）；

测试.相等（非有限修订结果【「错误」】【「代码」】，实体.快照无效错误）；

定 小数格式快照：典 为 基础.快照（）；

置 小数格式快照【「格式版本」】 为 1.5；

定 小数格式结果：典 为 实体.尝试恢复实体（小数格式快照）；

测试.相等（小数格式结果【「错误」】【「代码」】，实体.快照无效错误）；

定 小数组件版本：典 为 基础.尝试添加组件（「测试:组件/小数版本」，{「值」：1}，1.5）；

测试.相等（小数组件版本【「成功」】，假）；

测试.相等（小数组件版本【「错误」】【「代码」】，实体.快照无效错误）；

定 非有限组件版本：典 为 基础.尝试添加组件（「测试:组件/非有限版本」，{「值」：1}，数学.幂（10，400））；

测试.相等（非有限组件版本【「错误」】【「代码」】，实体.快照无效错误）；

定 工作 为 实体.恢复实体（基础.快照（））；

工作.添加标签（「测试」）；

定 小数变更集：典 为 工作.变更集（）；

定 非普通变更集：典 为 工作.变更集（）；

置 非普通变更集【「扩展」】 为 非普通扩展；

定 非普通变更结果：典 为 实体.尝试应用变更集（基础，非普通变更集）；

测试.相等（非普通变更结果【「错误」】【「代码」】，实体.变更集无效错误）；

置 小数变更集【「基础修订」】 为 0.5；

置 小数变更集【「结果修订」】 为 1.5；

定 小数变更结果：典 为 实体.尝试应用变更集（基础，小数变更集）；

测试.相等（小数变更结果【「成功」】，假）；

测试.相等（小数变更结果【「错误」】【「代码」】，实体.变更集无效错误）；

定 小数操作修订：典 为 工作.变更集（）；

置 小数操作修订【「变更」】【0】【「前修订」】 为 0.5；

定 小数操作结果：典 为 实体.尝试应用变更集（基础，小数操作修订）；

测试.相等（小数操作结果【「错误」】【「代码」】，实体.变更集无效错误）；

定 极限修订快照：典 为 实体.新实体（「测试:角色/原子边界」，「」）.快照（）；

置 极限修订快照【「修订」】 为 数学.幂（2，53）；

置 极限修订快照【「组件」】【「测试:组件/原有」】 为 {「版本」：1，「数据」：{「值」：「原有」}}；

定 原子边界 为 实体.恢复实体（极限修订快照）；

定 原子边界修订：数 为 原子边界.修订（）；

令 设置失败：理 为 假；

试 则
    原子边界.设置组件（「测试:组件/新增」，{「值」：「新增」}，1）；
救 所误 则
    置 设置失败 为 真；
终

测试.断言（设置失败，「无法可靠递增修订时必须拒绝组件设置」）；

测试.相等（原子边界.修订（），原子边界修订）；

测试.相等（原子边界.有组件（「测试:组件/新增」），假）；

令 移除失败：理 为 假；

试 则
    原子边界.移除组件（「测试:组件/原有」）；
救 所误 则
    置 移除失败 为 真；
终

测试.断言（移除失败，「无法可靠递增修订时必须拒绝组件移除」）；

测试.相等（原子边界.修订（），原子边界修订）；

测试.断言（原子边界.有组件（「测试:组件/原有」），「失败的组件移除不得改变实体状态」）；
