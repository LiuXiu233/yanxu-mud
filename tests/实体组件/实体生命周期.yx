引「../../src/实体/实体.yx」为 实体；

引「标准:测试」为 测试；

引「包:言据」为 言据；

定 玩家 为 实体.新实体（「测试:角色/子衿」，「测试:原型/玩家」）；

测试.相等（玩家.修订（），0）；

玩家.添加组件（「核心:组件/描述」，{「名称」：「子衿」}，1）；

玩家.添加标签（「在线」）；

测试.相等（玩家.修订（），2）；

测试.相等（玩家.取组件（「核心:组件/描述」）【「名称」】，「子衿」）；

测试.相等（玩家.有标签（「在线」），真）；

定 快照：典 为 玩家.快照（）；

定 恢复后 为 实体.恢复实体（快照）；

测试.断言（言据.相同（恢复后.快照（），快照），「实体快照应完整往返」）；

定 错误结果：典 为 恢复后.尝试添加组件（「核心:组件/描述」，{}，1）；

测试.相等（错误结果【「成功」】，假）；

测试.相等（错误结果【「错误」】【「代码」】，实体.组件已存在错误）；

定 移除对象 为 实体.新实体（「测试:物品/移除组件」，「」）；

移除对象.添加组件（「核心:组件/描述」，{「名称」：「待移除」}，1）；

测试.相等（移除对象.移除组件（「核心:组件/描述」），真）；

测试.相等（移除对象.修订（），2）；

测试.相等（移除对象.有组件（「核心:组件/描述」），假）；

测试.相等（移除对象.移除组件（「核心:组件/描述」），假）；

测试.相等（移除对象.修订（），2）；
