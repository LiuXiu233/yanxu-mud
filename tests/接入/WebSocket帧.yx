引「../../src/接入/WebSocket协议.yx」为 WebSocket；

引「标准:字节」为 字节；

引「标准:测试」为 测试；

法 客户端帧（操作：数，结束：理，载荷：字节串）：字节串 则
    定 输入：列<数> 为 字节.转数列（载荷）；
    令 首：数 为 操作；
    若 结束 则
        置 首 为 （首 加 128）；
    终
    令 输出：列<数> 为 【首】；
    若 （长度（输入） 小于 126） 则
        追加（输出，（128 加 长度（输入）））；
    否则
        追加（输出，254）；
        追加（输出，数学.下取整（（长度（输入） 除 256）））；
        追加（输出，（长度（输入） 减 （数学.下取整（（长度（输入） 除 256）） 乘 256）））；
    终
    逐 当前：数 于 【0，0，0，0】 则
        追加（输出，当前）；
    终
    逐 当前：数 于 输入 则
        追加（输出，当前）；
    终
    归 字节.从数列（输出）；
终

法 合并（各帧：列）：字节串 则
    令 输出：字节串 为 字节.从数列（【】）；
    逐 帧：字节串 于 各帧 则
        置 输出 为 字节.拼接（输出，帧）；
    终
    归 输出；
终

定 连接 为 WebSocket.WebSocket连接状态（{}）；

定 RFC帧：典 为 连接.接收（字节.从数列（【129，133，55，250，33，61，127，159，77，81，88】））；

测试.相等（RFC帧【「消息」】【0】【「文本」】，「Hello」）；

定 中文字节：列<数> 为 字节.转数列（字节.从文字（「你好，青石镇」））；

定 分片一：字节串 为 客户端帧（WebSocket.操作文本，假，字节.从数列（中文字节【0：5】））；

定 Ping：字节串 为 客户端帧（WebSocket.操作Ping，真，字节.从文字（「x」））；

定 分片二：字节串 为 客户端帧（WebSocket.操作继续，真，字节.从数列（中文字节【5：长度（中文字节）】））；

定 分片开始：典 为 连接.接收（合并（【分片一，Ping】））；

测试.相等（长度（分片开始【「消息」】），0）；

测试.相等（分片开始【「事件」】【0】【「种类」】，「Ping」）；

测试.相等（字节.转数列（分片开始【「发送」】【0】）【0】，138）；

定 分片完成：典 为 连接.接收（分片二）；

测试.相等（分片完成【「消息」】【0】【「文本」】，「你好，青石镇」）；

定 长文本：文 为 「aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa」；

定 长帧：列<数> 为 字节.转数列（WebSocket.编码文本（长文本））；

测试.相等（长帧【0】，129）；

测试.相等（长帧【1】，126）；

测试.相等（长帧【2】，0）；

测试.相等（长帧【3】，130）；

定 关闭连接 为 WebSocket.WebSocket连接状态（{}）；

定 关闭载荷：字节串 为 字节.从数列（【3，232】）；

定 关闭：典 为 关闭连接.接收（客户端帧（WebSocket.操作关闭，真，关闭载荷））；

测试.相等（关闭【「关闭」】，真）；

测试.相等（关闭【「事件」】【0】【「代码」】，1000）；

测试.相等（字节.转数列（关闭【「发送」】【0】）【0】，136）；
