引「../../src/接入/接入网关.yx」为 网关；

引「../../src/会话/会话服务.yx」为 会话；

引「../../src/消息/消息协议.yx」为 消息；

引「标准:哈希」为 哈希；

引「标准:JSON」为 JSON；

引「标准:测试」为 测试；

引「标准:文字」为 文字；

令 编号：数 为 0；

法 生成编号（类别：文）：文 则
    置 编号 为 （编号 加 1）；
    归 （（「测试:」 加 类别） 加 （「/」 加 JSON.序列化（编号）））；
终

令 令牌：数 为 0；

法 生成令牌（）：文 则
    置 令牌 为 （令牌 加 1）；
    归 （「secret-resume-」 加 JSON.序列化（令牌））；
终

法 摘要（明文：文）：文 则
    归 哈希.SHA256（明文）；
终

法 登录（登录名：文，密码：文，当前：数）：典 则
    若 （（登录名 不等于 「qinghe」） 或 （密码 不等于 「可靠密码」）） 则
        归 {「成功」：假，「错误」：{「代码」：「TEST_LOGIN_DENIED」，「消息」：（「不得回显：」 加 密码），「详情」：{「密码」：密码}}}；
    终
    归 {「成功」：真，「账户」：{「账户编号」：「测试:账户/青禾」，「登录名」：「qinghe」，「角色」：【「测试:角色/青禾」，「测试:角色/青苗」】}，「消息」：【】}；
终

法 角色归属（账户编号：文，角色编号：文）：理 则
    归 （（账户编号 等于 「测试:账户/青禾」） 且 包含（【「测试:角色/青禾」，「测试:角色/青苗」】，角色编号））；
终

法 执行命令（所会话：典，命令文：文，请求编号：文，当前：数）：典 则
    若 （命令文 等于 「观察」） 则
        归 {「成功」：真，「数据」：{「角色编号」：所会话【「角色编号」】}，「消息」：【消息.新消息（【消息.文字（「中央街」）】，{「请求编号」：请求编号}）】}；
    终
    归 {「成功」：假，「错误」：{「代码」：「TEST_COMMAND」，「消息」：「未知命令」，「详情」：{}}，「消息」：【】}；
终

法 含片（原文：文，片：文）：理 则
    归 （长度（文字.分割（原文，片）） 大于 1）；
终

定 会话服务 为 会话.会话服务（生成编号，生成令牌，摘要，{}）；

定 接入 为 网关.接入网关（会话服务，登录，角色归属，执行命令，{}）；

接入.打开连接（「ws-1」，「WebSocket」，「127.0.0.1:30001」，1）；

定 协商：典 为 接入.处理Web文本（「ws-1」，JSON.序列化（{「种类」：「协商」，「请求编号」：「req-1」，「能力」：【「请求编号」，「未知」】}），2）；

测试.相等（协商【「成功」】，真）；

测试.相等（协商【「请求编号」】，「req-1」）；

测试.相等（协商【「数据」】【「能力」】【0】，「请求编号」）；

定 登录拒绝：典 为 接入.处理Web文本（「ws-1」，JSON.序列化（{「种类」：「登录」，「请求编号」：「req-2」，「登录名」：「qinghe」，「密码」：「错误秘密」}），3）；

测试.相等（登录拒绝【「成功」】，假）；

测试.相等（登录拒绝【「错误」】【「代码」】，「TEST_LOGIN_DENIED」）；

测试.断言（非 含片（JSON.序列化（登录拒绝），「错误秘密」），「登录失败响应不得回显凭据」）；

定 已登录：典 为 接入.处理Web文本（「ws-1」，JSON.序列化（{「种类」：「登录」，「请求编号」：「req-3」，「登录名」：「qinghe」，「密码」：「可靠密码」，「角色编号」：「测试:角色/青禾」}），4）；

测试.相等（已登录【「成功」】，真）；

测试.相等（已登录【「数据」】【「会话」】【「角色编号」】，「测试:角色/青禾」）；

定 第一令牌：文 为 已登录【「数据」】【「恢复令牌」】；

定 命令结果：典 为 接入.处理Web文本（「ws-1」，JSON.序列化（{「种类」：「命令」，「请求编号」：「req-4」，「命令」：「观察」}），5）；

测试.相等（命令结果【「成功」】，真）；

测试.相等（命令结果【「数据」】【「角色编号」】，「测试:角色/青禾」）；

测试.相等（命令结果【「消息」】【0】【「节点」】【0】【「内容」】，「中央街」）；

测试.相等（接入.排队输出（「ws-1」，命令结果，128，5），真）；

定 已取输出：列 为 接入.取出输出（「ws-1」，4，1024）；

测试.相等（长度（已取输出），1）；

测试.相等（已取输出【0】【「载荷」】【「请求编号」】，「req-4」）；

接入.断开连接（「ws-1」，6，「测试断线」）；

接入.打开连接（「ws-2」，「WebSocket」，「127.0.0.1:30002」，7）；

定 已恢复：典 为 接入.处理Web文本（「ws-2」，JSON.序列化（{「种类」：「恢复」，「请求编号」：「req-5」，「恢复令牌」：第一令牌，「能力」：【「会话恢复」】}），8）；

测试.相等（已恢复【「成功」】，真）；

测试.断言（（已恢复【「数据」】【「恢复令牌」】 不等于 第一令牌），「恢复必须轮换令牌」）；

定 坏JSON：典 为 接入.处理Web文本（「ws-2」，「{」，9）；

测试.相等（坏JSON【「错误」】【「代码」】，网关.JSON错误）；

测试.断言（非 含片（JSON.序列化（会话服务.各审计（）），第一令牌），「会话审计不得含接入令牌」）；
