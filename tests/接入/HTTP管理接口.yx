引「../../src/接入/HTTP管理.yx」为 HTTP管理；

引「../../src/接入/接入网关.yx」为 网关；

引「../../src/会话/会话服务.yx」为 会话；

引「包:yanxu-http」为 HTTP；

引「标准:JSON」为 JSON；

引「标准:字节」为 字节；

引「标准:测试」为 测试；

引「标准:文字」为 文字；

令 编号：数 为 0；

法 新编号（类别：文）：文 则
    置 编号 为 （编号 加 1）；
    归 （（「测试:」 加 类别） 加 （「/」 加 JSON.序列化（编号）））；
终

法 新令牌（）：文 则
    归 （「http-resume-」 加 JSON.序列化（编号））；
终

法 摘要（令牌：文）：文 则
    归 （「digest-」 加 令牌）；
终

法 登录（登录名：文，密码：文，当前：数）：典 则
    若 （（登录名 等于 「qinghe」） 且 （密码 等于 「safe-password」）） 则
        归 {「成功」：真，「账户」：{「账户编号」：「测试:账户/青禾」，「登录名」：「qinghe」，「角色」：【「测试:角色/青禾」】}}；
    终
    归 {「成功」：假，「错误」：{「代码」：「LOGIN_DENIED」}}；
终

法 归属（账户编号：文，角色编号：文）：理 则
    归 （（账户编号 等于 「测试:账户/青禾」） 且 （角色编号 等于 「测试:角色/青禾」））；
终

法 命令（会话值：典，原文：文，请求编号：文，当前：数）：典 则
    归 {「成功」：真，「数据」：{}，「消息」：【】}；
终

令 当前：数 为 100；

法 时钟（）：数 则
    置 当前 为 （当前 加 1）；
    归 当前；
终

法 服务器信息（）：典 则
    归 {「名称」：「言域」，「版本」：「0.7.0-dev」}；
终

法 在线摘要（）：典 则
    归 {「连接」：0，「会话」：1}；
终

法 内容状态（）：典 则
    归 {「摘要」：「content-hash」}；
终

法 热重载（输入）：典 则
    归 {「已应用」：真，「输入」：输入}；
终

法 创建快照（输入）：典 则
    归 {「快照编号」：「snapshot-1」}；
终

法 日志查询（输入）：典 则
    归 {「项目」：【】，「查询」：输入}；
终

法 诊断（）：典 则
    归 {「状态」：「ok」}；
终

法 管理认证（首部：文，操作：文）：理 则
    归 （首部 等于 「Bearer admin-test-token」）；
终

定 会话服务 为 会话.会话服务（新编号，新令牌，摘要，{}）；

定 接入 为 网关.接入网关（会话服务，登录，归属，命令，{}）；

定 服务：典 为 {「服务器信息」：服务器信息，「在线摘要」：在线摘要，「内容状态」：内容状态，「热重载」：热重载，「创建快照」：创建快照，「日志查询」：日志查询，「诊断」：诊断}；

定 管理 为 HTTP管理.HTTP管理应用（接入，新编号，时钟，服务，管理认证，{「客户端根」：「examples/青石镇/web」}）；

定 客户端 为 管理.测试客户端（）；

定 首页 为 客户端.取（「/」）；

测试.相等（首页.状态码，200）；

测试.相等（首页.首部值（「cache-control」），「no-store」）；

测试.断言（（首页.首部值（「content-security-policy」） 是 文），「首页必须携带内容安全策略」）；

定 样式 为 客户端.取（「/assets/styles.css」）；

测试.相等（样式.状态码，200）；

测试.相等（样式.首部值（「content-type」），「text/css; charset=utf-8」）；

测试.相等（样式.首部值（「cache-control」），「no-store」）；

定 越界静态 为 客户端.取（「/assets/../言序.toml」）；

测试.相等（越界静态.状态码，404）；

定 健康 为 客户端.取（「/healthz」）；

测试.相等（健康.状态码，200）；

测试.相等（JSON.解析（字节.转文字（健康.正文））【「数据」】【「状态」】，「healthy」）；

测试.相等（健康.首部值（「cache-control」），「no-store」）；

定 信息 为 客户端.取（「/api/v1/server」）；

测试.相等（信息.状态码，200）；

测试.相等（JSON.解析（字节.转文字（信息.正文））【「数据」】【「名称」】，「言域」）；

定 WS入口 为 客户端.取（「/api/v1/ws」）；

测试.相等（WS入口.状态码，426）；

测试.相等（WS入口.首部值（「sec-websocket-version」），「13」）；

定 登录响应 为 客户端.发JSON（「/api/v1/login」，{「请求编号」：「http-login」，「登录名」：「qinghe」，「密码」：「safe-password」}）；

测试.相等（登录响应.状态码，200）；

定 登录体：典 为 JSON.解析（字节.转文字（登录响应.正文））；

测试.相等（登录体【「成功」】，真）；

测试.相等（登录体【「数据」】【「会话」】【「状态」】，「已连接」）；

测试.断言（（长度（登录体【「数据」】【「恢复令牌」】） 大于 5），「HTTP 登录必须返回恢复令牌」）；

定 未授权 为 客户端.发JSON（「/api/v1/admin/hot-reload」，{「摘要」：「new」}）；

测试.相等（未授权.状态码，403）；

定 正文：文 为 JSON.序列化（{「摘要」：「new」}）；

定 正文字节：字节串 为 字节.从文字（正文）；

定 正文长度：文 为 文字.替换（JSON.序列化（字节.长度（正文字节）），「.0」，「」）；

定 管理请求 为 HTTP.解析请求（（「POST /api/v1/admin/hot-reload HTTP/1.1\nHost: testserver\nContent-Type: application/json\nAuthorization: Bearer admin-test-token\nContent-Length: 」 加 正文长度），正文字节）；

定 已授权 为 管理.应用.处理（管理请求）；

测试.相等（已授权.状态码，200）；

测试.相等（JSON.解析（字节.转文字（已授权.正文））【「数据」】【「已应用」】，真）；
