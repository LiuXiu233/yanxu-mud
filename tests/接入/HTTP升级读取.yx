引「../../src/接入/HTTP升级.yx」为 升级；

引「标准:字节」为 字节；

引「标准:测试」为 测试；

定 状态 为 升级.HTTP升级读取状态（1024）；

定 第一：典 为 状态.接收（字节.从文字（「GET /ws HTTP/1.1\nHost: local」））；

测试.相等（第一【「完成」】，假）；

定 帧尾：列<数> 为 【129，129，0，0，0，0，65】；

定 第二输入：字节串 为 字节.拼接（字节.从文字（「host\nUpgrade: websocket\nConnection: Upgrade\nSec-WebSocket-Key: dGhlIHNhbXBsZSBub25jZQ==\nSec-WebSocket-Version: 13\n\n」），字节.从数列（帧尾））；

定 第二：典 为 状态.接收（第二输入）；

测试.相等（第二【「完成」】，真）；

测试.相等（第二【「成功」】，真）；

测试.相等（第二【「请求」】.路径，「/ws」）；

测试.相等（字节.转数列（第二【「剩余」】）【0】，129）；

测试.相等（字节.长度（第二【「剩余」】），7）；

定 坏状态 为 升级.HTTP升级读取状态（1024）；

定 坏请求：典 为 坏状态.接收（字节.从文字（「BROKEN\n\n」））；

测试.相等（坏请求【「完成」】，真）；

测试.相等（坏请求【「成功」】，假）；

测试.相等（坏请求【「错误」】【「代码」】，升级.请求错误）；

定 过长状态 为 升级.HTTP升级读取状态（1024）；

令 过长：文 为 「」；

令 位置：数 为 0；

当 （位置 小于 1025） 则
    置 过长 为 （过长 加 「a」）；
    置 位置 为 （位置 加 1）；
终

定 过长结果：典 为 过长状态.接收（字节.从文字（过长））；

测试.相等（过长结果【「错误」】【「代码」】，升级.预算错误）；

定 截断状态 为 升级.HTTP升级读取状态（1024）；

截断状态.接收（字节.从文字（「GET / HTTP/1.1\n」））；

测试.相等（截断状态.连接结束（）【「错误」】【「代码」】，升级.请求错误）；
