引「../../src/接入/WebSocket协议.yx」为 WebSocket；

引「标准:字节」为 字节；

引「标准:测试」为 测试；

法 零掩码帧（首：数，载荷：列<数>）：字节串 则
    令 输出：列<数> 为 【首，（128 加 长度（载荷）），0，0，0，0】；
    逐 当前：数 于 载荷 则
        追加（输出，当前）；
    终
    归 字节.从数列（输出）；
终

定 未掩码连接 为 WebSocket.WebSocket连接状态（{}）；

定 未掩码：典 为 未掩码连接.接收（字节.从数列（【129，1，65】））；

测试.相等（未掩码【「关闭」】，真）；

测试.相等（未掩码【「错误」】【「代码」】，WebSocket.帧错误）；

定 RSV连接 为 WebSocket.WebSocket连接状态（{}）；

定 RSV：典 为 RSV连接.接收（零掩码帧（193，【65】））；

测试.相等（RSV【「关闭」】，真）；

定 UTF8连接 为 WebSocket.WebSocket连接状态（{}）；

定 坏UTF8：典 为 UTF8连接.接收（零掩码帧（129，【194】））；

测试.相等（坏UTF8【「关闭」】，真）；

测试.相等（坏UTF8【「错误」】【「代码」】，WebSocket.UTF8错误）；

定 关闭UTF8连接 为 WebSocket.WebSocket连接状态（{}）；

定 坏关闭UTF8：典 为 关闭UTF8连接.接收（零掩码帧（136，【3，232，194】））；

测试.相等（坏关闭UTF8【「关闭」】，真）；

测试.相等（坏关闭UTF8【「错误」】【「代码」】，WebSocket.UTF8错误）；

定 小连接 为 WebSocket.WebSocket连接状态（{「最大帧字节」：4}）；

定 过大：典 为 小连接.接收（零掩码帧（129，【1，2，3，4，5】））；

测试.相等（过大【「关闭」】，真）；

测试.相等（过大【「错误」】【「代码」】，WebSocket.消息过大错误）；

定 二进制连接 为 WebSocket.WebSocket连接状态（{}）；

定 二进制：典 为 二进制连接.接收（零掩码帧（130，【1】））；

测试.相等（二进制【「关闭」】，真）；

定 继续连接 为 WebSocket.WebSocket连接状态（{}）；

定 错继续：典 为 继续连接.接收（零掩码帧（128，【1】））；

测试.相等（错继续【「关闭」】，真）；

定 非最短连接 为 WebSocket.WebSocket连接状态（{}）；

定 非最短：典 为 非最短连接.接收（字节.从数列（【129，254，0，1，0，0，0，0，65】））；

测试.相等（非最短【「关闭」】，真）；

测试.相等（非最短【「错误」】【「代码」】，WebSocket.帧错误）；

定 高位连接 为 WebSocket.WebSocket连接状态（{}）；

定 高位长度：典 为 高位连接.接收（字节.从数列（【129，255，128，0，0，0，0，0，0，0】））；

测试.相等（高位长度【「关闭」】，真）；

测试.相等（高位长度【「错误」】【「代码」】，WebSocket.帧错误）；

定 洪泛连接 为 WebSocket.WebSocket连接状态（{「单次最大帧数」：1}）；

定 洪泛：典 为 洪泛连接.接收（字节.拼接（零掩码帧（129，【65】），零掩码帧（129，【66】）））；

测试.相等（洪泛【「关闭」】，真）；

测试.相等（洪泛【「错误」】【「代码」】，WebSocket.消息过大错误）；
