引「../../src/命令/命令协议.yx」为 命令；

引「标准:数学」为 数学；

引「标准:测试」为 测试；

引「标准:文字」为 文字；

法 空成功（上下文：典，调用：典） 则
    归 {「世界时间」：上下文【「世界时间」】}；
终

定 注册 为 命令.注册表（）；

注册.注册（命令.新定义（{「编号」：「测试:命令/数值边界」，「名称」：「数值边界」，「冷却」：5}，空成功））；

定 深度执行 为 命令.执行器（注册，空）；

定 非有限数：数 为 数学.幂（10，400）；

定 非数：数 为 （非有限数 减 非有限数）；

法 拒绝深度（所值：数）：理 则
    试 则
        深度执行.设置最大执行深度（所值）；
    救 所误 则
        归 文字.始于（所误.消息，命令.上下文错误）；
    终
    归 假；
终

测试.断言（拒绝深度（0），「零层深度必须被拒绝」）；

测试.断言（拒绝深度（1.5），「小数深度必须被拒绝」）；

测试.断言（拒绝深度（非有限数），「无穷大深度必须被拒绝」）；

测试.断言（拒绝深度（非数），「非数深度必须被拒绝」）；

深度执行.设置最大执行深度（1）；

令 现在：数 为 非有限数；

法 可变时钟（上下文：典）：数 则
    归 现在；
终

定 时钟执行 为 命令.执行器（注册，可变时钟）；

定 非有限时钟：典 为 时钟执行.执行输入（「数值边界」，{「主体编号」：「测试:角色/时钟」}）；

测试.相等（非有限时钟【「错误」】【「代码」】，命令.上下文错误）；

置 现在 为 非数；

定 非数时钟：典 为 时钟执行.执行输入（「数值边界」，{「主体编号」：「测试:角色/时钟」}）；

测试.相等（非数时钟【「错误」】【「代码」】，命令.上下文错误）；

置 现在 为 减 1；

定 负时钟：典 为 时钟执行.执行输入（「数值边界」，{「主体编号」：「测试:角色/时钟」}）；

测试.相等（负时钟【「错误」】【「代码」】，命令.上下文错误）；

置 现在 为 10.5；

定 小数时钟：典 为 时钟执行.执行输入（「数值边界」，{「主体编号」：「测试:角色/时钟」}）；

测试.相等（小数时钟【「成功」】，真）；

测试.相等（小数时钟【「数据」】【「世界时间」】，10.5）；

置 现在 为 12；

定 小数冷却：典 为 时钟执行.执行输入（「数值边界」，{「主体编号」：「测试:角色/时钟」}）；

测试.相等（小数冷却【「错误」】【「代码」】，命令.冷却错误）；

测试.相等（小数冷却【「错误」】【「详情」】【「剩余」】，3.5）；

定 显式执行 为 命令.执行器（注册，空）；

定 负显式时间：典 为 显式执行.执行输入（「数值边界」，{「主体编号」：「测试:角色/显式」，「世界时间」：减 0.5}）；

测试.相等（负显式时间【「错误」】【「代码」】，命令.上下文错误）；

定 小数显式时间：典 为 显式执行.执行输入（「数值边界」，{「主体编号」：「测试:角色/显式」，「世界时间」：20.25}）；

测试.相等（小数显式时间【「成功」】，真）；
