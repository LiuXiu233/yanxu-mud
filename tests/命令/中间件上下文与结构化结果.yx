引「../../src/命令/命令协议.yx」为 命令；

引「../../src/消息/消息协议.yx」为 消息；

引「标准:测试」为 测试；

引「包:言据」为 言据；

令 轨迹：列<文> 为 【】；

法 全局件（上下文：典，调用：典，下一步） 则
    追加（轨迹，「全局前」）；
    置 上下文【「经过全局」】 为 真；
    定 结果 为 下一步（）；
    追加（轨迹，「全局后」）；
    归 结果；
终

法 命令件（上下文：典，调用：典，下一步） 则
    追加（轨迹，「命令前」）；
    置 调用【「位置参数」】【0】 为 「中间件改写」；
    定 结果 为 下一步（）；
    追加（轨迹，「命令后」）；
    归 结果；
终

法 处理（上下文：典，调用：典） 则
    追加（轨迹，「处理」）；
    归 {「成功」：真，「数据」：{「经过全局」：上下文【「经过全局」】，「会话编号」：上下文【「会话编号」】，「参数」：调用【「位置参数」】，「上下文参数」：上下文【「调用」】【「位置参数」】}，「消息」：【消息.新消息（【消息.文字（「问候」）】，{「键」：「测试.问候」}）】}；
终

定 定义 为 命令.新定义（{「编号」：「测试:命令/问候」，「名称」：「问候」}，处理）；

定义.使用（命令件）；

定 注册 为 命令.注册表（）；

注册.注册（定义）；

定 执行 为 命令.执行器（注册，空）；

执行.使用（全局件）；

定 结果：典 为 执行.执行输入（「问候 子衿」，{「会话编号」：「测试:会话/一」，「世界时间」：7}）；

测试.相等（结果【「成功」】，真）；

测试.相等（结果【「数据」】【「经过全局」】，真）；

测试.相等（结果【「数据」】【「会话编号」】，「测试:会话/一」）；

测试.相等（结果【「数据」】【「参数」】【0】，「中间件改写」）；

测试.相等（结果【「数据」】【「上下文参数」】【0】，「中间件改写」）；

测试.相等（结果【「消息」】【0】【「节点」】【0】【「种类」】，消息.种类文字）；

测试.相等（轨迹【0】，「全局前」）；

测试.相等（轨迹【1】，「命令前」）；

测试.相等（轨迹【2】，「处理」）；

测试.相等（轨迹【3】，「命令后」）；

测试.相等（轨迹【4】，「全局后」）；

测试.断言（言据.相同（结果，言据.解析据（言据.序列化（结果））），「命令结果必须可稳定序列化」）；

定 未知：典 为 执行.执行输入（「不存在」，{}）；

测试.相等（未知【「成功」】，假）；

测试.相等（未知【「错误」】【「代码」】，命令.命令不存在错误）；

定 坏调用：典 为 执行.执行调用（{「协议」：「言域/命令调用」，「版本」：99，「名称」：「问候」，「位置参数」：【1】}，{}）；

测试.相等（坏调用【「成功」】，假）；

测试.相等（坏调用【「错误」】【「代码」】，命令.解析错误）；

定 缺原文调用：典 为 执行.执行调用（{「协议」：「言域/命令调用」，「版本」：1，「名称」：「问候」，「位置参数」：【「子衿」】}，{}）；

测试.相等（缺原文调用【「错误」】【「代码」】，命令.解析错误）；

法 坏处理（上下文：典，调用：典） 则
    抛 「处理器故障」；
终

定 坏注册 为 命令.注册表（）；

坏注册.注册（命令.新定义（{「编号」：「测试:命令/坏」，「名称」：「坏」}，坏处理））；

定 坏执行 为 命令.执行器（坏注册，空）；

定 坏结果：典 为 坏执行.执行输入（「坏」，{}）；

测试.相等（坏结果【「错误」】【「代码」】，命令.处理器错误）；

定 原调用：典 为 {「协议」：「言域/命令调用」，「版本」：1，「原文」：「问候 原值」，「名称」：「问候」，「位置参数」：【「原值」】}；

执行.执行调用（原调用，{「会话编号」：「测试:会话/二」，「世界时间」：8}）；

测试.相等（原调用【「位置参数」】【0】，「原值」）；

法 非法成功字段（上下文：典，调用：典） 则
    归 {「成功」：1，「数据」：{}}；
终

定 非法注册 为 命令.注册表（）；

非法注册.注册（命令.新定义（{「编号」：「测试:命令/非法结果」，「名称」：「非法结果」}，非法成功字段））；

定 非法执行 为 命令.执行器（非法注册，空）；

定 非法结果：典 为 非法执行.执行输入（「非法结果」，{}）；

测试.相等（非法结果【「错误」】【「代码」】，命令.结果错误）；

法 裸消息处理（上下文：典，调用：典） 则
    归 {「成功」：真，「数据」：{}，「消息」：【「裸文字」】}；
终

定 裸消息注册 为 命令.注册表（）；

裸消息注册.注册（命令.新定义（{「编号」：「测试:命令/裸消息」，「名称」：「裸消息」}，裸消息处理））；

定 裸消息结果：典 为 命令.执行器（裸消息注册，空）.执行输入（「裸消息」，{}）；

测试.相等（裸消息结果【「错误」】【「代码」】，命令.结果错误）；

令 处理次数：数 为 0；

法 计数处理（上下文：典，调用：典） 则
    置 处理次数 为 （处理次数 加 1）；
    归 {}；
终

法 重复下一步（上下文：典，调用：典，下一步） 则
    定 第一结果 为 下一步（）；
    下一步（）；
    归 第一结果；
终

定 重复定义 为 命令.新定义（{「编号」：「测试:命令/重复下一步」，「名称」：「重复下一步」}，计数处理）；

重复定义.使用（重复下一步）；

定 重复注册 为 命令.注册表（）；

重复注册.注册（重复定义）；

定 重复结果：典 为 命令.执行器（重复注册，空）.执行输入（「重复下一步」，{}）；

测试.相等（重复结果【「错误」】【「代码」】，命令.中间件错误）；

测试.相等（处理次数，1）；

令 逃逸下一步 为 空；

法 保存下一步（上下文：典，调用：典，下一步） 则
    置 逃逸下一步 为 下一步；
    归 {}；
终

定 逃逸定义 为 命令.新定义（{「编号」：「测试:命令/逃逸下一步」，「名称」：「逃逸下一步」}，计数处理）；

逃逸定义.使用（保存下一步）；

定 逃逸注册 为 命令.注册表（）；

逃逸注册.注册（逃逸定义）；

定 逃逸结果：典 为 命令.执行器（逃逸注册，空）.执行输入（「逃逸下一步」，{}）；

测试.相等（逃逸结果【「成功」】，真）；

令 已拒绝逃逸：理 为 假；

试 则
    逃逸下一步（）；
救 所误 则
    置 已拒绝逃逸 为 真；
终

测试.相等（已拒绝逃逸，真）；

测试.相等（处理次数，1）；

法 环结果（上下文：典，调用：典） 则
    令 数据：典 为 {}；
    置 数据【「自己」】 为 数据；
    归 数据；
终

定 环注册 为 命令.注册表（）；

环注册.注册（命令.新定义（{「编号」：「测试:命令/环结果」，「名称」：「环结果」}，环结果））；

定 环结果值：典 为 命令.执行器（环注册，空）.执行输入（「环结果」，{}）；

测试.相等（环结果值【「错误」】【「代码」】，命令.结果错误）；

令 已拒绝坏定义：理 为 假；

试 则
    命令.新定义（{「编号」：「测试:命令/坏处理器」，「名称」：「坏处理器」}，「不是函数」）；
救 所误 则
    置 已拒绝坏定义 为 真；
终

测试.相等（已拒绝坏定义，真）；

令 已拒绝坏配置：理 为 假；

法 坏配置回调（） 则
    归 空；
终

试 则
    命令.新定义（{「编号」：「测试:命令/坏配置」，「名称」：「坏配置」，「扩展」：坏配置回调}，计数处理）；
救 所误 则
    置 已拒绝坏配置 为 真；
终

测试.相等（已拒绝坏配置，真）；

令 重入执行 为 空；

令 重入次数：数 为 0；

法 重入处理（上下文：典，调用：典） 则
    置 重入次数 为 （重入次数 加 1）；
    归 重入执行.执行输入（「重入」，{「主体编号」：「测试:角色/重入」，「世界时间」：100}）；
终

定 重入注册 为 命令.注册表（）；

重入注册.注册（命令.新定义（{「编号」：「测试:命令/重入」，「名称」：「重入」，「冷却」：5}，重入处理））；

置 重入执行 为 命令.执行器（重入注册，空）；

定 重入结果：典 为 重入执行.执行输入（「重入」，{「主体编号」：「测试:角色/重入」，「世界时间」：100}）；

测试.相等（重入结果【「错误」】【「代码」】，命令.重入错误）；

测试.相等（重入次数，1）；

定 再次重入结果：典 为 重入执行.执行输入（「重入」，{「主体编号」：「测试:角色/重入」，「世界时间」：100}）；

测试.相等（再次重入结果【「错误」】【「代码」】，命令.重入错误）；

测试.相等（重入次数，2）；

令 深度执行 为 空；

令 深度次数：数 为 0；

法 深度处理（上下文：典，调用：典） 则
    置 深度次数 为 （深度次数 加 1）；
    归 深度执行.执行输入（「深度」，{}）；
终

定 深度注册 为 命令.注册表（）；

深度注册.注册（命令.新定义（{「编号」：「测试:命令/深度」，「名称」：「深度」}，深度处理））；

置 深度执行 为 命令.执行器（深度注册，空）；

深度执行.设置最大执行深度（3）；

定 深度结果：典 为 深度执行.执行输入（「深度」，{}）；

测试.相等（深度结果【「错误」】【「代码」】，命令.递归超限错误）；

测试.相等（深度次数，3）；

令 碰撞执行 为 空；

法 碰撞甲处理（上下文：典，调用：典） 则
    归 碰撞执行.执行输入（「碰撞乙」，{「主体编号」：「x」，「世界时间」：500}）；
终

法 碰撞乙处理（上下文：典，调用：典） 则
    归 {「命令」：「乙已执行」}；
终

定 碰撞注册 为 命令.注册表（）；

碰撞注册.注册（命令.新定义（{「编号」：「pkg:命令/z」，「名称」：「碰撞甲」，「冷却」：5}，碰撞甲处理））；

碰撞注册.注册（命令.新定义（{「编号」：「y|pkg:命令/z」，「名称」：「碰撞乙」，「冷却」：5}，碰撞乙处理））；

置 碰撞执行 为 命令.执行器（碰撞注册，空）；

定 碰撞结果：典 为 碰撞执行.执行输入（「碰撞甲」，{「主体编号」：「x|y」，「世界时间」：500}）；

测试.相等（碰撞结果【「成功」】，真）；

测试.相等（碰撞结果【「数据」】【「命令」】，「乙已执行」）；
