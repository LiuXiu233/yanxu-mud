引「../../src/系统/系统注册表.yx」为 系统；

引「标准:测试」为 测试；

引「标准:文字」为 文字；

令 记录：列<文> 为 【】；

法 旧处理（上下文）：文 则
    追加（记录，「旧处理」）；
    归 「旧」；
终

法 新处理（上下文）：文 则
    追加（记录，「新处理」）；
    归 「新」；
终

法 旧启动（上下文）：空 则
    追加（记录，（「旧启动:」 加 上下文【「代」】））；
    归 空；
终

法 旧停止（上下文）：空 则
    追加（记录，（「旧停止:」 加 上下文【「代」】））；
    归 空；
终

法 新启动（上下文）：空 则
    追加（记录，（「新启动:」 加 上下文【「代」】））；
    归 空；
终

法 新停止（上下文）：空 则
    追加（记录，（「新停止:」 加 上下文【「代」】））；
    归 空；
终

定 注册 为 系统.注册表（）；

定 旧实例 为 系统.系统（「测试:系统/可替换」，10，旧处理，旧启动，旧停止）；

注册.注册（旧实例）；

注册.启动全部（{「代」：「甲」}）；

定 新实例 为 系统.系统（「测试:系统/可替换」，10，新处理，新启动，新停止）；

注册.替换（新实例）；

测试.相等（记录【0】，「旧启动:甲」）；

测试.相等（记录【1】，「新启动:甲」）；

测试.相等（记录【2】，「旧停止:甲」）；

测试.相等（旧实例.状态（），「已停止」）；

测试.相等（新实例.状态（），「运行中」）；

定 替换结果：列 为 注册.处理全部（{}）；

测试.相等（替换结果【0】【「结果」】，「新」）；

注册.停止全部（{「代」：「乙」}）；

测试.相等（记录【4】，「新停止:乙」）；

令 启动失败候选已清理：理 为 假；

法 候选启动失败（上下文）：空 则
    抛 「候选启动失败」；
终

法 清理启动失败候选（上下文）：空 则
    置 启动失败候选已清理 为 真；
    归 空；
终

定 启动失败注册 为 系统.注册表（）；

定 启动失败旧实例 为 系统.系统（「测试:系统/启动失败回滚」，10，旧处理，空，空）；

启动失败注册.注册（启动失败旧实例）.启动全部（{}）；

定 启动失败新实例 为 系统.系统（「测试:系统/启动失败回滚」，10，新处理，候选启动失败，清理启动失败候选）；

令 已拒绝启动失败替换：理 为 假；

试 则
    启动失败注册.替换（启动失败新实例）；
救 所误 则
    置 已拒绝启动失败替换 为 文字.始于（所误.消息，系统.生命周期错误）；
终

测试.相等（已拒绝启动失败替换，真）；

测试.相等（启动失败候选已清理，真）；

测试.相等（启动失败旧实例.状态（），「运行中」）；

测试.相等（启动失败新实例.状态（），「已停止」）；

测试.相等（启动失败注册.处理全部（{}）【0】【「结果」】，「旧」）；

启动失败注册.停止全部（{}）；

令 旧停止次数：数 为 0；

令 回滚候选已停止：理 为 假；

法 旧停止先失败（上下文）：空 则
    置 旧停止次数 为 （旧停止次数 加 1）；
    若 （旧停止次数 等于 1） 则
        抛 「旧停止失败」；
    终
    归 空；
终

法 回滚候选停止（上下文）：空 则
    置 回滚候选已停止 为 真；
    归 空；
终

定 停止失败注册 为 系统.注册表（）；

定 停止失败旧实例 为 系统.系统（「测试:系统/停止失败回滚」，10，旧处理，空，旧停止先失败）；

停止失败注册.注册（停止失败旧实例）.启动全部（{}）；

定 停止失败新实例 为 系统.系统（「测试:系统/停止失败回滚」，10，新处理，空，回滚候选停止）；

令 已拒绝停止失败替换：理 为 假；

试 则
    停止失败注册.替换（停止失败新实例）；
救 所误 则
    置 已拒绝停止失败替换 为 文字.始于（所误.消息，系统.生命周期错误）；
终

测试.相等（已拒绝停止失败替换，真）；

测试.相等（回滚候选已停止，真）；

测试.相等（停止失败旧实例.状态（），「运行中」）；

测试.相等（停止失败新实例.状态（），「已停止」）；

测试.相等（停止失败注册.处理全部（{}）【0】【「结果」】，「旧」）；

停止失败注册.停止全部（{}）；

定 外部启动注册 为 系统.注册表（）；

定 外部启动旧实例 为 系统.系统（「测试:系统/外部启动」，10，旧处理，空，空）；

外部启动注册.注册（外部启动旧实例）；

外部启动旧实例.启动（{}）；

定 外部启动新实例 为 系统.系统（「测试:系统/外部启动」，10，新处理，空，空）；

令 已拒绝外部启动替换：理 为 假；

试 则
    外部启动注册.替换（外部启动新实例）；
救 所误 则
    置 已拒绝外部启动替换 为 文字.始于（所误.消息，系统.生命周期错误）；
终

测试.相等（已拒绝外部启动替换，真）；

测试.相等（外部启动旧实例.状态（），「运行中」）；

测试.相等（外部启动新实例.状态（），「已创建」）；

外部启动旧实例.停止（{}）；
