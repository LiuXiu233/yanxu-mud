引「../../src/系统/技能系统.yx」为 技能；

引「../../src/系统/冷却系统.yx」为 冷却；

引「../../src/持久化/内存仓储.yx」为 内存；

引「../../src/实体/实体.yx」为 实体；

引「标准:测试」为 测试；

法 定义解析（编号：文） 则
    归 {「名称」：「失败技能」，「行为编号」：「测试:技能行为/失败」，「冷却毫秒」：10，「需要目标」：真}；
终

法 失败行为（事务，来源：文，目标：文，定义：典，上下文：典）：典 则
    法 修改（工作实体） 则
        工作实体.添加标签（「已命中」）；
        归 空；
    终
    事务.修改实体（目标，修改）；
    抛 「行为失败」；
终

定 仓储 为 内存.新仓储（）；

定 玩家 为 实体.新实体（「测试:角色/玩家」，「测试:原型/角色」）；

玩家.添加组件（技能.技能组件编号，{「已学」：【「测试:技能/失败」】}，1）；

玩家.添加组件（冷却.冷却组件编号，{「到期」：{}}，1）；

仓储.写入实体快照（玩家.快照（），空）；

定 目标 为 实体.新实体（「测试:NPC/目标」，「测试:原型/NPC」）；

仓储.写入实体快照（目标.快照（），空）；

定 注册 为 技能.新行为注册表（）；

注册.注册（「测试:技能行为/失败」，失败行为）；

定 服务 为 技能.技能服务（仓储，定义解析，注册）；

令 已失败：理 为 假；

试 则
    服务.使用（「测试:角色/玩家」，「测试:技能/失败」，「测试:NPC/目标」，{「事务编号」：「tx-skill-failure」，「世界时间」：1，「操作者编号」：「测试:角色/玩家」}）；
救 所误 则
    置 已失败 为 真；
终

测试.相等（已失败，真）；

测试.相等（长度（仓储.读取实体（「测试:角色/玩家」）.取组件（冷却.冷却组件编号）【「到期」】），0）；

测试.相等（包含（仓储.读取实体（「测试:NPC/目标」）.标签（），「已命中」），假）；
