引「../../src/系统/系统注册表.yx」为 系统；

引「标准:测试」为 测试；

引「标准:文字」为 文字；

引「标准:数学」为 数学；

法 应抛生命周期错误（操作：法）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，系统.生命周期错误）；
    终
    测试.断言（命中，「非法系统回调必须抛出稳定生命周期错误码」）；
终

法 正常处理（上下文） 则
    归 上下文【「值」】；
终

法 空回调（上下文）：空 则
    归 空；
终

法 非法处理器（） 则
    系统.系统（「测试:系统/非法处理器」，10，1，空，空）；
终

法 非法启动器（） 则
    系统.系统（「测试:系统/非法启动器」，10，正常处理，1，空）；
终

法 非法停止器（） 则
    系统.系统（「测试:系统/非法停止器」，10，正常处理，空，1）；
终

应抛生命周期错误（非法处理器）；

应抛生命周期错误（非法启动器）；

应抛生命周期错误（非法停止器）；

法 分数优先级（） 则
    系统.系统（「测试:系统/分数优先级」，1.5，正常处理，空，空）；
终

应抛生命周期错误（分数优先级）；

定 非有限数：数 为 数学.幂（10，400）；

法 非有限优先级（） 则
    系统.系统（「测试:系统/非有限优先级」，非有限数，正常处理，空，空）；
终

应抛生命周期错误（非有限优先级）；

定 待装饰 为 系统.系统（「测试:系统/装饰边界」，10，正常处理，空回调，空回调）；

法 非法装饰器参数（） 则
    待装饰.装饰（1）；
终

法 返回非法产物（原处理） 则
    归 1；
终

法 非法装饰器产物（） 则
    待装饰.装饰（返回非法产物）；
终

应抛生命周期错误（非法装饰器参数）；

应抛生命周期错误（非法装饰器产物）；

待装饰.启动（{}）；

测试.相等（待装饰.处理（{「值」：7}），7）；

待装饰.停止（{}）；
