引「../../src/系统/物品系统.yx」为 物品；

引「../../src/持久化/内存仓储.yx」为 内存；

引「../../src/实体/实体.yx」为 实体；

引「标准:文字」为 文字；

引「标准:测试」为 测试；

法 定义解析（编号：文） 则
    归 {「名称」：编号，「描述」：「」，「类型」：「物品」，「可堆叠」：假，「属性」：{}，「价值」：0，「重量」：0}；
终

法 管理（编号：文）：典 则
    归 {「事务编号」：编号，「世界时间」：1，「操作者编号」：「测试:系统/管理员」，「权限」：【物品.物品管理权限】}；
终

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.断言（命中，「物品转移应抛出指定错误码」）；
终

法 新容器（编号：文，容量：数） 则
    定 所实体 为 实体.新实体（编号，「测试:原型/容器」）；
    所实体.添加组件（物品.容器组件编号，{「容量」：容量，「内容」：【】}，1）；
    归 所实体；
终

定 仓储 为 内存.新仓储（）；

定 来源 为 新容器（「测试:容器/来源」，2）；

定 目标 为 新容器（「测试:容器/目标」，1）；

仓储.写入实体快照（来源.快照（），空）；

仓储.写入实体快照（目标.快照（），空）；

定 服务 为 物品.物品服务（仓储，定义解析）；

服务.创建物品（「测试:物品实例/转移项」，「测试:物品/转移项」，「测试:容器/来源」，1，管理（「tx-item-transfer-seed-source」））；

服务.创建物品（「测试:物品实例/占位项」，「测试:物品/占位项」，「测试:容器/目标」，1，管理（「tx-item-transfer-seed-target」））；

法 转移到满容器（） 则
    服务.转移（「测试:容器/来源」，「测试:容器/目标」，「测试:物品实例/转移项」，{「事务编号」：「tx-item-transfer-capacity」，「世界时间」：3，「操作者编号」：「测试:容器/来源」}）；
终

应抛（转移到满容器，物品.容量不足错误）；

测试.相等（仓储.读取实体（「测试:容器/来源」）.取组件（物品.容器组件编号）【「内容」】【0】，「测试:物品实例/转移项」）；

测试.相等（服务.读取物品（「测试:物品实例/转移项」）【「所在」】，「测试:容器/来源」）；

法 错配来源（） 则
    服务.转移（「测试:容器/错误来源」，「测试:容器/目标」，「测试:物品实例/转移项」，管理（「tx-item-transfer-mismatch」））；
终

应抛（错配来源，物品.位置冲突错误）；

测试.相等（服务.读取物品（「测试:物品实例/转移项」）【「所在」】，「测试:容器/来源」）；
