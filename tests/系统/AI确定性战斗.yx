引「../../src/系统/AI系统.yx」为 AI；

引「../../src/系统/时间系统.yx」为 时间；

引「../../src/测试支持/虚拟时钟.yx」为 测试时间；

引「../../src/持久化/内存仓储.yx」为 内存；

引「../../src/实体/实体.yx」为 实体；

引「标准:测试」为 测试；

法 新战斗实体（编号：文，阵营：文，生命：数，攻击：数，防御：数） 则
    定 所实体 为 实体.新实体（编号，「测试:原型/战斗实体」）；
    所实体.添加组件（AI.位置组件编号，{「所在」：「测试:房间/一」，「坐标」：【】}，1）；
    所实体.添加组件（AI.战斗组件编号，{「生命」：生命，「最大生命」：生命，「攻击」：攻击，「防御」：防御，「阵营」：阵营，「战斗中」：假}，1）；
    归 所实体；
终

法 管理上下文（编号：文）：典 则
    归 {「事务编号」：编号，「操作者编号」：「测试:系统/AI」，「权限」：【AI.AI管理权限，时间.时间管理权限】}；
终

法 执行上下文（编号：文）：典 则
    归 {「事务编号」：编号，「操作者编号」：「测试:系统/时间」，「权限」：【时间.时间管理权限】}；
终

定 仓储 为 内存.新仓储（）；

仓储.写入实体快照（新战斗实体（「测试:NPC实例/野狼」，「野兽」，30，10，2）.快照（），空）；

仓储.写入实体快照（新战斗实体（「测试:角色/b」，「玩家」，20，3，1）.快照（），空）；

仓储.写入实体快照（新战斗实体（「测试:角色/a」，「玩家」，20，3，1）.快照（），空）；

定 时钟 为 测试时间.虚拟时钟（0）；

定 任务注册 为 时间.新任务行为注册表（）；

定 时间服务 为 时间.时间服务（仓储，时钟，任务注册）；

定 行为注册 为 AI.新AI行为注册表（）；

AI.注册内置AI行为（行为注册）；

定 服务 为 AI.AI服务（仓储，时间服务，行为注册）；

AI.注册AI计划行为（任务注册，服务）；

定 已启用：典 为 服务.启用（「测试:NPC实例/野狼」，{「行为」：AI.敌对近战行为编号，「间隔毫秒」：100，「参数」：{「目标阵营」：【「玩家」】，「伤害参数」：{「倍率」：100}}，「状态」：{}}，管理上下文（「tx-ai-enable」））；

测试.相等（已启用【「任务」】【「到期」】，100）；

测试.相等（长度（时间服务.各任务（）），1）；

时钟.推进（100）；

定 候选：典 为 时间服务.调度器（）.到期（100，1）【0】；

定 已执行：典 为 时间服务.执行（候选，执行上下文（「tx-ai-execute」））；

测试.相等（已执行【「数据」】【「实体编号」】，「测试:NPC实例/野狼」）；

测试.相等（已执行【「数据」】【「结果」】【「目标编号」】，「测试:角色/a」）；

测试.相等（已执行【「数据」】【「结果」】【「剩余生命」】，11）；

测试.相等（已执行【「数据」】【「状态」】【「轮次」】，1）；

测试.相等（已执行【「数据」】【「状态」】【「上次目标」】，「测试:角色/a」）；

测试.相等（已执行【「下次任务」】【「到期」】，200）；

测试.相等（已执行【「事件」】【0】【「事件种类」】，「言域:事件/战斗伤害」）；

测试.相等（已执行【「事件」】【1】【「事件种类」】，「言域:事件/AI行动」）；

测试.相等（已执行【「事件」】【2】【「事件种类」】，「言域:事件/计划任务执行」）；
