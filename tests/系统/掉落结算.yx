引「../../src/系统/掉落系统.yx」为 掉落；

引「../../src/系统/物品系统.yx」为 物品；

引「../../src/持久化/内存仓储.yx」为 内存；

引「../../src/实体/实体.yx」为 实体；

引「../../src/世界/世界事务.yx」为 世界事务；

引「../../src/消息/消息协议.yx」为 消息；

引「标准:文字」为 文字；

引「标准:测试」为 测试；

法 物品解析（编号：文） 则
    若 （编号 等于 「测试:物品/铜钱」） 则
        归 {「名称」：「铜钱」，「描述」：「」，「类型」：「货币」，「可堆叠」：真，「属性」：{}，「价值」：1，「重量」：0}；
    终
    归 {「名称」：「旧剑」，「描述」：「」，「类型」：「武器」，「可堆叠」：假，「属性」：{}，「价值」：1，「重量」：1}；
终

法 掉落解析（编号：文） 则
    归 {「名称」：「测试掉落」，「条目」：【{「物品」：「测试:物品/铜钱」，「概率」：1，「最少」：3，「最多」：3}，{「物品」：「测试:物品/旧剑」，「概率」：1，「最少」：2，「最多」：2}】}；
终

法 随机源（请求：典）：数 则
    归 0；
终

定 编号列：列<文> 为 【「测试:物品实例/铜钱」，「测试:物品实例/旧剑一」，「测试:物品实例/旧剑二」】；

令 编号序号：数 为 0；

法 编号生成器（上下文：典）：文 则
    定 编号：文 为 编号列【编号序号】；
    置 编号序号 为 （编号序号 加 1）；
    归 编号；
终

定 仓储 为 内存.新仓储（）；

定 野狼 为 实体.新实体（「测试:NPC/野狼」，「测试:原型/野狼」）；

野狼.添加组件（掉落.位置组件编号，{「所在」：「测试:房间/北郊」，「坐标」：【】}，1）；

野狼.添加组件（掉落.掉落组件编号，{「表」：「测试:掉落表/野狼」，「次数」：1，「已结算」：假，「结算时间」：0，「结算事务」：「」}，1）；

仓储.写入实体快照（野狼.快照（），空）；

定 物品服务 为 物品.物品服务（仓储，物品解析）；

定 服务 为 掉落.掉落服务（仓储，掉落解析，物品服务，随机源，编号生成器）；

定 回滚事务 为 世界事务.开始（仓储，「tx-drop-settle-rollback」）；

定 回滚结果：典 为 服务.结算到事务（回滚事务，「测试:NPC/野狼」，{「事务编号」：「tx-drop-settle-rollback」，「世界时间」：49，「现实时间」：59，「操作者编号」：「测试:系统/掉落」，「权限」：【掉落.掉落管理权限】}）；

测试.相等（长度（回滚结果【「创建」】），3）；

测试.相等（仓储.读取实体（「测试:物品实例/铜钱」），空）；

回滚事务.回滚（）；

测试.相等（服务.查看（「测试:NPC/野狼」）【「掉落」】【「已结算」】，假）；

测试.相等（仓储.读取实体（「测试:物品实例/铜钱」），空）；

置 编号序号 为 0；

定 结果：典 为 服务.结算（「测试:NPC/野狼」，{「事务编号」：「tx-drop-settle」，「世界时间」：50，「现实时间」：60，「操作者编号」：「测试:系统/掉落」，「权限」：【掉落.掉落管理权限】}）；

测试.相等（长度（结果【「掉落」】），2）；

测试.相等（长度（结果【「创建」】），3）；

测试.相等（物品服务.读取物品（「测试:物品实例/铜钱」）【「数量」】，3）；

测试.相等（物品服务.读取物品（「测试:物品实例/旧剑一」）【「数量」】，1）；

测试.相等（物品服务.读取物品（「测试:物品实例/旧剑二」）【「所在」】，「测试:房间/北郊」）；

测试.相等（结果【「来源」】【「掉落」】【「已结算」】，真）；

测试.相等（结果【「来源」】【「掉落」】【「结算时间」】，50）；

测试.相等（结果【「事件」】【0】【「事件种类」】，「言域:事件/掉落结算」）；

测试.相等（消息.验证消息（结果【「消息」】【0】），真）；

定 已结算修订：数 为 仓储.读取实体（「测试:NPC/野狼」）.修订（）；

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.相等（命中，真）；
终

法 重复结算（） 则
    服务.结算（「测试:NPC/野狼」，{「事务编号」：「tx-drop-repeat」，「世界时间」：51，「操作者编号」：「测试:系统/掉落」，「权限」：【掉落.掉落管理权限】}）；
终

应抛（重复结算，掉落.已结算错误）；

测试.相等（仓储.读取实体（「测试:NPC/野狼」）.修订（），已结算修订）；
