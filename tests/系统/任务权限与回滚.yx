引「../../src/系统/任务系统.yx」为 任务；

引「../../src/持久化/内存仓储.yx」为 内存；

引「../../src/实体/实体.yx」为 实体；

引「标准:文字」为 文字；

引「标准:测试」为 测试；

法 定义解析（编号：文） 则
    归 {「名称」：「采集」，「步骤」：【{「类型」：「收集」，「目标」：「测试:物品/药草」，「数量」：2}】}；
终

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.相等（命中，真）；
终

定 仓储 为 内存.新仓储（）；

定 玩家 为 实体.新实体（「测试:角色/玩家」，「测试:原型/角色」）；

玩家.添加组件（任务.任务组件编号，{「进度」：{}}，1）；

仓储.写入实体快照（玩家.快照（），空）；

定 服务 为 任务.任务服务（仓储，定义解析）；

服务.接受（「测试:角色/玩家」，「测试:任务/采集」，{「事务编号」：「tx-quest-boundary-accept」，「世界时间」：1，「操作者编号」：「测试:角色/玩家」}）；

定 原修订：数 为 仓储.读取实体（「测试:角色/玩家」）.修订（）；

法 越权推进（） 则
    服务.推进（「测试:角色/玩家」，「测试:任务/采集」，0，1，{「事务编号」：「tx-quest-denied」，「世界时间」：2，「操作者编号」：「测试:角色/玩家」}）；
终

应抛（越权推进，任务.权限不足错误）；

法 重复接受（） 则
    服务.接受（「测试:角色/玩家」，「测试:任务/采集」，{「事务编号」：「tx-quest-duplicate」，「世界时间」：2，「操作者编号」：「测试:角色/玩家」}）；
终

应抛（重复接受，任务.任务重复错误）；

法 步骤越界（） 则
    服务.推进（「测试:角色/玩家」，「测试:任务/采集」，1，1，{「事务编号」：「tx-quest-step」，「世界时间」：2，「操作者编号」：「测试:系统/任务」，「权限」：【任务.任务管理权限】}）；
终

应抛（步骤越界，任务.步骤无效错误）；

测试.相等（仓储.读取实体（「测试:角色/玩家」）.修订（），原修订）；

测试.相等（仓储.读取实体（「测试:角色/玩家」）.取组件（任务.任务组件编号）【「进度」】【「测试:任务/采集」】【「步骤」】【0】，0）；

定 放弃：典 为 服务.放弃（「测试:角色/玩家」，「测试:任务/采集」，{「事务编号」：「tx-quest-abandon」，「世界时间」：3，「操作者编号」：「测试:角色/玩家」}）；

测试.相等（长度（放弃【「任务」】【「任务」】），0）；

测试.相等（放弃【「事件」】【0】【「载荷」】【「动作」】，「放弃」）；
