引「../../src/系统/空间系统.yx」为 空间；

引「标准:文字」为 文字；

引「标准:测试」为 测试；

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.断言（命中，「应抛出指定房间错误」）；
终

测试.相等（空间.规范方向（「NORTH」），「北」）；

测试.相等（空间.规范方向（「西北」），「西北」）；

定 房间：典 为 空间.规范房间（「测试:房间/甲」，{「数据」：{「名称」：「甲」，「描述」：「测试」，「出口」：{「东」：「测试:房间/乙」}}}）；

测试.相等（房间【「出口」】【「东」】，「测试:房间/乙」）；

法 错误出口（） 则
    空间.规范房间（「测试:房间/甲」，{「名称」：「甲」，「描述」：「」，「出口」：{「东」：「无效编号」}}）；
终

应抛（错误出口，空间.房间无效错误）；
