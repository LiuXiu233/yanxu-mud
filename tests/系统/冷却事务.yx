引「../../src/系统/冷却系统.yx」为 冷却；

引「../../src/持久化/内存仓储.yx」为 内存；

引「../../src/实体/实体.yx」为 实体；

引「标准:文字」为 文字；

引「标准:测试」为 测试；

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.断言（命中，「冷却事务应抛出指定错误码」）；
终

法 上下文（编号：文，时间：数，权限：列<文>）：典 则
    归 {「事务编号」：编号，「世界时间」：时间，「操作者编号」：「测试:系统/冷却」，「权限」：权限}；
终

定 仓储 为 内存.新仓储（）；

定 玩家 为 实体.新实体（「测试:角色/玩家」，「测试:原型/角色」）；

玩家.添加组件（冷却.冷却组件编号，{「到期」：{}}，1）；

仓储.写入实体快照（玩家.快照（），空）；

定 服务 为 冷却.冷却服务（仓储）；

法 越权设置（） 则
    服务.设置（「测试:角色/玩家」，「测试:技能/重击」，20，上下文（「tx-cooldown-denied」，1，【】））；
终

应抛（越权设置，冷却.权限不足错误）；

定 设置：典 为 服务.设置（「测试:角色/玩家」，「测试:技能/重击」，20，上下文（「tx-cooldown-set」，1，【冷却.冷却管理权限】））；

测试.相等（设置【「冷却」】【「冷却」】【0】【「剩余毫秒」】，19）；

测试.相等（设置【「事件」】【0】【「事件种类」】，「言域:事件/冷却变更」）；

定 清理：典 为 服务.清理到期（「测试:角色/玩家」，上下文（「tx-cooldown-cleanup」，20，【冷却.冷却管理权限】））；

测试.相等（长度（清理【「冷却」】【「冷却」】），0）；
