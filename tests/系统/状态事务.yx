引「../../src/系统/状态系统.yx」为 状态；

引「../../src/系统/属性系统.yx」为 属性；

引「../../src/持久化/内存仓储.yx」为 内存；

引「../../src/实体/实体.yx」为 实体；

引「../../src/消息/消息协议.yx」为 消息；

引「标准:文字」为 文字；

引「标准:测试」为 测试；

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.断言（命中，「状态事务应抛出指定错误码」）；
终

法 定义解析（编号：文） 则
    归 {「名称」：「怒意」，「描述」：「攻击提高」，「属性」：{「攻击」：2}，「持续毫秒」：10，「最大层数」：3，「叠加策略」：「叠加」}；
终

法 上下文（编号：文，时间：数，权限：列<文>）：典 则
    归 {「事务编号」：编号，「世界时间」：时间，「现实时间」：时间，「操作者编号」：「测试:系统/状态」，「权限」：权限}；
终

定 仓储 为 内存.新仓储（）；

定 玩家 为 实体.新实体（「测试:角色/玩家」，「测试:原型/角色」）；

玩家.添加组件（状态.状态组件编号，{「状态」：【】}，1）；

玩家.添加组件（属性.属性组件编号，{「基础」：{「攻击」：5，「最大生命」：100}，「当前」：{「攻击」：5，「最大生命」：100}，「来源」：【】}，1）；

玩家.添加组件（属性.战斗组件编号，{「生命」：100，「最大生命」：100，「攻击」：5，「防御」：0，「阵营」：「玩家」，「战斗中」：假}，1）；

仓储.写入实体快照（玩家.快照（），空）；

定 初始修订：数 为 仓储.读取实体（「测试:角色/玩家」）.修订（）；

定 聚合器 为 属性.新修正源聚合器（）；

状态.注册状态属性提供器（聚合器，定义解析）；

定 服务 为 状态.状态服务（仓储，定义解析，聚合器）；

法 越权施加（） 则
    服务.施加（「测试:角色/玩家」，「测试:状态实例/怒意」，「测试:状态/怒意」，「测试:角色/玩家」，1，上下文（「tx-status-denied」，1，【】））；
终

应抛（越权施加，状态.权限不足错误）；

测试.相等（仓储.读取实体（「测试:角色/玩家」）.修订（），初始修订）；

定 施加：典 为 服务.施加（「测试:角色/玩家」，「测试:状态实例/怒意」，「测试:状态/怒意」，「测试:角色/玩家」，2，上下文（「tx-status-apply」，10，【状态.状态管理权限】））；

测试.相等（施加【「状态」】【「状态」】【0】【「层数」】，2）；

测试.相等（仓储.读取实体（「测试:角色/玩家」）.取组件（属性.属性组件编号）【「当前」】【「攻击」】，9）；

测试.相等（施加【「事件」】【0】【「事件种类」】，「言域:事件/状态变更」）；

测试.相等（消息.验证消息（施加【「消息」】【0】），真）；

定 未到期：典 为 服务.清理到期（「测试:角色/玩家」，上下文（「tx-status-not-expired」，19，【状态.状态管理权限】））；

测试.相等（长度（未到期【「事件」】），0）；

定 到期：典 为 服务.清理到期（「测试:角色/玩家」，上下文（「tx-status-expired」，20，【状态.状态管理权限】））；

测试.相等（长度（到期【「状态」】【「状态」】），0）；

测试.相等（仓储.读取实体（「测试:角色/玩家」）.取组件（属性.属性组件编号）【「当前」】【「攻击」】，5）；
