引「../../src/系统/空间系统.yx」为 空间；

引「../../src/持久化/内存仓储.yx」为 内存；

引「../../src/实体/实体.yx」为 实体；

引「标准:文字」为 文字；

引「标准:测试」为 测试；

法 房间解析（编号：文） 则
    若 （编号 等于 「测试:房间/甲」） 则
        归 {「名称」：「甲」，「描述」：「」，「出口」：{「东」：「测试:房间/乙」}}；
    终
    归 {「名称」：「乙」，「描述」：「」，「出口」：{「西」：「测试:房间/甲」}}；
终

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.断言（命中，「应抛出空间权限错误」）；
终

定 仓储 为 内存.新仓储（）；

定 目标 为 实体.新实体（「测试:NPC/目标」，「测试:原型/目标」）；

目标.添加组件（空间.位置组件编号，{「所在」：「测试:房间/甲」，「坐标」：【】}，1）；

仓储.写入实体快照（目标.快照（），空）；

定 服务 为 空间.空间服务（仓储，房间解析）；

法 越权移动（） 则
    服务.移动（「测试:NPC/目标」，「东」，{「事务编号」：「tx-空间-越权」，「世界时间」：1，「操作者编号」：「测试:账户/管理员」}）；
终

应抛（越权移动，空间.操作权限错误）；

定 管理移动：典 为 服务.移动（「测试:NPC/目标」，「东」，{「事务编号」：「tx-空间-管理移动」，「世界时间」：2，「操作者编号」：「测试:账户/管理员」，「权限」：【空间.移动管理权限】}）；

测试.相等（管理移动【「成功」】，真）；

测试.相等（管理移动【「事件」】【0】【「载荷」】【「操作者编号」】，「测试:账户/管理员」）；
