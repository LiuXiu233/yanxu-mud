引「../../src/系统/物品系统.yx」为 物品；

引「标准:文字」为 文字；

引「标准:测试」为 测试；

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.断言（命中，「应抛出指定物品定义错误码」）；
终

定 规范：典 为 物品.规范物品定义（「测试:物品/药草」，{「数据」：{「名称」：「药草」，「描述」：「新鲜药草。」，「类型」：「材料」，「可堆叠」：真，「属性」：{「药性」：2}，「价值」：4，「重量」：1}}）；

测试.相等（规范【「编号」】，「测试:物品/药草」）；

测试.相等（规范【「可堆叠」】，真）；

测试.相等（规范【「属性」】【「药性」】，2）；

法 无效定义（） 则
    物品.规范物品定义（「测试:物品/错误」，{「名称」：「」，「价值」：减 1}）；
终

应抛（无效定义，物品.定义无效错误）；

法 错误类别解析（编号：文） 则
    归 {「编号」：编号，「类别」：「房间」，「数据」：{「名称」：「伪装物品」}}；
终

法 错误类别（） 则
    物品.读取物品定义（错误类别解析，「测试:物品/伪装」）；
终

应抛（错误类别，物品.定义无效错误）；
