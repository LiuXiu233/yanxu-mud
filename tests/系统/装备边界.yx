引「../../src/系统/装备系统.yx」为 装备；

引「../../src/系统/物品系统.yx」为 物品；

引「../../src/系统/属性系统.yx」为 属性；

引「../../src/持久化/内存仓储.yx」为 内存；

引「../../src/实体/实体.yx」为 实体；

引「标准:文字」为 文字；

引「标准:测试」为 测试；

法 定义解析（编号：文） 则
    若 （编号 等于 「测试:物品/剑」） 则
        归 {「名称」：「剑」，「描述」：「」，「类型」：「武器」，「可堆叠」：假，「装备槽」：【「主手」】，「属性」：{「攻击」：3}，「价值」：1，「重量」：1}；
    终
    归 {「名称」：「石头」，「描述」：「」，「类型」：「材料」，「可堆叠」：假，「装备槽」：【】，「属性」：{}，「价值」：0，「重量」：1}；
终

法 新物品（编号：文，定义编号：文） 则
    定 所实体 为 实体.新实体（编号，定义编号）；
    所实体.添加组件（物品.物品组件编号，{「定义编号」：定义编号，「数量」：1，「绑定」：假}，1）；
    所实体.添加组件（物品.位置组件编号，{「所在」：「测试:角色/玩家」，「坐标」：【】}，1）；
    归 所实体；
终

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.断言（命中，「装备边界应抛出指定错误码」）；
终

定 仓储 为 内存.新仓储（）；

仓储.写入实体快照（新物品（「测试:物品实例/剑」，「测试:物品/剑」）.快照（），空）；

仓储.写入实体快照（新物品（「测试:物品实例/石头」，「测试:物品/石头」）.快照（），空）；

定 玩家 为 实体.新实体（「测试:角色/玩家」，「测试:原型/角色」）；

玩家.添加组件（装备.背包组件编号，{「容量」：1，「物品」：【「测试:物品实例/石头」】}，1）；

玩家.添加组件（装备.装备组件编号，{「槽位」：{「主手」：「测试:物品实例/剑」}}，1）；

玩家.添加组件（属性.属性组件编号，{「基础」：{「攻击」：5}，「当前」：{「攻击」：8}，「来源」：【「测试:物品实例/剑」】}，1）；

仓储.写入实体快照（玩家.快照（），空）；

定 服务 为 装备.装备服务（仓储，物品.物品服务（仓储，定义解析））；

法 满包卸下（） 则
    服务.卸下（「测试:角色/玩家」，「主手」，{「事务编号」：「tx-equipment-full」，「世界时间」：1，「操作者编号」：「测试:角色/玩家」}）；
终

应抛（满包卸下，装备.背包已满错误）；

测试.相等（仓储.读取实体（「测试:角色/玩家」）.取组件（装备.装备组件编号）【「槽位」】【「主手」】，「测试:物品实例/剑」）；

测试.相等（仓储.读取实体（「测试:角色/玩家」）.取组件（属性.属性组件编号）【「当前」】【「攻击」】，8）；

法 越权卸下（） 则
    服务.卸下（「测试:角色/玩家」，「主手」，{「事务编号」：「tx-equipment-denied」，「世界时间」：2，「操作者编号」：「测试:角色/旁观者」}）；
终

应抛（越权卸下，装备.权限不足错误）；

法 不兼容穿戴（） 则
    定 空仓储 为 内存.新仓储（）；
    定 石头 为 新物品（「测试:物品实例/另一个石头」，「测试:物品/石头」）；
    空仓储.写入实体快照（石头.快照（），空）；
    定 空角色 为 实体.新实体（「测试:角色/玩家」，「测试:原型/角色」）；
    空角色.添加组件（装备.背包组件编号，{「容量」：1，「物品」：【「测试:物品实例/另一个石头」】}，1）；
    空角色.添加组件（装备.装备组件编号，{「槽位」：{}}，1）；
    空角色.添加组件（属性.属性组件编号，{「基础」：{}，「当前」：{}，「来源」：【】}，1）；
    空仓储.写入实体快照（空角色.快照（），空）；
    定 空服务 为 装备.装备服务（空仓储，物品.物品服务（空仓储，定义解析））；
    空服务.穿戴（「测试:角色/玩家」，「测试:物品实例/另一个石头」，「主手」，{「事务编号」：「tx-equipment-incompatible」，「世界时间」：3，「操作者编号」：「测试:角色/玩家」}）；
终

应抛（不兼容穿戴，装备.槽位不兼容错误）；
