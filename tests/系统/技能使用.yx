引「../../src/系统/技能系统.yx」为 技能；

引「../../src/系统/冷却系统.yx」为 冷却；

引「../../src/持久化/内存仓储.yx」为 内存；

引「../../src/实体/实体.yx」为 实体；

引「../../src/消息/消息协议.yx」为 消息；

引「标准:文字」为 文字；

引「标准:测试」为 测试；

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.断言（命中，「技能使用应抛出指定错误码」）；
终

法 定义解析（编号：文） 则
    归 {「名称」：「重击」，「行为编号」：「测试:技能行为/伤害」，「冷却毫秒」：10，「需要目标」：真}；
终

法 行为（事务，来源：文，目标：文，定义：典，上下文：典）：典 则
    归 {「数据」：{「命中」：真}，「消息」：【消息.新消息（【消息.战斗（来源，目标，「重击」，3，【消息.文字（「造成伤害」）】）】，{}）】}；
终

定 仓储 为 内存.新仓储（）；

定 玩家 为 实体.新实体（「测试:角色/玩家」，「测试:原型/角色」）；

玩家.添加组件（技能.技能组件编号，{「已学」：【「测试:技能/重击」】}，1）；

玩家.添加组件（冷却.冷却组件编号，{「到期」：{}}，1）；

仓储.写入实体快照（玩家.快照（），空）；

定 注册 为 技能.新行为注册表（）；

注册.注册（「测试:技能行为/伤害」，行为）；

定 服务 为 技能.技能服务（仓储，定义解析，注册）；

定 使用：典 为 服务.使用（「测试:角色/玩家」，「测试:技能/重击」，「测试:NPC/野狼」，{「事务编号」：「tx-skill-use」，「世界时间」：100，「操作者编号」：「测试:角色/玩家」}）；

测试.相等（使用【「数据」】【「命中」】，真）；

测试.相等（使用【「冷却」】【「到期时间」】，110）；

测试.相等（消息.验证消息（使用【「消息」】【0】），真）；

法 冷却中使用（） 则
    服务.使用（「测试:角色/玩家」，「测试:技能/重击」，「测试:NPC/野狼」，{「事务编号」：「tx-skill-use-cooldown」，「世界时间」：101，「操作者编号」：「测试:角色/玩家」}）；
终

应抛（冷却中使用，冷却.尚在冷却错误）；
