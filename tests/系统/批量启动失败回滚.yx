引「../../src/系统/系统注册表.yx」为 系统；

引「标准:测试」为 测试；

引「标准:文字」为 文字；

令 记录：列<文> 为 【】；

法 空处理（上下文）：空 则
    归 空；
终

法 启动甲（上下文）：空 则
    追加（记录，「启动甲」）；
    归 空；
终

法 停止甲（上下文）：空 则
    追加（记录，「停止甲」）；
    归 空；
终

法 启动乙失败（上下文）：空 则
    追加（记录，「启动乙」）；
    抛 「内部启动秘密」；
终

法 清理乙（上下文）：空 则
    追加（记录，「清理乙」）；
    归 空；
终

法 启动丙（上下文）：空 则
    追加（记录，「启动丙」）；
    归 空；
终

定 注册 为 系统.注册表（）；

定 甲 为 系统.系统（「测试:系统/甲」，10，空处理，启动甲，停止甲）；

定 乙 为 系统.系统（「测试:系统/乙」，20，空处理，启动乙失败，清理乙）；

定 丙 为 系统.系统（「测试:系统/丙」，30，空处理，启动丙，空）；

注册.注册（甲）.注册（乙）.注册（丙）；

令 已拒绝：理 为 假；

令 已泄露内部消息：理 为 假；

试 则
    注册.启动全部（{}）；
救 所误 则
    置 已拒绝 为 文字.始于（所误.消息，系统.生命周期错误）；
    置 已泄露内部消息 为 （长度（文字.分割（所误.消息，「内部启动秘密」）） 大于 1）；
终

测试.相等（已拒绝，真）；

测试.相等（已泄露内部消息，假）；

测试.相等（长度（记录），4）；

测试.相等（记录【0】，「启动甲」）；

测试.相等（记录【1】，「启动乙」）；

测试.相等（记录【2】，「清理乙」）；

测试.相等（记录【3】，「停止甲」）；

测试.相等（甲.状态（），「已停止」）；

测试.相等（乙.状态（），「已停止」）；

测试.相等（丙.状态（），「已创建」）；

令 已拒绝处理：理 为 假；

试 则
    注册.处理全部（{}）；
救 所误 则
    置 已拒绝处理 为 文字.始于（所误.消息，系统.生命周期错误）；
终

测试.相等（已拒绝处理，真）；
