引「../../src/系统/战斗系统.yx」为 战斗；

引「../../src/持久化/内存仓储.yx」为 内存；

引「../../src/实体/实体.yx」为 实体；

引「标准:文字」为 文字；

引「标准:测试」为 测试；

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.断言（命中，「战斗边界应抛出指定错误码」）；
终

法 新战斗实体（编号：文，房间：文） 则
    定 所实体 为 实体.新实体（编号，「测试:原型/战斗者」）；
    所实体.添加组件（战斗.位置组件编号，{「所在」：房间，「坐标」：【】}，1）；
    所实体.添加组件（战斗.战斗组件编号，{「生命」：10，「最大生命」：10，「攻击」：5，「防御」：1，「阵营」：「测试」，「战斗中」：假}，1）；
    归 所实体；
终

定 仓储 为 内存.新仓储（）；

仓储.写入实体快照（新战斗实体（「测试:角色/玩家」，「测试:房间/甲」）.快照（），空）；

仓储.写入实体快照（新战斗实体（「测试:NPC/目标」，「测试:房间/乙」）.快照（），空）；

定 服务 为 战斗.战斗服务（仓储）；

法 越权攻击（） 则
    服务.攻击（「测试:角色/玩家」，「测试:NPC/目标」，「攻击」，{}，{「事务编号」：「tx-combat-denied」，「世界时间」：1，「操作者编号」：「测试:角色/旁观者」}）；
终

应抛（越权攻击，战斗.权限不足错误）；

法 跨房攻击（） 则
    服务.攻击（「测试:角色/玩家」，「测试:NPC/目标」，「攻击」，{}，{「事务编号」：「tx-combat-range」，「世界时间」：2，「操作者编号」：「测试:角色/玩家」}）；
终

应抛（跨房攻击，战斗.距离无效错误）；

测试.相等（仓储.读取实体（「测试:NPC/目标」）.取组件（战斗.战斗组件编号）【「生命」】，10）；

法 攻击自己（） 则
    服务.攻击（「测试:角色/玩家」，「测试:角色/玩家」，「攻击」，{}，{「事务编号」：「tx-combat-self」，「世界时间」：3，「操作者编号」：「测试:角色/玩家」}）；
终

应抛（攻击自己，战斗.目标无效错误）；
