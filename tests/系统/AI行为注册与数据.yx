引「../../src/系统/AI系统.yx」为 AI；

引「标准:文字」为 文字；

引「标准:测试」为 测试；

法 行为甲（事务，实体编号：文，AI数据：典，上下文：典）：典 则
    归 {「数据」：{「版本」：1}，「状态」：{「来源」：「甲」}}；
终

法 行为乙（事务，实体编号：文，AI数据：典，上下文：典）：典 则
    归 {「数据」：{「版本」：2}，「状态」：{「来源」：「乙」}}；
终

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.相等（命中，真）；
终

定 数据：典 为 AI.规范AI数据（「测试:NPC实例/一」，{「行为」：「测试:AI行为/甲」，「间隔毫秒」：100，「参数」：{「范围」：1}，「状态」：{}}）；

测试.相等（数据【「任务编号」】，AI.AI任务编号（「测试:NPC实例/一」））；

测试.相等（数据【「间隔毫秒」】，100）；

定 近战参数：典 为 AI.规范敌对近战参数（{「目标阵营」：【「玩家」，「守卫」】，「伤害参数」：{「倍率」：120}}）；

测试.相等（近战参数【「目标阵营」】【0】，「守卫」）；

测试.相等（近战参数【「伤害参数」】【「倍率」】，120）；

定 注册 为 AI.新AI行为注册表（）；

注册.注册（「测试:AI行为/甲」，行为甲）；

测试.相等（注册.执行（「测试:AI行为/甲」，空，「测试:NPC实例/一」，数据，{}）【「数据」】【「版本」】，1）；

注册.替换（「测试:AI行为/甲」，行为乙）；

测试.相等（注册.执行（「测试:AI行为/甲」，空，「测试:NPC实例/一」，数据，{}）【「数据」】【「版本」】，2）；

法 重复注册（） 则
    注册.注册（「测试:AI行为/甲」，行为甲）；
终

应抛（重复注册，AI.行为重复错误）；

注册.移除（「测试:AI行为/甲」）；

法 执行未知（） 则
    注册.执行（「测试:AI行为/甲」，空，「测试:NPC实例/一」，数据，{}）；
终

应抛（执行未知，AI.行为不存在错误）；

法 无效间隔（） 则
    AI.规范AI数据（「测试:NPC实例/一」，{「行为」：「测试:AI行为/甲」，「间隔毫秒」：0，「参数」：{}，「状态」：{}}）；
终

应抛（无效间隔，AI.数据无效错误）；

法 重复阵营（） 则
    AI.规范敌对近战参数（{「目标阵营」：【「玩家」，「玩家」】}）；
终

应抛（重复阵营，AI.参数无效错误）；
