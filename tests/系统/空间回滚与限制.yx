引「../../src/系统/空间系统.yx」为 空间；

引「../../src/持久化/内存仓储.yx」为 内存；

引「../../src/实体/实体.yx」为 实体；

引「标准:文字」为 文字；

引「标准:测试」为 测试；

法 房间解析（编号：文） 则
    若 （编号 等于 「测试:房间/南门」） 则
        归 {「名称」：「南门」，「描述」：「」，「出口」：{「北」：「测试:房间/中央街」}}；
    终
    归 {「名称」：「中央街」，「描述」：「」，「出口」：{「南」：「测试:房间/南门」}}；
终

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.断言（命中，「应抛出指定空间错误码」）；
终

定 仓储 为 内存.新仓储（）；

定 玩家 为 实体.新实体（「测试:角色/玩家」，「言域:原型/玩家角色」）；

玩家.添加组件（空间.位置组件编号，{「所在」：「测试:房间/南门」，「坐标」：【】}，1）；

玩家.添加组件（空间.可移动组件编号，{「可移动」：真，「重量」：0}，1）；

仓储.写入实体快照（玩家.快照（），空）；

定 服务 为 空间.空间服务（仓储，房间解析）；

法 无出口（） 则
    服务.移动（「测试:角色/玩家」，「西」，{「事务编号」：「tx-空间-无出口」，「世界时间」：1，「操作者编号」：「测试:角色/玩家」}）；
终

应抛（无出口，空间.出口不存在错误）；

测试.相等（仓储.读取实体（「测试:角色/玩家」）.取组件（空间.位置组件编号）【「所在」】，「测试:房间/南门」）；

定 固定物 为 实体.新实体（「测试:实体/固定物」，「测试:原型/固定物」）；

固定物.添加组件（空间.位置组件编号，{「所在」：「测试:房间/南门」，「坐标」：【】}，1）；

固定物.添加组件（空间.可移动组件编号，{「可移动」：假，「重量」：100}，1）；

仓储.写入实体快照（固定物.快照（），空）；

法 固定物移动（） 则
    服务.移动（「测试:实体/固定物」，「北」，{「事务编号」：「tx-空间-固定物」，「世界时间」：2，「操作者编号」：「测试:实体/固定物」}）；
终

应抛（固定物移动，空间.移动受限错误）；
