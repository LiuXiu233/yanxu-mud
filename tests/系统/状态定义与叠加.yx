引「../../src/系统/状态系统.yx」为 状态；

引「标准:文字」为 文字；

引「标准:测试」为 测试；

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.断言（命中，「状态定义与叠加应抛出指定错误码」）；
终

法 定义解析（编号：文） 则
    若 （编号 等于 「测试:状态/怒意」） 则
        归 {「名称」：「怒意」，「描述」：「攻击提高」，「属性」：{「攻击」：2}，「持续毫秒」：10，「最大层数」：3，「叠加策略」：「叠加」}；
    终
    若 （编号 等于 「测试:状态/守势」） 则
        归 {「名称」：「守势」，「属性」：{「防御」：1}，「持续毫秒」：20，「最大层数」：2，「叠加策略」：「刷新」}；
    终
    归 {「名称」：「唯一」，「属性」：{}，「持续毫秒」：0，「最大层数」：1，「叠加策略」：「拒绝」}；
终

定 首次：典 为 状态.计算施加状态（【】，定义解析，「测试:状态实例/怒意」，「测试:状态/怒意」，「测试:角色/玩家」，2，100）；

测试.相等（首次【「项目」】【「层数」】，2）；

测试.相等（首次【「项目」】【「到期时间」】，110）；

定 叠加：典 为 状态.计算施加状态（首次【「状态」】，定义解析，「测试:状态实例/怒意」，「测试:状态/怒意」，「测试:角色/玩家」，2，105）；

测试.相等（叠加【「项目」】【「层数」】，3）；

测试.相等（叠加【「项目」】【「到期时间」】，115）；

测试.相等（长度（状态.计算清理到期（叠加【「状态」】，114）【「状态」】），1）；

测试.相等（状态.计算清理到期（叠加【「状态」】，115）【「到期」】【0】，「测试:状态实例/怒意」）；

定 过期重施：典 为 状态.计算施加状态（叠加【「状态」】，定义解析，「测试:状态实例/怒意」，「测试:状态/怒意」，「测试:角色/玩家」，1，115）；

测试.相等（过期重施【「项目」】【「层数」】，1）；

测试.相等（过期重施【「项目」】【「到期时间」】，125）；

定 刷新一：典 为 状态.计算施加状态（【】，定义解析，「测试:状态实例/守势」，「测试:状态/守势」，「测试:角色/玩家」，2，10）；

定 刷新二：典 为 状态.计算施加状态（刷新一【「状态」】，定义解析，「测试:状态实例/守势」，「测试:状态/守势」，「测试:角色/玩家」，1，15）；

测试.相等（刷新二【「项目」】【「层数」】，1）；

测试.相等（刷新二【「项目」】【「到期时间」】，35）；

定 唯一：典 为 状态.计算施加状态（【】，定义解析，「测试:状态实例/唯一」，「测试:状态/唯一」，「测试:角色/玩家」，1，1）；

法 重复唯一（） 则
    状态.计算施加状态（唯一【「状态」】，定义解析，「测试:状态实例/唯一」，「测试:状态/唯一」，「测试:角色/玩家」，1，2）；
终

应抛（重复唯一，状态.状态冲突错误）；

法 坏定义（） 则
    状态.规范状态定义（「测试:状态/坏」，{「名称」：「坏状态」，「属性」：{「攻击」：「高」}}）；
终

应抛（坏定义，状态.定义无效错误）；
