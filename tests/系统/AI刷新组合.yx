引「../../src/系统/AI系统.yx」为 AI；

引「../../src/系统/刷新系统.yx」为 刷新；

引「../../src/系统/时间系统.yx」为 时间；

引「../../src/测试支持/虚拟时钟.yx」为 测试时间；

引「../../src/持久化/内存仓储.yx」为 内存；

引「../../src/实体/实体.yx」为 实体；

引「标准:测试」为 测试；

法 刷新定义（编号：文）：典 则
    归 {「名称」：「AI 野狼刷新」，「目标」：「测试:NPC/野狼」，「位置」：「测试:房间/北郊」，「间隔秒」：1，「上限」：1，「行为编号」：刷新.刷新计划行为编号}；
终

法 实例编号（上下文：典）：文 则
    归 「测试:NPC实例/野狼一」；
终

法 刷新管理（编号：文）：典 则
    归 {「事务编号」：编号，「操作者编号」：「测试:系统/刷新」，「权限」：【刷新.刷新管理权限，时间.时间管理权限】}；
终

法 时间执行（编号：文）：典 则
    归 {「事务编号」：编号，「操作者编号」：「测试:系统/时间」，「权限」：【时间.时间管理权限】}；
终

定 仓储 为 内存.新仓储（）；

定 时钟 为 测试时间.虚拟时钟（0）；

定 任务注册 为 时间.新任务行为注册表（）；

定 时间服务 为 时间.时间服务（仓储，时钟，任务注册）；

定 AI行为注册 为 AI.新AI行为注册表（）；

AI.注册内置AI行为（AI行为注册）；

定 AI服务 为 AI.AI服务（仓储，时间服务，AI行为注册）；

AI.注册AI计划行为（任务注册，AI服务）；

法 生成器（事务，编号：文，定义：典，上下文：典）：典 则
    定 所实体 为 实体.新实体（编号，定义【「目标」】）；
    所实体.添加组件（AI.位置组件编号，{「所在」：定义【「位置」】，「坐标」：【】}，1）；
    所实体.添加组件（AI.战斗组件编号，{「生命」：30，「最大生命」：30，「攻击」：8，「防御」：2，「阵营」：「野兽」，「战斗中」：假}，1）；
    事务.写入实体快照（所实体.快照（），空）；
    AI服务.启用到事务（事务，编号，{「行为」：AI.敌对近战行为编号，「间隔毫秒」：100，「参数」：{「目标阵营」：【「玩家」】}，「状态」：{}}，上下文）；
    归 {「数据」：{「实体编号」：编号}}；
终

定 刷新服务 为 刷新.刷新服务（仓储，刷新定义，时间服务，生成器，实例编号）；

刷新.注册刷新计划行为（任务注册，刷新服务）；

刷新服务.启用（「测试:刷新/野狼」，刷新管理（「tx-ai-refresh-enable」））；

时钟.推进（1000）；

定 刷新候选：典 为 时间服务.调度器（）.到期（1000，1）【0】；

定 已刷新：典 为 时间服务.执行（刷新候选，时间执行（「tx-ai-refresh-run」））；

测试.相等（已刷新【「数据」】【「创建」】【0】，「测试:NPC实例/野狼一」）；

测试.相等（仓储.读取实体（「测试:NPC实例/野狼一」）.有组件（AI.AI组件编号），真）；

测试.相等（AI服务.查看（「测试:NPC实例/野狼一」）【「任务」】【「到期」】，1100）；

测试.相等（时间服务.读取任务（「测试:刷新/野狼」）【「到期」】，2000）；

测试.相等（长度（时间服务.各任务（）），2）；
