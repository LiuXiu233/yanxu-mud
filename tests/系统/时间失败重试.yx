引「../../src/系统/时间系统.yx」为 时间；

引「../../src/测试支持/虚拟时钟.yx」为 测试时间；

引「../../src/持久化/内存仓储.yx」为 内存；

引「标准:文字」为 文字；

引「标准:测试」为 测试；

法 失败行为（事务，任务：典，上下文：典）：典 则
    事务.设置世界变量（「测试.时间.不可提交」，真）；
    抛 「INTERNAL_SCHEDULE_FAILURE」；
终

法 成功行为（事务，任务：典，上下文：典）：典 则
    事务.设置世界变量（「测试.时间.已提交」，真）；
    归 {「数据」：{「重试」：真}}；
终

法 上下文（编号：文）：典 则
    归 {「事务编号」：编号，「操作者编号」：「测试:系统/时间」，「权限」：【时间.时间管理权限】}；
终

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.相等（命中，真）；
终

定 仓储 为 内存.新仓储（）；

定 时钟 为 测试时间.虚拟时钟（10）；

定 注册 为 时间.新任务行为注册表（）；

注册.注册（「测试:计划行为/重试」，失败行为）；

定 服务 为 时间.时间服务（仓储，时钟，注册）；

服务.计划（「测试.一次任务」，10，0，「测试:计划行为/重试」，{}，上下文（「tx-time-retry-schedule」））；

定 候选：典 为 服务.调度器（）.到期（10，1）【0】；

法 首次失败（） 则
    服务.执行（候选，上下文（「tx-time-retry-fail」））；
终

应抛（首次失败，时间.行为结果错误）；

测试.相等（仓储.读取世界变量（「测试.时间.不可提交」），空）；

测试.相等（服务.读取任务（「测试.一次任务」）【「修订」】，0）；

注册.替换（「测试:计划行为/重试」，成功行为）；

定 重试：典 为 服务.执行（候选，上下文（「tx-time-retry-success」））；

测试.相等（重试【「数据」】【「重试」】，真）；

测试.相等（服务.读取任务（「测试.一次任务」），空）；

测试.相等（仓储.读取世界变量（「测试.时间.已提交」），真）；

法 重复旧候选（） 则
    服务.执行（候选，上下文（「tx-time-retry-stale」））；
终

应抛（重复旧候选，时间.任务不存在错误）；
