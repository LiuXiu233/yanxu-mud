引「../../src/系统/物品系统.yx」为 物品；

引「../../src/持久化/内存仓储.yx」为 内存；

引「../../src/实体/实体.yx」为 实体；

引「标准:文字」为 文字；

引「标准:测试」为 测试；

法 定义解析（编号：文） 则
    归 {「名称」：编号，「描述」：「」，「类型」：「物品」，「可堆叠」：假，「属性」：{}，「价值」：0，「重量」：0}；
终

法 管理（编号：文）：典 则
    归 {「事务编号」：编号，「世界时间」：1，「操作者编号」：「测试:系统/管理员」，「权限」：【物品.物品管理权限】}；
终

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.断言（命中，「物品事务应抛出指定错误码」）；
终

法 新容器（编号：文，容量：数） 则
    定 所实体 为 实体.新实体（编号，「测试:原型/容器」）；
    所实体.添加组件（物品.容器组件编号，{「容量」：容量，「内容」：【】}，1）；
    归 所实体；
终

定 仓储 为 内存.新仓储（）；

定 房间 为 新容器（「测试:房间/仓库」，10）；

仓储.写入实体快照（房间.快照（），空）；

定 玩家 为 实体.新实体（「测试:角色/玩家」，「测试:原型/角色」）；

玩家.添加组件（物品.位置组件编号，{「所在」：「测试:房间/仓库」，「坐标」：【】}，1）；

玩家.添加组件（物品.背包组件编号，{「容量」：1，「物品」：【】}，1）；

仓储.写入实体快照（玩家.快照（），空）；

定 服务 为 物品.物品服务（仓储，定义解析）；

服务.创建物品（「测试:物品实例/已有」，「测试:物品/已有」，「测试:角色/玩家」，1，管理（「tx-item-capacity-seed-bag」））；

服务.创建物品（「测试:物品实例/待拾取」，「测试:物品/待拾取」，「测试:房间/仓库」，1，管理（「tx-item-capacity-seed-ground」））；

法 拾取到满包（） 则
    服务.拾取（「测试:角色/玩家」，「测试:物品实例/待拾取」，{「事务编号」：「tx-item-capacity-pickup」，「世界时间」：2，「操作者编号」：「测试:角色/玩家」}）；
终

应抛（拾取到满包，物品.容量不足错误）；

测试.相等（仓储.读取实体（「测试:角色/玩家」）.取组件（物品.背包组件编号）【「物品」】【0】，「测试:物品实例/已有」）；

测试.相等（仓储.读取实体（「测试:房间/仓库」）.取组件（物品.容器组件编号）【「内容」】【0】，「测试:物品实例/待拾取」）；

测试.相等（服务.读取物品（「测试:物品实例/待拾取」）【「所在」】，「测试:房间/仓库」）；
