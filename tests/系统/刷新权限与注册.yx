引「../../src/系统/刷新系统.yx」为 刷新；

引「../../src/系统/时间系统.yx」为 时间；

引「../../src/测试支持/虚拟时钟.yx」为 测试时间；

引「../../src/持久化/内存仓储.yx」为 内存；

引「标准:文字」为 文字；

引「标准:测试」为 测试；

法 定义解析（编号：文） 则
    归 {「名称」：「刷新」，「目标」：「测试:NPC/一」，「位置」：「测试:房间/一」，「间隔秒」：1，「上限」：1，「行为编号」：刷新.刷新计划行为编号}；
终

法 生成器（事务，实例编号：文，定义：典，上下文：典）：典 则
    归 {「数据」：{}}；
终

法 编号生成器（上下文：典）：文 则
    归 「测试:NPC实例/一」；
终

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.相等（命中，真）；
终

定 仓储 为 内存.新仓储（）；

定 注册 为 时间.新任务行为注册表（）；

定 时间服务 为 时间.时间服务（仓储，测试时间.虚拟时钟（0），注册）；

定 服务 为 刷新.刷新服务（仓储，定义解析，时间服务，生成器，编号生成器）；

刷新.注册刷新计划行为（注册，服务）；

法 缺刷新权限（） 则
    服务.启用（「测试:刷新/一」，{「事务编号」：「tx-refresh-no-refresh」，「操作者编号」：「测试:系统/管理」，「权限」：【时间.时间管理权限】}）；
终

应抛（缺刷新权限，刷新.权限不足错误）；

法 缺时间权限（） 则
    服务.启用（「测试:刷新/一」，{「事务编号」：「tx-refresh-no-time」，「操作者编号」：「测试:系统/管理」，「权限」：【刷新.刷新管理权限】}）；
终

应抛（缺时间权限，时间.权限不足错误）；

测试.相等（刷新.建立刷新系统（服务）.编号，「言域:系统/刷新」）；

测试.相等（刷新.建立刷新系统（服务）.优先级，250）；
