引「../../src/系统/掉落系统.yx」为 掉落；

引「标准:文字」为 文字；

引「标准:测试」为 测试；

定 定义：典 为 掉落.规范掉落表定义（「测试:掉落表/野狼」，{「名称」：「野狼掉落」，「条目」：【{「物品」：「测试:物品/狼皮」，「概率」：0.5，「最少」：1，「最多」：3}，{「物品」：「测试:物品/铜钱」，「概率」：0.5，「最少」：2，「最多」：2}】}）；

测试.相等（定义【「编号」】，「测试:掉落表/野狼」）；

测试.相等（定义【「条目」】【0】【「序号」】，0）；

定 序列：列<数> 为 【499999，1，500000】；

令 随机序号：数 为 0；

法 随机源（请求：典）：数 则
    定 当前：数 为 序列【随机序号】；
    置 随机序号 为 （随机序号 加 1）；
    测试.断言（（当前 小于 请求【「上限」】），「测试随机数必须位于请求上限内」）；
    归 当前；
终

定 结果：列 为 掉落.抽取掉落（定义，1，随机源）；

测试.相等（长度（结果），1）；

测试.相等（结果【0】【「物品编号」】，「测试:物品/狼皮」）；

测试.相等（结果【0】【「数量」】，2）；

测试.相等（随机序号，3）；

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.相等（命中，真）；
终

法 无效概率（） 则
    掉落.规范掉落表定义（「测试:掉落表/无效」，{「名称」：「无效」，「条目」：【{「物品」：「测试:物品/一」，「概率」：1.1，「最少」：1，「最多」：1}】}）；
终

应抛（无效概率，掉落.定义无效错误）；

法 越界随机（请求：典）：数 则
    归 请求【「上限」】；
终

法 随机越界（） 则
    掉落.抽取掉落（定义，1，越界随机）；
终

应抛（随机越界，掉落.随机源错误）；
