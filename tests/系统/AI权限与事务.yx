引「../../src/系统/AI系统.yx」为 AI；

引「../../src/系统/时间系统.yx」为 时间；

引「../../src/世界/世界事务.yx」为 世界事务；

引「../../src/测试支持/虚拟时钟.yx」为 测试时间；

引「../../src/持久化/内存仓储.yx」为 内存；

引「../../src/实体/实体.yx」为 实体；

引「标准:文字」为 文字；

引「标准:测试」为 测试；

法 空行为（事务，实体编号：文，AI数据：典，上下文：典）：典 则
    归 {「数据」：{}，「状态」：AI数据【「状态」】}；
终

法 上下文（编号：文，权限：列<文>）：典 则
    归 {「事务编号」：编号，「操作者编号」：「测试:系统/AI」，「权限」：权限}；
终

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.相等（命中，真）；
终

定 仓储 为 内存.新仓储（）；

仓储.写入实体快照（实体.新实体（「测试:NPC实例/一」，「测试:NPC/一」）.快照（），空）；

定 任务注册 为 时间.新任务行为注册表（）；

定 时间服务 为 时间.时间服务（仓储，测试时间.虚拟时钟（0），任务注册）；

定 行为注册 为 AI.新AI行为注册表（）；

行为注册.注册（「测试:AI行为/空」，空行为）；

定 服务 为 AI.AI服务（仓储，时间服务，行为注册）；

AI.注册AI计划行为（任务注册，服务）；

定 配置：典 为 {「行为」：「测试:AI行为/空」，「间隔毫秒」：100，「参数」：{}，「状态」：{}}；

法 缺AI权限（） 则
    服务.启用（「测试:NPC实例/一」，配置，上下文（「tx-ai-denied-ai」，【时间.时间管理权限】））；
终

应抛（缺AI权限，AI.权限不足错误）；

法 缺时间权限（） 则
    服务.启用（「测试:NPC实例/一」，配置，上下文（「tx-ai-denied-time」，【AI.AI管理权限】））；
终

应抛（缺时间权限，AI.权限不足错误）；

测试.相等（仓储.读取实体（「测试:NPC实例/一」）.有组件（AI.AI组件编号），假）；

测试.相等（长度（时间服务.各任务（）），0）；

测试.相等（AI.建立AI系统（服务）.编号，「言域:系统/AI」）；

测试.相等（AI.建立AI系统（服务）.优先级，260）；

定 组合事务 为 世界事务.开始（仓储，「tx-ai-composed-rollback」）；

服务.启用到事务（组合事务，「测试:NPC实例/一」，配置，上下文（「tx-ai-composed-rollback」，【】））；

组合事务.回滚（）；

测试.相等（仓储.读取实体（「测试:NPC实例/一」）.有组件（AI.AI组件编号），假）；

测试.相等（长度（时间服务.各任务（）），0）；

定 已启用：典 为 服务.启用（「测试:NPC实例/一」，配置，上下文（「tx-ai-enable-public」，【AI.AI管理权限，时间.时间管理权限】））；

测试.相等（已启用【「状态」】【「AI」】【「行为」】，「测试:AI行为/空」）；

测试.相等（长度（已启用【「事件」】），1）；

测试.相等（长度（时间服务.各任务（）），1）；

法 重复启用（） 则
    服务.启用（「测试:NPC实例/一」，配置，上下文（「tx-ai-enable-duplicate」，【AI.AI管理权限，时间.时间管理权限】））；
终

应抛（重复启用，AI.已启用错误）；

定 已停用：典 为 服务.停用（「测试:NPC实例/一」，上下文（「tx-ai-disable-public」，【AI.AI管理权限，时间.时间管理权限】））；

测试.相等（已停用【「AI」】【「行为」】，「测试:AI行为/空」）；

测试.相等（仓储.读取实体（「测试:NPC实例/一」）.有组件（AI.AI组件编号），假）；

测试.相等（长度（时间服务.各任务（）），0）；
