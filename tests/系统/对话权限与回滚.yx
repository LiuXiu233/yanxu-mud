引「../../src/系统/对话系统.yx」为 对话；

引「../../src/持久化/内存仓储.yx」为 内存；

引「../../src/实体/实体.yx」为 实体；

引「标准:文字」为 文字；

引「标准:测试」为 测试；

法 定义解析（编号：文） 则
    归 {「名称」：「测试」，「节点」：【{「编号」：「一」，「文字」：「第一句。」}】}；
终

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.相等（命中，真）；
终

定 仓储 为 内存.新仓储（）；

定 玩家 为 实体.新实体（「测试:角色/玩家」，「测试:原型/角色」）；

玩家.添加组件（对话.对话组件编号，{「节点」：「」，「进度」：{}}，1）；

仓储.写入实体快照（玩家.快照（），空）；

定 服务 为 对话.对话服务（仓储，定义解析）；

定 原修订：数 为 仓储.读取实体（「测试:角色/玩家」）.修订（）；

法 越权开始（） 则
    服务.开始（「测试:角色/玩家」，「测试:NPC/甲」，「测试:对话/测试」，{「事务编号」：「tx-dialogue-denied」，「世界时间」：1，「操作者编号」：「测试:角色/旁观者」}）；
终

应抛（越权开始，对话.权限不足错误）；

测试.相等（仓储.读取实体（「测试:角色/玩家」）.修订（），原修订）；

服务.开始（「测试:角色/玩家」，「测试:NPC/甲」，「测试:对话/测试」，{「事务编号」：「tx-dialogue-active」，「世界时间」：1，「操作者编号」：「测试:角色/玩家」}）；

定 活跃修订：数 为 仓储.读取实体（「测试:角色/玩家」）.修订（）；

法 重复开始（） 则
    服务.开始（「测试:角色/玩家」，「测试:NPC/乙」，「测试:对话/测试」，{「事务编号」：「tx-dialogue-duplicate」，「世界时间」：2，「操作者编号」：「测试:角色/玩家」}）；
终

应抛（重复开始，对话.对话进行中错误）；

测试.相等（仓储.读取实体（「测试:角色/玩家」）.修订（），活跃修订）；

定 已结束：典 为 服务.结束（「测试:角色/玩家」，{「事务编号」：「tx-dialogue-cancel」，「世界时间」：3，「操作者编号」：「测试:角色/玩家」}）；

测试.相等（已结束【「已结束」】，真）；

定 空闲修订：数 为 仓储.读取实体（「测试:角色/玩家」）.修订（）；

法 空闲继续（） 则
    服务.继续（「测试:角色/玩家」，{「事务编号」：「tx-dialogue-inactive」，「世界时间」：4，「操作者编号」：「测试:角色/玩家」}）；
终

应抛（空闲继续，对话.对话未开始错误）；

测试.相等（仓储.读取实体（「测试:角色/玩家」）.修订（），空闲修订）；

定 死者 为 实体.新实体（「测试:角色/死者」，「测试:原型/角色」）；

死者.添加组件（对话.对话组件编号，{「节点」：「」，「进度」：{}}，1）；

死者.添加标签（「死亡」）；

仓储.写入实体快照（死者.快照（），空）；

法 死者开始（） 则
    服务.开始（「测试:角色/死者」，「测试:NPC/甲」，「测试:对话/测试」，{「事务编号」：「tx-dialogue-dead」，「世界时间」：5，「操作者编号」：「测试:角色/死者」}）；
终

应抛（死者开始，对话.对话受限错误）；

测试.相等（仓储.读取实体（「测试:角色/死者」）.取组件（对话.对话组件编号）【「节点」】，「」）；
