引「../../src/系统/状态系统.yx」为 状态；

引「../../src/系统/属性系统.yx」为 属性；

引「../../src/持久化/内存仓储.yx」为 内存；

引「标准:测试」为 测试；

法 定义解析（编号：文） 则
    归 {「名称」：「测试状态」，「属性」：{}}；
终

定 聚合器 为 属性.新修正源聚合器（）；

状态.注册状态属性提供器（聚合器，定义解析）；

定 服务 为 状态.状态服务（内存.新仓储（），定义解析，聚合器）；

定 所系统 为 状态.建立状态系统（服务）；

测试.相等（所系统.编号，「言域:系统/状态」）；

测试.相等（所系统.优先级，170）；
