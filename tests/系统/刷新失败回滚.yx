引「../../src/系统/刷新系统.yx」为 刷新；

引「../../src/系统/时间系统.yx」为 时间；

引「../../src/测试支持/虚拟时钟.yx」为 测试时间；

引「../../src/持久化/内存仓储.yx」为 内存；

引「../../src/实体/实体.yx」为 实体；

引「标准:文字」为 文字；

引「标准:测试」为 测试；

法 定义解析（编号：文） 则
    归 {「名称」：「失败刷新」，「目标」：「测试:NPC/目标」，「位置」：「测试:房间/一」，「间隔秒」：1，「上限」：2，「行为编号」：刷新.刷新计划行为编号}；
终

法 生成器（事务，实例编号：文，定义：典，上下文：典）：典 则
    定 所实体 为 实体.新实体（实例编号，定义【「目标」】）；
    所实体.添加组件（刷新.位置组件编号，{「所在」：定义【「位置」】，「坐标」：【】}，1）；
    事务.写入实体快照（所实体.快照（），空）；
    若 （实例编号 等于 「测试:NPC实例/失败二」） 则
        抛 「INTERNAL_REFRESH_GENERATOR_FAILURE」；
    终
    归 {「数据」：{}}；
终

定 编号列：列<文> 为 【「测试:NPC实例/失败一」，「测试:NPC实例/失败二」】；

令 编号序号：数 为 0；

法 编号生成器（上下文：典）：文 则
    定 编号：文 为 编号列【编号序号】；
    置 编号序号 为 （编号序号 加 1）；
    归 编号；
终

法 上下文（编号：文）：典 则
    归 {「事务编号」：编号，「操作者编号」：「测试:系统/刷新」，「权限」：【刷新.刷新管理权限，时间.时间管理权限】}；
终

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.相等（命中，真）；
终

定 仓储 为 内存.新仓储（）；

定 时钟 为 测试时间.虚拟时钟（0）；

定 注册 为 时间.新任务行为注册表（）；

定 时间服务 为 时间.时间服务（仓储，时钟，注册）；

定 服务 为 刷新.刷新服务（仓储，定义解析，时间服务，生成器，编号生成器）；

刷新.注册刷新计划行为（注册，服务）；

服务.启用（「测试:刷新/失败」，上下文（「tx-refresh-failure-enable」））；

时钟.推进（1000）；

定 候选：典 为 时间服务.调度器（）.到期（1000，1）【0】；

法 执行失败（） 则
    时间服务.执行（候选，{「事务编号」：「tx-refresh-failure-run」，「操作者编号」：「测试:系统/时间」，「权限」：【时间.时间管理权限】}）；
终

应抛（执行失败，时间.行为结果错误）；

测试.相等（仓储.读取实体（「测试:NPC实例/失败一」），空）；

测试.相等（仓储.读取实体（「测试:NPC实例/失败二」），空）；

测试.相等（时间服务.读取任务（「测试:刷新/失败」）【「修订」】，0）；

测试.相等（时间服务.读取任务（「测试:刷新/失败」）【「到期」】，1000）；

测试.相等（服务.查看（「测试:刷新/失败」）【「数量」】，0）；
