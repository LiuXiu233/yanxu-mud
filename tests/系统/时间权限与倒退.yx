引「../../src/系统/时间系统.yx」为 时间；

引「../../src/持久化/内存仓储.yx」为 内存；

引「标准:文字」为 文字；

引「标准:测试」为 测试；

公 类 可变时钟 则
    私 域 当前值：数；
    公 法 初始化（当前：数） 则
        置 此.当前值 为 当前；
    终
    公 法 毫秒（）：数 则
        归 此.当前值；
    终
    公 法 设置（当前：数）：空 则
        置 此.当前值 为 当前；
        归 空；
    终
终

法 空行为（事务，任务：典，上下文：典）：典 则
    归 {「数据」：{}}；
终

法 管理（编号：文）：典 则
    归 {「事务编号」：编号，「操作者编号」：「测试:系统/时间」，「权限」：【时间.时间管理权限】}；
终

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.相等（命中，真）；
终

定 仓储 为 内存.新仓储（）；

定 时钟 为 可变时钟（100）；

定 注册 为 时间.新任务行为注册表（）；

注册.注册（「测试:计划行为/空」，空行为）；

定 服务 为 时间.时间服务（仓储，时钟，注册）；

法 越权计划（） 则
    服务.计划（「测试.越权」，100，0，「测试:计划行为/空」，{}，{「事务编号」：「tx-time-denied」，「操作者编号」：「测试:角色/玩家」}）；
终

应抛（越权计划，时间.权限不足错误）；

服务.计划（「测试.任务」，100，0，「测试:计划行为/空」，{}，管理（「tx-time-plan」））；

法 重复计划（） 则
    服务.计划（「测试.任务」，100，0，「测试:计划行为/空」，{}，管理（「tx-time-duplicate」））；
终

应抛（重复计划，时间.任务重复错误）；

时钟.设置（99）；

法 时钟倒退（） 则
    服务.毫秒（）；
终

应抛（时钟倒退，时间.时间倒退错误）；

时钟.设置（101）；

定 已取消：典 为 服务.取消（「测试.任务」，管理（「tx-time-cancel」））；

测试.相等（已取消【「任务」】【「编号」】，「测试.任务」）；

测试.相等（服务.读取任务（「测试.任务」），空）；
