引「../../src/系统/属性系统.yx」为 属性；

引「../../src/实体/实体.yx」为 实体；

引「标准:文字」为 文字；

引「标准:测试」为 测试；

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.断言（命中，「属性来源聚合应抛出指定错误码」）；
终

法 乙提供器（实体值，上下文：典）：列 则
    归 【{「来源」：「测试:修正/b」，「属性」：{「攻击」：2}}】；
终

法 甲提供器（实体值，上下文：典）：列 则
    归 【{「来源」：「测试:修正/a」，「属性」：{「防御」：1}}】；
终

法 替换提供器（实体值，上下文：典）：列 则
    归 【{「来源」：「测试:修正/c」，「属性」：{「攻击」：4}}】；
终

定 聚合器 为 属性.新修正源聚合器（）；

聚合器.注册（「测试:属性源/z」，乙提供器）；

聚合器.注册（「测试:属性源/a」，甲提供器）；

测试.相等（聚合器.编号列（）【0】，「测试:属性源/a」）；

定 玩家 为 实体.新实体（「测试:角色/玩家」，「测试:原型/角色」）；

玩家.添加组件（属性.属性组件编号，{「基础」：{「攻击」：5，「防御」：2}，「当前」：{「攻击」：5，「防御」：2}，「来源」：【】}，1）；

定 结果：典 为 属性.应用聚合属性（玩家，聚合器，{「世界时间」：1}）；

测试.相等（结果【「当前」】【「攻击」】，7）；

测试.相等（结果【「当前」】【「防御」】，3）；

测试.相等（结果【「来源」】【0】，「测试:修正/a」）；

测试.相等（结果【「来源」】【1】，「测试:修正/b」）；

聚合器.替换（「测试:属性源/z」，替换提供器）；

测试.相等（聚合器.聚合（玩家，{}）【1】【「来源」】，「测试:修正/c」）；

聚合器.移除（「测试:属性源/a」）；

测试.相等（长度（聚合器.编号列（）），1）；

法 重复提供器（） 则
    聚合器.注册（「测试:属性源/z」，乙提供器）；
终

应抛（重复提供器，属性.提供器重复错误）；

法 坏提供器（实体值，上下文：典） 则
    归 {}；
终

定 坏聚合器 为 属性.新修正源聚合器（）；

坏聚合器.注册（「测试:属性源/坏」，坏提供器）；

法 坏返回（） 则
    坏聚合器.聚合（玩家，{}）；
终

应抛（坏返回，属性.提供器数据错误）；
