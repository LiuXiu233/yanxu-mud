引「../../src/系统/时间系统.yx」为 时间；

引「../../src/测试支持/虚拟时钟.yx」为 测试时间；

引「../../src/持久化/内存仓储.yx」为 内存；

引「../../src/消息/消息协议.yx」为 消息；

引「标准:测试」为 测试；

法 行为（事务，任务：典，上下文：典）：典 则
    事务.设置世界变量（「测试.时间.已执行」，任务【「载荷」】【「参数」】）；
    归 {「数据」：{「值」：任务【「载荷」】【「参数」】}，「消息」：【消息.新消息（【消息.文字（「计划任务已执行」）】，{}）】}；
终

法 上下文（编号：文）：典 则
    归 {「事务编号」：编号，「操作者编号」：「测试:系统/时间」，「权限」：【时间.时间管理权限】}；
终

定 仓储 为 内存.新仓储（）；

定 时钟 为 测试时间.虚拟时钟（100）；

定 注册 为 时间.新任务行为注册表（）；

注册.注册（「测试:计划行为/记录」，行为）；

定 服务 为 时间.时间服务（仓储，时钟，注册）；

定 已计划：典 为 服务.计划（「测试.重复任务」，120，10，「测试:计划行为/记录」，{「次数」：1}，上下文（「tx-time-schedule」））；

测试.相等（已计划【「任务」】【「修订」】，0）；

测试.相等（服务.当前（）【「已记录」】，100）；

定 调度 为 服务.调度器（）；

测试.相等（调度.下一到期（），120）；

时钟.推进（19）；

测试.相等（长度（调度.到期（服务.毫秒（），10）），0）；

时钟.推进（1）；

定 到期：列 为 调度.到期（服务.毫秒（），10）；

测试.相等（长度（到期），1）；

定 已执行：典 为 服务.执行（到期【0】，上下文（「tx-time-run」））；

测试.相等（已执行【「数据」】【「值」】【「次数」】，1）；

测试.相等（已执行【「下次任务」】【「到期」】，130）；

测试.相等（已执行【「下次任务」】【「修订」】，1）；

测试.相等（仓储.读取世界变量（「测试.时间.已执行」）【「次数」】，1）；

测试.相等（已执行【「事件」】【0】【「事件种类」】，「言域:事件/计划任务执行」）；

测试.相等（消息.验证消息（已执行【「消息」】【0】），真）；

测试.相等（时间.持久调度器（仓储）.下一到期（），130）；
