引「../../src/系统/系统注册表.yx」为 系统；

引「标准:测试」为 测试；

令 记录：列<文> 为 【】；

法 处理甲（上下文） 则
    追加（记录，「甲」）；
    归 （上下文【「值」】 加 1）；
终

法 处理乙（上下文） 则
    追加（记录，「乙」）；
    归 （上下文【「值」】 加 2）；
终

法 加装饰（原处理） 则
    法 已装饰（上下文） 则
        追加（记录，「前」）；
        定 结果 为 原处理（上下文）；
        追加（记录，「后」）；
        归 结果；
    终
    归 已装饰；
终

定 注册 为 系统.注册表（）；

注册.注册（系统.系统（「言域:系统/乙」，20，处理乙，空，空））；

注册.注册（系统.系统（「言域:系统/甲」，10，处理甲，空，空））；

注册.装饰（「言域:系统/甲」，加装饰）；

注册.启动全部（{}）；

定 结果：列 为 注册.处理全部（{「值」：5}）；

测试.相等（结果【0】【「系统编号」】，「言域:系统/甲」）；

测试.相等（结果【0】【「结果」】，6）；

测试.相等（记录【0】，「前」）；

测试.相等（记录【1】，「甲」）；

测试.相等（记录【2】，「后」）；

注册.禁用（「言域:系统/甲」）；

定 仅乙：列 为 注册.处理全部（{「值」：5}）；

测试.相等（长度（仅乙），1）；

测试.相等（仅乙【0】【「系统编号」】，「言域:系统/乙」）；

注册.停止全部（{}）；

测试.相等（注册.查询（「言域:系统/甲」）.状态（），「已停止」）；
