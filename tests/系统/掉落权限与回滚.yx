引「../../src/系统/掉落系统.yx」为 掉落；

引「../../src/系统/物品系统.yx」为 物品；

引「../../src/持久化/内存仓储.yx」为 内存；

引「../../src/实体/实体.yx」为 实体；

引「标准:文字」为 文字；

引「标准:测试」为 测试；

法 物品解析（编号：文） 则
    归 {「名称」：编号，「描述」：「」，「类型」：「材料」，「可堆叠」：真，「属性」：{}，「价值」：0，「重量」：0}；
终

法 掉落解析（编号：文） 则
    归 {「名称」：「容量回滚」，「条目」：【{「物品」：「测试:物品/甲」，「概率」：1，「最少」：1，「最多」：1}，{「物品」：「测试:物品/乙」，「概率」：1，「最少」：1，「最多」：1}】}；
终

法 随机源（请求：典）：数 则
    归 0；
终

定 编号列：列<文> 为 【「测试:物品实例/甲」，「测试:物品实例/乙」】；

令 编号序号：数 为 0；

法 编号生成器（上下文：典）：文 则
    定 编号：文 为 编号列【编号序号】；
    置 编号序号 为 （编号序号 加 1）；
    归 编号；
终

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.相等（命中，真）；
终

定 仓储 为 内存.新仓储（）；

定 房间 为 实体.新实体（「测试:房间/小屋」，「测试:原型/房间」）；

房间.添加组件（物品.容器组件编号，{「容量」：1，「内容」：【】}，1）；

仓储.写入实体快照（房间.快照（），空）；

定 来源 为 实体.新实体（「测试:NPC/目标」，「测试:原型/目标」）；

来源.添加组件（掉落.位置组件编号，{「所在」：「测试:房间/小屋」，「坐标」：【】}，1）；

来源.添加组件（掉落.掉落组件编号，{「表」：「测试:掉落表/容量」，「次数」：1}，1）；

仓储.写入实体快照（来源.快照（），空）；

定 服务 为 掉落.掉落服务（仓储，掉落解析，物品.物品服务（仓储，物品解析），随机源，编号生成器）；

定 原修订：数 为 仓储.读取实体（「测试:NPC/目标」）.修订（）；

法 越权结算（） 则
    服务.结算（「测试:NPC/目标」，{「事务编号」：「tx-drop-denied」，「世界时间」：1，「操作者编号」：「测试:角色/玩家」}）；
终

应抛（越权结算，掉落.权限不足错误）；

法 容量失败（） 则
    服务.结算（「测试:NPC/目标」，{「事务编号」：「tx-drop-capacity」，「世界时间」：2，「操作者编号」：「测试:系统/掉落」，「权限」：【掉落.掉落管理权限】}）；
终

应抛（容量失败，物品.容量不足错误）；

测试.相等（仓储.读取实体（「测试:NPC/目标」）.修订（），原修订）；

测试.相等（服务.查看（「测试:NPC/目标」）【「掉落」】【「已结算」】，假）；

测试.相等（长度（仓储.读取实体（「测试:房间/小屋」）.取组件（物品.容器组件编号）【「内容」】），0）；

测试.相等（仓储.读取实体（「测试:物品实例/甲」），空）；

测试.相等（仓储.读取实体（「测试:物品实例/乙」），空）；
