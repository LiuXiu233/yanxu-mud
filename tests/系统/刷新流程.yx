引「../../src/系统/刷新系统.yx」为 刷新；

引「../../src/系统/时间系统.yx」为 时间；

引「../../src/测试支持/虚拟时钟.yx」为 测试时间；

引「../../src/持久化/内存仓储.yx」为 内存；

引「../../src/实体/实体.yx」为 实体；

引「../../src/消息/消息协议.yx」为 消息；

引「标准:测试」为 测试；

法 定义解析（编号：文） 则
    归 {「名称」：「野狼刷新」，「目标」：「测试:NPC/野狼」，「位置」：「测试:房间/北郊」，「间隔秒」：1，「上限」：3，「行为编号」：刷新.刷新计划行为编号}；
终

法 新实例（编号：文，死亡：理） 则
    定 所实体 为 实体.新实体（编号，「测试:NPC/野狼」）；
    所实体.添加组件（刷新.位置组件编号，{「所在」：「测试:房间/北郊」，「坐标」：【】}，1）；
    若 死亡 则
        所实体.添加标签（「死亡」）；
    终
    归 所实体；
终

法 生成器（事务，实例编号：文，定义：典，上下文：典）：典 则
    事务.写入实体快照（新实例（实例编号，假）.快照（），空）；
    归 {「数据」：{「实例编号」：实例编号}}；
终

定 编号列：列<文> 为 【「测试:NPC实例/野狼二」，「测试:NPC实例/野狼三」】；

令 编号序号：数 为 0；

法 编号生成器（上下文：典）：文 则
    定 编号：文 为 编号列【编号序号】；
    置 编号序号 为 （编号序号 加 1）；
    归 编号；
终

法 管理上下文（编号：文）：典 则
    归 {「事务编号」：编号，「操作者编号」：「测试:系统/刷新」，「权限」：【刷新.刷新管理权限，时间.时间管理权限】}；
终

法 执行上下文（编号：文）：典 则
    归 {「事务编号」：编号，「操作者编号」：「测试:系统/时间」，「权限」：【时间.时间管理权限】}；
终

定 仓储 为 内存.新仓储（）；

仓储.写入实体快照（新实例（「测试:NPC实例/野狼一」，假）.快照（），空）；

仓储.写入实体快照（新实例（「测试:NPC实例/野狼尸体」，真）.快照（），空）；

定 时钟 为 测试时间.虚拟时钟（0）；

定 注册 为 时间.新任务行为注册表（）；

定 时间服务 为 时间.时间服务（仓储，时钟，注册）；

定 服务 为 刷新.刷新服务（仓储，定义解析，时间服务，生成器，编号生成器）；

刷新.注册刷新计划行为（注册，服务）；

定 已启用：典 为 服务.启用（「测试:刷新/野狼」，管理上下文（「tx-refresh-enable」））；

测试.相等（已启用【「时间」】【「任务」】【「到期」】，1000）；

时钟.推进（1000）；

定 候选：典 为 时间服务.调度器（）.到期（1000，1）【0】；

定 首次：典 为 时间服务.执行（候选，执行上下文（「tx-refresh-first」））；

测试.相等（长度（首次【「数据」】【「创建」】），2）；

测试.相等（首次【「数据」】【「最终数量」】，3）；

测试.相等（首次【「下次任务」】【「到期」】，2000）；

测试.相等（首次【「事件」】【0】【「事件种类」】，「言域:事件/刷新执行」）；

测试.相等（首次【「事件」】【1】【「事件种类」】，「言域:事件/计划任务执行」）；

测试.相等（消息.验证消息（首次【「消息」】【0】），真）；

测试.相等（服务.查看（「测试:刷新/野狼」）【「数量」】，3）；

测试.相等（编号序号，2）；

时钟.推进（1000）；

定 再次候选：典 为 时间服务.调度器（）.到期（2000，1）【0】；

定 再次：典 为 时间服务.执行（再次候选，执行上下文（「tx-refresh-again」））；

测试.相等（长度（再次【「数据」】【「创建」】），0）；

测试.相等（长度（再次【「事件」】），1）；

测试.相等（长度（再次【「消息」】），0）；

测试.相等（再次【「下次任务」】【「到期」】，3000）；

测试.相等（编号序号，2）；
