引「../../src/系统/属性系统.yx」为 属性；

引「../../src/持久化/内存仓储.yx」为 内存；

引「标准:测试」为 测试；

定 服务 为 属性.属性服务（内存.新仓储（），属性.新修正源聚合器（））；

定 所系统 为 属性.建立属性系统（服务）；

测试.相等（所系统.编号，「言域:系统/属性」）；

测试.相等（所系统.优先级，160）；

所系统.启动（{}）；

测试.相等（所系统.处理（{「动作」：「查询」，「实体编号」：「测试:角色/不存在」}），空）；

所系统.停止（{}）；
