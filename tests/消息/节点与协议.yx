引「../../src/消息/消息协议.yx」为 消息；

引「标准:测试」为 测试；

引「标准:文字」为 文字库；

法 应抛代码（操作：法，代码：文）：空 则
    令 已抛：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 已抛 为 文字库.始于（所误.消息，代码）；
    终
    测试.断言（已抛，「操作应抛出指定稳定错误码」）；
终

定 普通 为 消息.文字（「青石镇醒来了。」）；

定 富文 为 消息.富文本（【{「文字」：「危险」，「样式」：{「前景」：「红」，「加粗」：真}}，{「文字」：「将至」，「样式」：{「前景」：「黄」，「下划线」：真}}】）；

定 人物 为 消息.人物引用（「青石镇:人物/铁匠」，「老铁匠」）；

定 物品 为 消息.物品引用（「青石镇:物品/短剑」，「短剑」）；

定 房间 为 消息.房间引用（「青石镇:房间/南门」，「南门」）；

定 命令 为 消息.命令链接（「向北」，「走 北」）；

定 所列表 为 消息.列表（【【人物，消息.文字（「正在打铁」）】，【物品】】，假）；

定 所表格 为 消息.表格（【「名称」，「位置」】，【【【人物】，【房间】】，【【物品】，【消息.文字（「背包」）】】】）；

定 状态 为 消息.状态栏（「天气」，「晴」，「微风」）；

定 进度 为 消息.进度条（「体力」，7，10）；

定 场景 为 消息.场景描述（「青石镇南门」，【普通】，【消息.房间引用（「青石镇:房间/北街」，「北」）】）；

定 系统 为 消息.系统提示（「成功」，【消息.文字（「已保存」）】）；

定 所错误 为 消息.错误（「YANYU_TEST」，「动作失败」，{「原因」：「太远」}）；

定 所频道 为 消息.频道（「闲聊」，「子衿」，【消息.文字（「诸位安好」）】）；

定 所战斗 为 消息.战斗（「子衿」，「野狼」，「挥剑」，8，【消息.文字（「 点伤害」）】）；

定 扩展 为 消息.自定义（「青石镇:消息/天气」，{「云量」：2}，【消息.文字（「天色清朗」）】）；

定 各节点：列 为 【普通，富文，人物，物品，房间，命令，所列表，所表格，状态，进度，场景，系统，所错误，所频道，所战斗，扩展】；

定 元数据：典 为 {「追踪编号」：「trace-1」，「语言」：「zh-CN」}；

定 信封：典 为 消息.新消息（各节点，元数据）；

测试.相等（消息.验证消息（信封），真）；

测试.相等（信封【「协议」】，消息.协议名称）；

测试.相等（信封【「版本」】，消息.协议版本）；

测试.相等（长度（信封【「节点」】），16）；

测试.相等（信封【「节点」】【15】【「自定义种类」】，「青石镇:消息/天气」）；

置 元数据【「语言」】 为 「en-US」；

置 各节点【0】【「内容」】 为 「已改」；

测试.相等（信封【「元数据」】【「语言」】，「zh-CN」）；

测试.相等（信封【「节点」】【0】【「内容」】，「青石镇醒来了。」）；

法 坏样式（） 则
    消息.富文本（【{「文字」：「注入」，「样式」：{「前景」：「31;5」}}】）；
终

应抛代码（坏样式，「YANYU_MESSAGE_INVALID」）；

法 坏普通值（） 则
    法 处理（） 则
        归 空；
    终
    消息.自定义（「青石镇:消息/坏值」，{「处理器」：处理}，【】）；
终

应抛代码（坏普通值，「YANYU_MESSAGE_INVALID」）；

法 空回退（） 则
    消息.自定义（「青石镇:消息/无回退」，{}，【】）；
终

应抛代码（空回退，「YANYU_MESSAGE_INVALID」）；

法 坏版本（） 则
    定 副本：典 为 消息.新消息（【消息.文字（「测试」）】，{}）；
    置 副本【「版本」】 为 2；
    消息.验证消息（副本）；
终

应抛代码（坏版本，「YANYU_MESSAGE_VERSION_UNSUPPORTED」）；

法 坏表格（） 则
    消息.表格（【「甲」，「乙」】，【【【消息.文字（「仅一格」）】】】）；
终

应抛代码（坏表格，「YANYU_MESSAGE_INVALID」）；

法 循环节点（） 则
    令 节点：典 为 {「种类」：消息.种类自定义，「自定义种类」：「青石镇:消息/循环」，「数据」：{}，「回退」：【】}；
    置 节点【「数据」】【「自己」】 为 节点；
    消息.验证节点（节点）；
终

应抛代码（循环节点，「YANYU_MESSAGE_INVALID」）；

法 过深数据（） 则
    令 根：典 为 {}；
    令 当前：典 为 根；
    逐 次：数 于 范围（0，70） 则
        令 下一层：典 为 {}；
        置 当前【「下一层」】 为 下一层；
        置 当前 为 下一层；
    终
    消息.自定义（「青石镇:消息/过深」，根，【】）；
终

应抛代码（过深数据，「YANYU_MESSAGE_INVALID」）；

法 过多节点（） 则
    令 各值：列 为 【】；
    逐 次：数 于 范围（0，10001） 则
        追加（各值，次）；
    终
    消息.自定义（「青石镇:消息/过多」，各值，【】）；
终

应抛代码（过多节点，「YANYU_MESSAGE_INVALID」）；
