引「../../src/应用/账户服务.yx」为 账户；

引「../../src/持久化/内存仓储.yx」为 内存；

引「标准:哈希」为 哈希；

引「标准:文字」为 文字；

引「标准:JSON」为 JSON；

引「标准:测试」为 测试；

法 生成摘要（密码：文）：典 则
    归 {「方案」：「测试/SHA256」，「摘要」：哈希.SHA256（（「仅用于测试的盐:」 加 密码））}；
终

法 验证摘要（密码：文，凭据：典）：理 则
    归 （凭据【「摘要」】 等于 哈希.SHA256（（「仅用于测试的盐:」 加 密码）））；
终

法 上下文（编号：文，世界时间：数，操作者：文，权限：列<文>）：典 则
    归 {「事务编号」：（「tx-账户-」 加 编号），「追踪编号」：（「trace-账户-」 加 编号），「世界时间」：世界时间，「现实时间」：世界时间，「操作者编号」：操作者，「权限」：权限，「内容包版本」：「测试@0.5.0」}；
终

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.断言（命中，「应抛出指定账户错误码」）；
终

定 仓储 为 内存.新仓储（）；

定 服务 为 账户.账户服务（仓储，生成摘要，验证摘要，{「最大失败次数」：3，「锁定毫秒」：1000，「最短密码长度」：10}）；

定 创建：典 为 服务.创建账户（「测试:账户/青禾」，「QingHe」，「可靠的测试密码123」，上下文（「创建」，100，「」，【】））；

测试.相等（创建【「成功」】，真）；

测试.相等（创建【「账户」】【「登录名」】，「QingHe」）；

测试.相等（创建【「事件」】【0】【「事件种类」】，「言域:事件/账户创建」）；

测试.相等（长度（创建【「消息」】），1）；

定 已保存 为 仓储.读取实体（「测试:账户/青禾」）；

定 已保存账户：典 为 已保存.取组件（账户.账户组件编号）；

测试.相等（已保存账户【「登录键」】，「qinghe」）；

测试.相等（长度（文字.分割（JSON.序列化（创建），「可靠的测试密码123」）），1）；

测试.相等（长度（文字.分割（JSON.序列化（创建），已保存账户【「凭据」】【「摘要」】）），1）；

法 重复登录名（） 则
    服务.创建账户（「测试:账户/另一个」，「qinghe」，「另一个可靠密码123」，上下文（「重复」，101，「」，【】））；
终

应抛（重复登录名，账户.账户重复错误）；

测试.相等（服务.读取账户（「测试:账户/青禾」）【「账户编号」】，「测试:账户/青禾」）；

测试.相等（服务.读取账户（「测试:账户/不存在」），空）；
