引「../../src/应用/角色服务.yx」为 角色；

引「../../src/持久化/内存仓储.yx」为 内存；

引「../../src/实体/实体.yx」为 实体；

引「标准:文字」为 文字；

引「标准:测试」为 测试；

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.断言（命中，「应抛出指定角色错误码」）；
终

定 仓储 为 内存.新仓储（）；

定 账户实体 为 实体.新实体（「测试:账户/甲」，「言域:原型/账户」）；

账户实体.添加组件（角色.账户组件编号，{「账户编号」：「测试:账户/甲」，「角色」：【「测试:角色/已有」】，「状态」：「正常」}，1）；

仓储.写入实体快照（账户实体.快照（），空）；

定 服务 为 角色.角色服务（仓储，{「最大角色数」：1}）；

法 超限（） 则
    服务.创建角色（「测试:账户/甲」，「测试:角色/新建」，「新建」，{「初始房间」：「青石镇:房间/中央街」}，{「事务编号」：「tx-角色-上限」，「世界时间」：1，「操作者编号」：「测试:账户/甲」}）；
终

应抛（超限，角色.角色上限错误）；

测试.相等（仓储.读取实体（「测试:角色/新建」），空）；
