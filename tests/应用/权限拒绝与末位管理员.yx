引「../../src/应用/权限服务.yx」为 权限；

引「../../src/持久化/内存仓储.yx」为 内存；

引「../../src/实体/实体.yx」为 实体；

引「标准:文字」为 文字；

引「标准:测试」为 测试；

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.断言（命中，「应抛出指定权限错误码」）；
终

定 仓储 为 内存.新仓储（）；

定 管理员 为 实体.新实体（「测试:账户/管理员」，「言域:原型/账户」）；

管理员.添加组件（权限.权限组件编号，{「角色」：【权限.管理员角色组】，「授权」：【】}，1）；

仓储.写入实体快照（管理员.快照（），空）；

定 玩家 为 实体.新实体（「测试:账户/玩家」，「言域:原型/账户」）；

玩家.添加组件（权限.权限组件编号，{「角色」：【权限.玩家角色组】，「授权」：【】}，1）；

仓储.写入实体快照（玩家.快照（），空）；

定 服务 为 权限.权限服务（仓储，{}）；

法 玩家越权（） 则
    服务.授予权限（「测试:账户/玩家」，「测试:权限/传送」，{「事务编号」：「tx-权限-越权」，「世界时间」：1，「操作者编号」：「测试:账户/玩家」}）；
终

应抛（玩家越权，权限.权限不足错误）；

法 撤销末位管理员（） 则
    服务.撤销角色组（「测试:账户/管理员」，权限.管理员角色组，{「事务编号」：「tx-权限-末位管理员」，「世界时间」：2，「操作者编号」：「测试:账户/管理员」}）；
终

应抛（撤销末位管理员，权限.末位管理员错误）；

测试.相等（包含（仓储.读取实体（「测试:账户/管理员」）.取组件（权限.权限组件编号）【「角色」】，权限.管理员角色组），真）；
