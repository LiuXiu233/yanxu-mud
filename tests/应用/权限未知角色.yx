引「../../src/应用/权限服务.yx」为 权限；

引「../../src/实体/实体.yx」为 实体；

引「标准:文字」为 文字；

引「标准:测试」为 测试；

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.断言（命中，「应抛出指定权限错误码」）；
终

定 玩家 为 实体.新实体（「测试:角色/未知」，「言域:原型/玩家角色」）；

玩家.添加组件（权限.权限组件编号，{「角色」：【「未注册角色组」】，「授权」：【】}，1）；

法 展开未知角色（） 则
    权限.有效权限（玩家，{}）；
终

应抛（展开未知角色，权限.角色组未知错误）；
