引「../../src/应用/频道服务.yx」为 频道；

引「../../src/持久化/内存仓储.yx」为 内存；

引「../../src/实体/实体.yx」为 实体；

引「../../src/消息/消息协议.yx」为 消息；

引「标准:文字」为 文字；

引「标准:测试」为 测试；

法 新角色（编号：文，名称：文，房间：文，静音：列<文>） 则
    定 所实体 为 实体.新实体（编号，「测试:原型/角色」）；
    所实体.添加组件（频道.描述组件编号，{「名称」：名称，「描述」：「」}，1）；
    所实体.添加组件（频道.位置组件编号，{「所在」：房间，「坐标」：【】}，1）；
    所实体.添加组件（频道.频道组件编号，{「频道」：【频道.附近频道编号，频道.世界频道编号】，「静音」：静音}，1）；
    归 所实体；
终

法 上下文（编号：文，操作者：文，权限：列<文>）：典 则
    归 {「事务编号」：编号，「操作者编号」：操作者，「权限」：权限，「世界时间」：10，「现实时间」：20}；
终

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.相等（命中，真）；
终

定 仓储 为 内存.新仓储（）；

仓储.写入实体快照（新角色（「测试:角色/a」，「阿青」，「测试:房间/一」，【】）.快照（），空）；

仓储.写入实体快照（新角色（「测试:角色/b」，「白芷」，「测试:房间/一」，【】）.快照（），空）；

仓储.写入实体快照（新角色（「测试:角色/c」，「长风」，「测试:房间/二」，【】）.快照（），空）；

仓储.写入实体快照（新角色（「测试:角色/d」，「冬青」，「测试:房间/一」，【频道.附近频道编号，频道.世界频道编号】）.快照（），空）；

定 服务 为 频道.频道服务（仓储，频道.默认频道注册表（））；

定 附近：典 为 服务.发送（「测试:角色/a」，「附近」，「  大家好  」，上下文（「tx-channel-local」，「测试:角色/a」，【频道.游戏权限】））；

测试.相等（附近【「内容」】，「大家好」）；

测试.相等（长度（附近【「接收者」】），2）；

测试.相等（附近【「接收者」】【0】，「测试:角色/a」）；

测试.相等（附近【「接收者」】【1】，「测试:角色/b」）；

测试.相等（附近【「事件」】【0】【「事件种类」】，「言域:事件/频道消息」）；

测试.相等（消息.验证消息（附近【「消息」】【0】），真）；

测试.相等（附近【「消息」】【0】【「元数据」】【「接收者」】【1】，「测试:角色/b」）；

测试.相等（附近【「消息」】【0】【「节点」】【0】【「发送者」】，「阿青」）；

定 世界：典 为 服务.发送（「测试:角色/a」，频道.世界频道编号，「世界消息」，上下文（「tx-channel-world」，「测试:角色/a」，【频道.游戏权限】））；

测试.相等（长度（世界【「接收者」】），3）；

测试.相等（世界【「接收者」】【2】，「测试:角色/c」）；

法 越权发送（） 则
    服务.发送（「测试:角色/a」，频道.世界频道编号，「越权」，上下文（「tx-channel-permission」，「测试:角色/c」，【频道.游戏权限】））；
终

应抛（越权发送，频道.权限不足错误）；

法 缺游戏权限（） 则
    服务.发送（「测试:角色/a」，频道.世界频道编号，「无权限」，上下文（「tx-channel-game-permission」，「测试:角色/a」，【】））；
终

应抛（缺游戏权限，频道.权限不足错误）；

令 过长字符：列<文> 为 【】；

令 过长序号：数 为 0；

当 （过长序号 小于 501） 则
    追加（过长字符，「甲」）；
    置 过长序号 为 （过长序号 加 1）；
终

定 过长内容：文 为 文字.联结（过长字符，「」）；

法 超长消息（） 则
    服务.发送（「测试:角色/a」，频道.世界频道编号，过长内容，上下文（「tx-channel-too-long」，「测试:角色/a」，【频道.游戏权限】））；
终

应抛（超长消息，频道.参数无效错误）；
