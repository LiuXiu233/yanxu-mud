引「../../src/应用/账户服务.yx」为 账户；

引「../../src/持久化/内存仓储.yx」为 内存；

引「标准:哈希」为 哈希；

引「标准:文字」为 文字；

引「标准:测试」为 测试；

法 生成摘要（密码：文）：典 则
    归 {「方案」：「测试/SHA256」，「摘要」：哈希.SHA256（（「仅用于测试的盐:」 加 密码））}；
终

法 验证摘要（密码：文，凭据：典）：理 则
    归 （凭据【「摘要」】 等于 哈希.SHA256（（「仅用于测试的盐:」 加 密码）））；
终

法 上下文（编号：文，世界时间：数，操作者：文，权限：列<文>）：典 则
    归 {「事务编号」：（「tx-冻结-」 加 编号），「追踪编号」：（「trace-冻结-」 加 编号），「世界时间」：世界时间，「现实时间」：世界时间，「操作者编号」：操作者，「权限」：权限}；
终

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.断言（命中，「应抛出指定账户错误码」）；
终

定 仓储 为 内存.新仓储（）；

定 服务 为 账户.账户服务（仓储，生成摘要，验证摘要，{}）；

服务.创建账户（「测试:账户/青禾」，「QingHe」，「可靠的测试密码123」，上下文（「创建」，100，「」，【】））；

法 无权冻结（） 则
    服务.设置冻结（「测试:账户/青禾」，真，上下文（「无权」，101，「测试:账户/管理员」，【】））；
终

应抛（无权冻结，账户.权限不足错误）；

定 已冻结：典 为 服务.设置冻结（「测试:账户/青禾」，真，上下文（「冻结」，102，「测试:账户/管理员」，【账户.账户管理权限】））；

测试.相等（已冻结【「账户」】【「状态」】，「冻结」）；

测试.相等（已冻结【「事件」】【0】【「来源」】，「测试:账户/管理员」）；

定 冻结登录：典 为 服务.验证登录（「qinghe」，「可靠的测试密码123」，上下文（「登录」，103，「」，【】））；

测试.相等（冻结登录【「代码」】，账户.账户冻结错误）；

定 已解冻：典 为 服务.设置冻结（「测试:账户/青禾」，假，上下文（「解冻」，104，「测试:账户/管理员」，【账户.账户管理权限】））；

测试.相等（已解冻【「账户」】【「状态」】，「正常」）；
