引「../../src/应用/角色服务.yx」为 角色；

引「../../src/持久化/内存仓储.yx」为 内存；

引「../../src/实体/实体.yx」为 实体；

引「标准:测试」为 测试；

定 仓储 为 内存.新仓储（）；

定 所角色 为 实体.新实体（「测试:角色/甲」，「言域:原型/玩家角色」）；

所角色.添加组件（角色.角色组件编号，{「账户编号」：「测试:账户/甲」，「名称」：「甲」，「删除状态」：「正常」，「删除保护到」：0}，1）；

仓储.写入实体快照（所角色.快照（），空）；

定 账户实体 为 实体.新实体（「测试:账户/甲」，「言域:原型/账户」）；

账户实体.添加组件（角色.账户组件编号，{「账户编号」：「测试:账户/甲」，「角色」：【「测试:角色/甲」】，「状态」：「正常」}，1）；

仓储.写入实体快照（账户实体.快照（），空）；

定 服务 为 角色.角色服务（仓储，{「删除保护毫秒」：1000}）；

定 请求：典 为 服务.请求删除（「测试:账户/甲」，「测试:角色/甲」，{「事务编号」：「tx-角色-管理员请求」，「世界时间」：100，「现实时间」：100，「操作者编号」：「测试:账户/管理员」，「权限」：【角色.角色管理权限】}）；

测试.相等（请求【「成功」】，真）；

测试.相等（请求【「事件」】【0】【「来源」】，「测试:账户/管理员」）；

测试.相等（请求【「角色」】【「删除保护到」】，1100）；
