引「../../src/应用/频道服务.yx」为 频道；

引「../../src/持久化/内存仓储.yx」为 内存；

引「../../src/实体/实体.yx」为 实体；

引「标准:文字」为 文字；

引「标准:测试」为 测试；

法 新角色（编号：文，名称：文） 则
    定 所实体 为 实体.新实体（编号，「测试:原型/角色」）；
    所实体.添加组件（频道.描述组件编号，{「名称」：名称，「描述」：「」}，1）；
    所实体.添加组件（频道.频道组件编号，{「频道」：【频道.附近频道编号，频道.世界频道编号】，「静音」：【】}，1）；
    归 所实体；
终

法 上下文（编号：文，操作者：文，权限：列<文>）：典 则
    归 {「事务编号」：编号，「操作者编号」：操作者，「权限」：权限}；
终

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.相等（命中，真）；
终

定 仓储 为 内存.新仓储（）；

仓储.写入实体快照（新角色（「测试:角色/a」，「阿青」）.快照（），空）；

仓储.写入实体快照（新角色（「测试:角色/b」，「白芷」）.快照（），空）；

定 服务 为 频道.频道服务（仓储，频道.默认频道注册表（））；

法 越权静音（） 则
    服务.静音（「测试:角色/a」，频道.世界频道编号，上下文（「tx-channel-setting-denied」，「测试:角色/b」，【频道.游戏权限】））；
终

应抛（越权静音，频道.权限不足错误）；

测试.相等（长度（服务.查看（「测试:角色/a」）【「频道」】【「静音」】），0）；

服务.静音（「测试:角色/a」，频道.世界频道编号，上下文（「tx-channel-mute-world」，「测试:角色/a」，【频道.游戏权限】））；

法 静音后发送（） 则
    服务.发送（「测试:角色/a」，频道.世界频道编号，「不应发送」，上下文（「tx-channel-muted-send」，「测试:角色/a」，【频道.游戏权限】））；
终

应抛（静音后发送，频道.发送受限错误）；

服务.取消静音（「测试:角色/a」，频道.世界频道编号，上下文（「tx-channel-unmute-world」，「测试:角色/a」，【频道.游戏权限】））；

服务.退订（「测试:角色/a」，频道.世界频道编号，上下文（「tx-channel-unsubscribe-world」，「测试:角色/a」，【频道.游戏权限】））；

法 退订后发送（） 则
    服务.发送（「测试:角色/a」，频道.世界频道编号，「不应发送」，上下文（「tx-channel-unsubscribed-send」，「测试:角色/a」，【频道.游戏权限】））；
终

应抛（退订后发送，频道.发送受限错误）；

服务.订阅（「测试:角色/a」，频道.世界频道编号，上下文（「tx-channel-subscribe-world」，「测试:角色/a」，【频道.游戏权限】））；

测试.相等（频道.建立频道系统（服务）.编号，「言域:系统/频道」）；

测试.相等（频道.建立频道系统（服务）.优先级，270）；
