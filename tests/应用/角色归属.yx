引「../../src/应用/角色服务.yx」为 角色；

引「../../src/持久化/内存仓储.yx」为 内存；

引「../../src/实体/实体.yx」为 实体；

引「标准:文字」为 文字；

引「标准:测试」为 测试；

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.断言（命中，「应抛出指定角色错误码」）；
终

定 仓储 为 内存.新仓储（）；

定 所角色 为 实体.新实体（「测试:角色/甲」，「言域:原型/玩家角色」）；

所角色.添加组件（角色.角色组件编号，{「账户编号」：「测试:账户/甲」，「名称」：「甲」，「删除状态」：「正常」，「删除保护到」：0}，1）；

仓储.写入实体快照（所角色.快照（），空）；

定 服务 为 角色.角色服务（仓储，{}）；

法 跨账户删除（） 则
    服务.请求删除（「测试:账户/乙」，「测试:角色/甲」，{「事务编号」：「tx-角色-跨账户」，「世界时间」：1，「操作者编号」：「测试:账户/乙」}）；
终

应抛（跨账户删除，角色.归属错误）；

测试.相等（仓储.读取实体（「测试:角色/甲」）.取组件（角色.角色组件编号）【「删除状态」】，「正常」）；
