引「../../src/应用/角色服务.yx」为 角色；

引「../../src/持久化/内存仓储.yx」为 内存；

引「../../src/实体/实体.yx」为 实体；

引「标准:测试」为 测试；

定 仓储 为 内存.新仓储（）；

定 所角色 为 实体.新实体（「测试:角色/青禾」，「言域:原型/玩家角色」）；

所角色.添加组件（角色.角色组件编号，{「账户编号」：「测试:账户/甲」，「名称」：「青禾」，「删除状态」：「待删除」，「删除保护到」：1100}，1）；

所角色.添加标签（「待删除」）；

仓储.写入实体快照（所角色.快照（），空）；

定 账户实体 为 实体.新实体（「测试:账户/甲」，「言域:原型/账户」）；

账户实体.添加组件（角色.账户组件编号，{「账户编号」：「测试:账户/甲」，「角色」：【「测试:角色/青禾」】，「状态」：「正常」}，1）；

仓储.写入实体快照（账户实体.快照（），空）；

定 服务 为 角色.角色服务（仓储，{}）；

定 取消：典 为 服务.取消删除（「测试:账户/甲」，「测试:角色/青禾」，{「事务编号」：「tx-角色-取消删除」，「世界时间」：200，「现实时间」：200，「操作者编号」：「测试:账户/甲」}）；

测试.相等（取消【「角色」】【「删除状态」】，「正常」）；

测试.相等（取消【「角色」】【「删除保护到」】，0）；

测试.相等（仓储.读取实体（「测试:角色/青禾」）.有标签（「待删除」），假）；
