引「../../src/应用/权限服务.yx」为 权限；

引「../../src/持久化/内存仓储.yx」为 内存；

引「../../src/实体/实体.yx」为 实体；

引「../../src/系统/系统注册表.yx」为 系统；

引「标准:文字」为 文字；

引「标准:测试」为 测试；

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.断言（命中，「权限系统应拒绝未知动作」）；
终

定 仓储 为 内存.新仓储（）；

定 玩家 为 实体.新实体（「测试:角色/玩家」，「言域:原型/玩家角色」）；

玩家.添加组件（权限.权限组件编号，{「角色」：【权限.玩家角色组】，「授权」：【】}，1）；

仓储.写入实体快照（玩家.快照（），空）；

定 服务 为 权限.权限服务（仓储，{}）；

定 注册 为 系统.注册表（）；

注册.注册（权限.建立系统（服务））；

注册.启动全部（{}）；

定 结果：列 为 注册.处理全部（{「动作」：「查询权限」，「实体编号」：「测试:角色/玩家」}）；

测试.相等（结果【0】【「系统编号」】，「言域:系统/权限」）；

测试.相等（包含（结果【0】【「结果」】，权限.游戏权限），真）；

法 未知动作（） 则
    注册.处理全部（{「动作」：「不存在」，「实体编号」：「测试:角色/玩家」}）；
终

应抛（未知动作，权限.系统动作错误）；

注册.停止全部（{}）；
