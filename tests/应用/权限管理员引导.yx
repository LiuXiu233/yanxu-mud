引「../../src/应用/权限服务.yx」为 权限；

引「../../src/持久化/内存仓储.yx」为 内存；

引「../../src/实体/实体.yx」为 实体；

引「标准:文字」为 文字；

引「标准:测试」为 测试；

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.断言（命中，「应抛出指定权限错误码」）；
终

定 仓储 为 内存.新仓储（）；

定 账户 为 实体.新实体（「测试:账户/首位」，「言域:原型/账户」）；

账户.添加组件（权限.权限组件编号，{「角色」：【权限.玩家角色组】，「授权」：【】}，1）；

账户.添加组件（权限.账户组件编号，{「账户编号」：「测试:账户/首位」，「角色」：【】}，1）；

仓储.写入实体快照（账户.快照（），空）；

定 服务 为 权限.权限服务（仓储，{}）；

定 引导：典 为 服务.引导管理员（「测试:账户/首位」，{「事务编号」：「tx-权限-引导」，「世界时间」：100，「现实时间」：100，「操作者编号」：「测试:账户/首位」}）；

测试.相等（引导【「成功」】，真）；

测试.相等（包含（引导【「权限」】，权限.权限管理权限），真）；

测试.相等（引导【「事件」】【0】【「事件种类」】，「言域:事件/管理员引导」）；

测试.相等（仓储.读取世界变量（权限.管理员引导变量）【「账户编号」】，「测试:账户/首位」）；

法 重复引导（） 则
    服务.引导管理员（「测试:账户/首位」，{「事务编号」：「tx-权限-重复引导」，「世界时间」：101，「操作者编号」：「测试:账户/首位」}）；
终

应抛（重复引导，权限.管理员引导错误）；
