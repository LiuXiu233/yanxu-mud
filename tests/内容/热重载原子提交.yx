引「../../src/内容/热重载.yx」为 重载；

引「../../src/内容/内容图.yx」为 内容图；

引「../../src/内容/内容包协议.yx」为 内容包；

引「../../src/内容/内容文档.yx」为 内容；

引「../../src/内容/Schema注册表.yx」为 Schema；

引「标准:测试」为 测试；

法 清单（）：典 则
    归 {「协议」：内容包.协议名称，「格式版本」：内容包.格式版本，「包编号」：「基础世界」，「包名称」：「基础世界」，「版本」：「1.0.0」，「引擎版本」：「>=0.3.0 <1.0.0」，「依赖」：【】，「可选依赖」：【】，「加载顺序」：0，「入口」：【「内容/世界.yj」】，「Schema」：【「Schema/通用.yj」】，「本地化」：{}，「迁移」：【】，「权限声明」：【】，「内容签名」：{}，「热重载」：真，「扩展」：{}}；
终

法 所Schema（）：典 则
    归 {「协议」：Schema.定义协议，「格式版本」：Schema.格式版本，「编号」：「言域:Schema/原子重载」，「版本」：1，「适用类别」：「房间」，「规则」：{「类型」：「据」，「必需」：【「名称」】，「允许额外」：真，「属性」：{「名称」：{「类型」：「文」}}}，「说明」：「原子重载测试」，「扩展」：{}}；
终

法 房间（标识：文，名称：文）：典 则
    归 {「协议」：内容.协议名称，「格式版本」：内容.格式版本，「包编号」：「基础世界」，「类别」：「房间」，「编号」：（「基础世界:房间/」 加 标识），「Schema编号」：「言域:Schema/原子重载」，「Schema版本」：1，「数据」：{「名称」：名称}，「原型」：空，「引用」：【】，「行为编号」：【】，「本地化键」：【】，「扩展」：{}}；
终

法 建图（名称：文） 则
    归 内容图.构建（「0.3.0」，【清单（）】，【所Schema（）】，【房间（「主厅」，名称）】，【】，「zh-CN」，【】，【】）；
终

法 时钟（）：数 则
    归 1000；
终

法 无阻止（影响）：列<文> 则
    归 【】；
终

令 外部事件：列 为 【】；

令 外部审计：列 为 【】；

法 收事件（所事件）：空 则
    追加（外部事件，所事件）；
    归 空；
终

法 收审计（所审计）：空 则
    追加（外部审计，所审计）；
    归 空；
终

定 旧图 为 建图（「旧主厅」）；

定 新图 为 建图（「新主厅」）；

定 运行 为 重载.运行时（旧图，时钟，无阻止，收事件，收审计）；

定 预检：典 为 运行.预检（新图）；

测试.相等（预检【「允许」】，真）；

测试.相等（预检【「基准代数」】，0）；

定 结果：典 为 运行.提交（新图，0）；

测试.相等（结果【「成功」】，真）；

测试.相等（结果【「已应用」】，真）；

测试.相等（运行.代数（），1）；

测试.相等（运行.当前摘要（），新图.摘要）；

测试.相等（运行.当前图（）.内容文档（「基础世界:房间/主厅」）【「数据」】【「名称」】，「新主厅」）；

测试.相等（长度（运行.事件记录（）），1）；

测试.相等（长度（运行.审计记录（）），1）；

测试.相等（长度（外部事件），1）；

测试.相等（长度（外部审计），1）；

测试.相等（运行.事件记录（）【0】【「事件种类」】，「言域:事件/内容热重载」）；

定 无变化：典 为 运行.提交（新图，1）；

测试.相等（无变化【「已应用」】，假）；

测试.相等（运行.代数（），1）；

定 冲突：典 为 运行.尝试提交（旧图，0）；

测试.相等（冲突【「成功」】，假）；

测试.相等（冲突【「错误」】【「代码」】，重载.提交冲突错误）；

测试.相等（运行.当前摘要（），新图.摘要）；

法 现场阻止（影响）：列<文> 则
    归 【「仍有房间实例引用候选原型」】；
终

定 被阻止运行 为 重载.运行时（旧图，时钟，现场阻止，空，空）；

定 被阻止计划：典 为 被阻止运行.预检（新图）；

测试.相等（被阻止计划【「允许」】，假）；

测试.相等（被阻止计划【「阻止原因」】【0】【「代码」】，重载.原因现场影响）；

定 被阻止结果：典 为 被阻止运行.尝试提交（新图，0）；

测试.相等（被阻止结果【「错误」】【「代码」】，重载.策略拒绝错误）；

测试.相等（被阻止运行.当前摘要（），旧图.摘要）；

测试.相等（被阻止运行.代数（），0）；

法 坏接收（所值）：空 则
    抛 「外部接收失败」；
终

定 通知失败运行 为 重载.运行时（旧图，时钟，无阻止，坏接收，坏接收）；

定 通知失败结果：典 为 通知失败运行.提交（新图，0）；

测试.相等（通知失败结果【「成功」】，真）；

测试.相等（长度（通知失败结果【「通知错误」】），2）；

测试.相等（通知失败运行.当前摘要（），新图.摘要）；
