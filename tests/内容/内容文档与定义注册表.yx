引「../../src/内容/内容文档.yx」为 内容；

引「../../src/内容/Schema注册表.yx」为 Schema；

引「../../src/实体/值支持.yx」为 值；

引「包:言据」为 言据；

引「标准:测试」为 测试；

引「标准:文字」为 文字；

法 房间Schema（版本：数，类别：文）：典 则
    归 {「协议」：Schema.定义协议，「格式版本」：Schema.格式版本，「编号」：「基础世界:Schema/房间」，「版本」：版本，「适用类别」：类别，「规则」：{「类型」：「据」，「必需」：【「名称」，「描述」】，「允许额外」：假，「属性」：{「名称」：{「类型」：「文」，「最短」：1}，「描述」：{「类型」：「文」}}}，「说明」：「房间内容」，「扩展」：{}}；
终

法 新房间（编号：文，名称：文）：典 则
    归 {「协议」：内容.协议名称，「格式版本」：内容.格式版本，「包编号」：「基础世界」，「类别」：「房间」，「编号」：编号，「Schema编号」：「基础世界:Schema/房间」，「Schema版本」：1，「数据」：{「名称」：名称，「描述」：「青石铺地」}，「原型」：空，「引用」：【】，「行为编号」：【「基础世界:行为/进入房间」】，「本地化键」：【「基础世界.房间.名称」】，「扩展」：{}}；
终

法 应抛（操作：法，代码：文）：空 则
    令 已抛：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 已抛 为 文字.始于（所误.消息，代码）；
    终
    测试.断言（已抛，「非法内容文档操作必须返回稳定错误码」）；
终

定 Schema表 为 Schema.注册表（）；

Schema表.注册（房间Schema（1，「房间」））；

定 注册 为 内容.定义注册表（Schema表）；

定 南门：典 为 新房间（「基础世界:房间/南门」，「青石镇南门」）；

追加（南门【「引用」】，{「路径」：「$.出口.北」，「目标」：「基础世界:房间/中心街」，「类别」：「房间」，「可选」：假}）；

注册.注册（南门）；

置 南门【「数据」】【「名称」】 为 「已篡改」；

测试.相等（注册.查询（「基础世界:房间/南门」）【「数据」】【「名称」】，「青石镇南门」）；

定 查询副本：典 为 注册.查询（「基础世界:房间/南门」）；

置 查询副本【「扩展」】【「测试」】 为 真；

测试.相等（长度（注册.查询（「基础世界:房间/南门」）【「扩展」】），0）；

测试.相等（内容.内容编号各段（「基础世界:房间/中心街」）【「标识」】，「中心街」）；

测试.相等（内容.内容编号有效（「基础世界:房间/中心街」），真）；

测试.相等（内容.内容编号有效（「基础世界:房间/二层/阁楼」），假）；

定 自文字：典 为 内容.自文字（言据.序列化（新房间（「基础世界:房间/客栈」，「来福客栈」）））；

测试.相等（自文字【「数据」】【「名称」】，「来福客栈」）；

注册.批量注册（【新房间（「基础世界:房间/中心街」，「中心街」），新房间（「基础世界:房间/北门」，「北门」）】）；

定 已排序编号：列<文> 为 注册.编号列（）；

测试.相等（长度（已排序编号），3）；

测试.相等（已排序编号【0】，「基础世界:房间/中心街」）；

测试.相等（已排序编号【1】，「基础世界:房间/北门」）；

测试.相等（已排序编号【2】，「基础世界:房间/南门」）；

测试.相等（长度（注册.包内编号列（「基础世界」）），3）；

测试.相等（长度（注册.类别编号列（「房间」）），3）；

定 新南门：典 为 新房间（「基础世界:房间/南门」，「重修南门」）；

注册.替换（新南门）；

测试.相等（注册.查询（「基础世界:房间/南门」）【「数据」】【「名称」】，「重修南门」）；

法 重复注册（） 则
    注册.注册（新房间（「基础世界:房间/南门」，「重复」））；
终

应抛（重复注册，内容.定义重复错误）；

法 替换缺失（） 则
    注册.替换（新房间（「基础世界:房间/不存在」，「不存在」））；
终

应抛（替换缺失，内容.定义不存在错误）；

法 查询缺失（） 则
    注册.查询（「基础世界:房间/不存在」）；
终

应抛（查询缺失，内容.定义不存在错误）；

法 包编号不一致（） 则
    定 文档：典 为 新房间（「其他世界:房间/越界」，「越界」）；
    内容.规范文档（文档）；
终

应抛（包编号不一致，内容.文档无效错误）；

法 类别不一致（） 则
    定 文档：典 为 新房间（「基础世界:物品/伪装房间」，「伪装」）；
    内容.规范文档（文档）；
终

应抛（类别不一致，内容.文档无效错误）；

法 原型类别不一致（） 则
    定 文档：典 为 新房间（「基础世界:房间/坏原型」，「坏原型」）；
    置 文档【「原型」】 为 「基础世界:物品/模板」；
    内容.规范文档（文档）；
终

应抛（原型类别不一致，内容.文档无效错误）；

法 自继承（） 则
    定 文档：典 为 新房间（「基础世界:房间/自继承」，「自继承」）；
    置 文档【「原型」】 为 「基础世界:房间/自继承」；
    内容.规范文档（文档）；
终

应抛（自继承，内容.文档无效错误）；

法 引用类别不一致（） 则
    定 文档：典 为 新房间（「基础世界:房间/坏引用」，「坏引用」）；
    追加（文档【「引用」】，{「路径」：「$.出口.北」，「目标」：「基础世界:物品/剑」，「类别」：「房间」，「可选」：假}）；
    内容.规范文档（文档）；
终

应抛（引用类别不一致，内容.文档无效错误）；

法 重复引用路径（） 则
    定 文档：典 为 新房间（「基础世界:房间/重复引用」，「重复引用」）；
    追加（文档【「引用」】，{「路径」：「$.出口.北」，「目标」：「基础世界:房间/甲」，「类别」：「房间」，「可选」：假}）；
    追加（文档【「引用」】，{「路径」：「$.出口.北」，「目标」：「基础世界:房间/乙」，「类别」：「房间」，「可选」：真}）；
    内容.规范文档（文档）；
终

应抛（重复引用路径，内容.文档无效错误）；

法 无效引用路径（） 则
    定 文档：典 为 新房间（「基础世界:房间/坏路径」，「坏路径」）；
    追加（文档【「引用」】，{「路径」：「出口..北」，「目标」：「基础世界:房间/甲」，「类别」：「房间」，「可选」：假}）；
    内容.规范文档（文档）；
终

应抛（无效引用路径，内容.文档无效错误）；

法 重复行为（） 则
    定 文档：典 为 新房间（「基础世界:房间/重复行为」，「重复行为」）；
    追加（文档【「行为编号」】，「基础世界:行为/进入房间」）；
    内容.规范文档（文档）；
终

应抛（重复行为，内容.文档无效错误）；

法 无效本地化键（） 则
    定 文档：典 为 新房间（「基础世界:房间/坏键」，「坏键」）；
    置 文档【「本地化键」】 为 【「基础世界..名称」】；
    内容.规范文档（文档）；
终

应抛（无效本地化键，内容.文档无效错误）；

法 Schema版本不符（） 则
    定 文档：典 为 新房间（「基础世界:房间/旧Schema」，「旧Schema」）；
    置 文档【「Schema版本」】 为 2；
    注册.注册（文档）；
终

应抛（Schema版本不符，内容.Schema不匹配错误）；

法 Schema类别不符（） 则
    定 另一Schema表 为 Schema.注册表（）；
    另一Schema表.注册（房间Schema（1，「物品」））；
    定 另一注册 为 内容.定义注册表（另一Schema表）；
    另一注册.注册（新房间（「基础世界:房间/错Schema」，「错Schema」））；
终

应抛（Schema类别不符，内容.Schema不匹配错误）；

法 数据不符Schema（） 则
    定 文档：典 为 新房间（「基础世界:房间/坏数据」，「坏数据」）；
    置 文档【「数据」】 为 {「名称」：「坏数据」}；
    注册.注册（文档）；
终

应抛（数据不符Schema，Schema.数据无效错误）；

法 非普通文档（） 则
    法 回调（） 则
        归 空；
    终
    定 文档：典 为 新房间（「基础世界:房间/带代码」，「带代码」）；
    置 文档【「数据」】【「回调」】 为 回调；
    内容.规范文档（文档）；
终

应抛（非普通文档，内容.文档无效错误）；

法 批量原子失败（） 则
    注册.批量注册（【新房间（「基础世界:房间/批量甲」，「批量甲」），新房间（「基础世界:房间/批量甲」，「重复批量甲」）】）；
终

应抛（批量原子失败，内容.定义重复错误）；

测试.相等（注册.已注册（「基础世界:房间/批量甲」），假）；

法 额外字段（） 则
    定 文档：典 为 新房间（「基础世界:房间/额外字段」，「额外字段」）；
    置 文档【「拼写错误」】 为 真；
    内容.规范文档（文档）；
终

应抛（额外字段，内容.文档无效错误）；

定 尝试结果：典 为 内容.尝试自文字（「不是合法言据」）；

测试.相等（尝试结果【「成功」】，假）；

测试.相等（尝试结果【「错误」】【「代码」】，内容.文档无效错误）；

测试.相等（值.有键（尝试结果【「错误」】，「消息」），假）；
