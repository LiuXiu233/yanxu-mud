引「../../src/内容/语义版本.yx」为 版本；

引「标准:测试」为 测试；

引「标准:文字」为 文字；

法 应抛（操作：法，代码：文）：空 则
    令 已抛：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 已抛 为 文字.始于（所误.消息，代码）；
    终
    测试.断言（已抛，「无效版本或约束必须返回稳定错误码」）；
终

定 正常：典 为 版本.解析（「1.2.3-alpha.1+build.7」）；

测试.相等（正常【「主」】，1）；

测试.相等（正常【「次」】，2）；

测试.相等（正常【「修」】，3）；

测试.相等（正常【「预发布」】【0】，「alpha」）；

测试.相等（正常【「预发布」】【1】，「1」）；

测试.相等（正常【「构建」】【0】，「build」）；

测试.相等（版本.比较（「1.0.0-alpha」，「1.0.0-alpha.1」），减 1）；

测试.相等（版本.比较（「1.0.0-alpha.1」，「1.0.0-alpha.beta」），减 1）；

测试.相等（版本.比较（「1.0.0-beta.11」，「1.0.0-rc.1」），减 1）；

测试.相等（版本.比较（「1.0.0-rc.1」，「1.0.0」），减 1）；

测试.相等（版本.比较（「1.0.0+build.1」，「1.0.0+build.2」），0）；

测试.相等（版本.符合约束（「1.4.2」，「>=1.2.0 <2.0.0」），真）；

测试.相等（版本.符合约束（「2.0.0」，「>=1.2.0 <2.0.0」），假）；

测试.相等（版本.符合约束（「1.9.9」，「^1.2.3」），真）；

测试.相等（版本.符合约束（「2.0.0」，「^1.2.3」），假）；

测试.相等（版本.符合约束（「0.2.9」，「^0.2.3」），真）；

测试.相等（版本.符合约束（「0.3.0」，「^0.2.3」），假）；

测试.相等（版本.符合约束（「1.2.9」，「~1.2.3」），真）；

测试.相等（版本.符合约束（「1.3.0」，「~1.2.3」），假）；

测试.相等（版本.符合约束（「99.0.0」，「*」），真）；

法 前导零（） 则
    版本.解析（「01.2.3」）；
终

法 缺少修订（） 则
    版本.解析（「1.2」）；
终

法 数字预发布前导零（） 则
    版本.解析（「1.2.3-01」）；
终

法 中文预发布（） 则
    版本.解析（「1.2.3-预览」）；
终

法 多个构建分隔（） 则
    版本.解析（「1.2.3+a+b」）；
终

法 空比较器（） 则
    版本.解析约束（「>=」）；
终

法 无效约束版本（） 则
    版本.解析约束（「^1.2」）；
终

应抛（前导零，版本.版本无效错误）；

应抛（缺少修订，版本.版本无效错误）；

应抛（数字预发布前导零，版本.版本无效错误）；

应抛（中文预发布，版本.版本无效错误）；

应抛（多个构建分隔，版本.版本无效错误）；

应抛（空比较器，版本.约束无效错误）；

应抛（无效约束版本，版本.约束无效错误）；
