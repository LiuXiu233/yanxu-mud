引「../../src/内容/Schema注册表.yx」为 Schema；

引「../../src/实体/值支持.yx」为 值；

引「包:言据」为 言据；

引「标准:测试」为 测试；

引「标准:文字」为 文字；

法 房间定义（版本：数）：典 则
    归 {「协议」：Schema.定义协议，「格式版本」：Schema.格式版本，「编号」：「言域:Schema/房间」，「版本」：版本，「适用类别」：「房间」，「规则」：{「类型」：「据」，「必需」：【「名称」，「出口」】，「允许额外」：假，「属性」：{「名称」：{「类型」：「文」，「最短」：1，「最长」：80}，「出口」：{「类型」：「列」，「最长」：16，「项目」：{「类型」：「文」}}}}，「说明」：「房间静态定义」，「扩展」：{}}；
终

法 应抛（操作：法，代码：文）：空 则
    令 已抛：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 已抛 为 文字.始于（所误.消息，代码）；
    终
    测试.断言（已抛，「非法 Schema 操作必须返回稳定错误码」）；
终

定 注册 为 Schema.注册表（）；

定 原定义：典 为 房间定义（1）；

注册.注册（原定义）；

置 原定义【「规则」】【「属性」】【「名称」】【「最长」】 为 1；

测试.相等（注册.查询（「言域:Schema/房间」）【「规则」】【「属性」】【「名称」】【「最长」】，80）；

定 查询副本：典 为 注册.查询（「言域:Schema/房间」）；

置 查询副本【「说明」】 为 「篡改」；

测试.相等（注册.查询（「言域:Schema/房间」）【「说明」】，「房间静态定义」）；

定 合法房间：典 为 {「名称」：「长安南门」，「出口」：【「北」】}；

测试.相等（注册.验证（「言域:Schema/房间」，合法房间），真）；

定 非法房间：典 为 {「出口」：【】，「拼写错误」：真}；

定 各错：列 为 注册.检查（「言域:Schema/房间」，非法房间）；

测试.相等（长度（各错），2）；

测试.相等（值.有键（各错【0】，「实际」），假）；

法 断验失败（） 则
    注册.断验（「言域:Schema/房间」，非法房间）；
终

应抛（断验失败，Schema.数据无效错误）；

法 重复注册（） 则
    注册.注册（房间定义（2））；
终

应抛（重复注册，Schema.定义重复错误）；

法 查询缺失（） 则
    注册.查询（「言域:Schema/物品」）；
终

应抛（查询缺失，Schema.定义不存在错误）；

法 低版本替换（） 则
    注册.替换（房间定义（1））；
终

应抛（低版本替换，Schema.定义无效错误）；

注册.替换（房间定义（2））；

测试.相等（注册.查询（「言域:Schema/房间」）【「版本」】，2）；

定 文字定义：典 为 Schema.定义自文字（言据.序列化（房间定义（3）））；

测试.相等（文字定义【「版本」】，3）；

法 未知规则字段（） 则
    定 定义：典 为 房间定义（1）；
    置 定义【「规则」】【「未知」】 为 真；
    Schema.规范定义（定义）；
终

应抛（未知规则字段，Schema.定义无效错误）；

法 嵌套未知类型（） 则
    定 定义：典 为 房间定义（1）；
    置 定义【「规则」】【「属性」】【「名称」】【「类型」】 为 「文字」；
    Schema.规范定义（定义）；
终

应抛（嵌套未知类型，Schema.定义无效错误）；

法 必需缺属性（） 则
    定 定义：典 为 房间定义（1）；
    追加（定义【「规则」】【「必需」】，「描述」）；
    Schema.规范定义（定义）；
终

应抛（必需缺属性，Schema.定义无效错误）；

法 长度边界倒置（） 则
    定 定义：典 为 房间定义（1）；
    置 定义【「规则」】【「属性」】【「名称」】【「最短」】 为 100；
    Schema.规范定义（定义）；
终

应抛（长度边界倒置，Schema.定义无效错误）；

法 非普通定义（） 则
    法 回调（） 则
        归 空；
    终
    定 定义：典 为 房间定义（1）；
    置 定义【「扩展」】【「回调」】 为 回调；
    Schema.规范定义（定义）；
终

应抛（非普通定义，Schema.定义无效错误）；

法 非普通数据（） 则
    法 回调（） 则
        归 空；
    终
    注册.检查（「言域:Schema/房间」，{「名称」：「坏」，「出口」：【】，「回调」：回调}）；
终

应抛（非普通数据，Schema.数据无效错误）；
