引「../../src/内容/文件加载器.yx」为 加载器；

引「../../src/内容/内容包协议.yx」为 内容包；

引「../../src/实体/值支持.yx」为 值；

引「标准:测试」为 测试；

法 错误码（各根：列）：文 则
    定 结果：典 为 加载器.尝试加载世界（各根，「0.3.0」）；
    测试.相等（结果【「成功」】，假）；
    测试.相等（值.有键（结果【「错误」】，「消息」），假）；
    归 结果【「错误」】【「代码」】；
终

定 固件根：文 为 「tests/fixtures/内容包/」；

测试.相等（错误码（【】），加载器.根目录无效错误）；

令 过多根：列 为 【】；

令 次：数 为 0；

当 （次 小于 257） 则
    追加（过多根，「不会读取」）；
    置 次 为 （次 加 1）；
终

测试.相等（错误码（过多根），加载器.根目录无效错误）；

测试.相等（错误码（【「README.md」】），加载器.根目录无效错误）；

定 基础根：文 为 （固件根 加 「基础世界」）；

测试.相等（错误码（【基础根，基础根】），加载器.根目录无效错误）；

测试.相等（错误码（【（固件根 加 「路径冲突」）】），加载器.路径冲突错误）；

测试.相等（错误码（【（固件根 加 「缺失文件」）】），加载器.文件缺失错误）；

测试.相等（错误码（【（固件根 加 「目录文件」）】），加载器.文件类型错误）；

测试.相等（错误码（【（固件根 加 「依赖先决」）】），内容包.依赖缺失错误）；

测试.相等（错误码（【（固件根 加 「越界路径」）】），内容包.清单无效错误）；
