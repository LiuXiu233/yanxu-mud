引「../../src/内容/文件加载器.yx」为 加载器；

引「../../src/实体/值支持.yx」为 值；

引「标准:测试」为 测试；

法 错误码（根：文）：文 则
    定 结果：典 为 加载器.尝试加载包（根）；
    测试.相等（结果【「成功」】，假）；
    测试.相等（值.有键（结果【「错误」】，「消息」），假）；
    归 结果【「错误」】【「代码」】；
终

定 固件根：文 为 「tests/fixtures/内容包/」；

测试.相等（错误码（（固件根 加 「解析错误」）），加载器.文件解析错误）；

测试.相等（错误码（（固件根 加 「形状错误」）），加载器.文件形状错误）；

测试.相等（错误码（（固件根 加 「归属错误」）），加载器.包归属错误）；

测试.相等（错误码（（固件根 加 「语言错配」）），加载器.本地化语言错误）；

测试.相等（错误码（（固件根 加 「Schema归属错误」）），加载器.包归属错误）；
