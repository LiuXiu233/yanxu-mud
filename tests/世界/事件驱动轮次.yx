引「../../src/世界/世界循环.yx」为 世界；

引「../../src/测试支持/虚拟时钟.yx」为 时间；

引「标准:测试」为 测试；

令 顺序：列<文> 为 【】；

令 收到：列 为 【】；

法 处理输入（输入） 则
    追加（顺序，「输入」）；
    归 {「命令」：【{「编号」：「命令一」}】}；
终

法 处理任务（任务） 则
    追加（顺序，「任务」）；
    归 {「命令」：【{「编号」：「命令二」}】}；
终

法 处理命令（命令） 则
    追加（顺序，「命令」）；
    归 {「事务」：【{「编号」：命令【「编号」】}】}；
终

法 处理事务（事务） 则
    追加（顺序，「事务」）；
    归 {「事件」：【{「编号」：事务【「编号」】}】，「持久化」：【事务】}；
终

法 处理事件（事件） 则
    追加（顺序，「事件」）；
    归 {「消息」：【{「种类」：「文字」，「内容」：事件【「编号」】}】}；
终

法 处理持久化（变更） 则
    追加（顺序，「持久化」）；
    归 空；
终

法 处理输出（消息） 则
    追加（顺序，「输出」）；
    追加（收到，消息）；
    归 空；
终

定 时钟 为 时间.虚拟时钟（1000）；

定 调度 为 时间.调度器（）；

调度.计划（「公告」，1000，0，{「文字」：「到期」}）；

定 循环 为 世界.世界循环（时钟，调度，处理输入，处理任务，处理命令，处理事务，处理事件，处理持久化，处理输出）；

测试.相等（循环.是否有工作（），真）；

循环.接收输入（{「连接」：「本地」，「文字」：「观察」}）；

定 结果：典 为 循环.轮转（世界.默认预算（））；

测试.相等（结果【「计数」】【「输入」】，1）；

测试.相等（结果【「计数」】【「任务」】，1）；

测试.相等（结果【「计数」】【「命令」】，2）；

测试.相等（结果【「计数」】【「事务」】，2）；

测试.相等（结果【「计数」】【「事件」】，2）；

测试.相等（结果【「计数」】【「持久化」】，2）；

测试.相等（结果【「计数」】【「输出」】，2）；

测试.相等（长度（结果【「错误」】），0）；

测试.相等（长度（收到），2）；

测试.相等（顺序【0】，「输入」）；

测试.相等（顺序【1】，「任务」）；

测试.相等（顺序【2】，「命令」）；

测试.相等（顺序【4】，「事务」）；

测试.相等（顺序【6】，「事件」）；

测试.相等（顺序【8】，「持久化」）；

测试.相等（顺序【10】，「输出」）；

测试.相等（结果【「仍有工作」】，假）；

测试.相等（结果【「下一唤醒」】，空）；
