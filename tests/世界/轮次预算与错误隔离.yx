引「../../src/世界/世界循环.yx」为 世界；

引「../../src/测试支持/虚拟时钟.yx」为 时间；

引「标准:测试」为 测试；

法 输入转命令（输入） 则
    归 {「命令」：【输入】}；
终

法 命令失败（命令） 则
    若 （命令【「编号」】 等于 「坏」） 则
        抛 「EXPECTED_COMMAND_FAILURE」；
    终
    归 {「消息」：【{「种类」：「文字」，「内容」：命令【「编号」】}】}；
终

法 空处理（值） 则
    归 空；
终

定 时钟 为 时间.虚拟时钟（10）；

定 调度 为 时间.调度器（）；

定 循环 为 世界.世界循环（时钟，调度，输入转命令，空处理，命令失败，空处理，空处理，空处理，空处理）；

循环.接收输入（{「编号」：「好」}）；

循环.接收输入（{「编号」：「坏」}）；

定 小预算：典 为 {「输入」：1，「任务」：0，「命令」：1，「事务」：0，「事件」：0，「持久化」：0，「输出」：0}；

定 首轮：典 为 循环.轮转（小预算）；

测试.相等（首轮【「仍有工作」】，真）；

测试.相等（循环.队列状态（）【「输入」】，1）；

测试.相等（循环.队列状态（）【「输出」】，1）；

定 次轮：典 为 循环.轮转（世界.默认预算（））；

测试.相等（长度（次轮【「错误」】），1）；

测试.相等（次轮【「错误」】【0】【「阶段」】，「命令」）；

测试.相等（次轮【「计数」】【「输出」】，1）；

测试.相等（次轮【「仍有工作」】，假）；
