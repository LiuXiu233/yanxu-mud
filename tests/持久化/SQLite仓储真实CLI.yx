引「../../src/持久化/SQLite仓储.yx」为 SQLite仓储；

引「../../src/持久化/仓储协议.yx」为 协议；

引「标准:文件」为 文件；

引「标准:环境」为 环境；

引「标准:路径」为 路径；

引「标准:测试」为 测试；

引「标准:文字」为 文字；

法 应抛冲突（操作：法）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，协议.事务冲突错误）；
    终
    测试.断言（命中，「真实SQLite并发写入必须返回事务冲突」）；
终

定 程序值 为 环境.读取（「YANYU_SQLITE3」）；

若 （程序值 是 空） 则
    抛 「YANYU_SQLITE_INTEGRATION_REQUIRED：必须通过YANYU_SQLITE3指定SQLite 3.38+ CLI」；
终

定 程序：文 为 程序值；

定 根：文 为 「tests/.SQLite仓储真实CLI」；

若 文件.存在（根） 则
    文件.删除（根，真）；
终

文件.创建目录（根）；

定 数据库路径：文 为 路径.合并（根，「仓储.sqlite3」）；

定 仓储 为 SQLite仓储.打开为（数据库路径，程序，30000）；

仓储.设置世界变量（「真实.值」，{「中文」：「甲'乙」，「列表」：【1，2，3】}）；

定 快照：典 为 仓储.创建快照（「真实快照」，{「来源」：「sqlite3-cli」}）；

仓储.关闭（）；

测试.相等（文件.状态（数据库路径）【「种类」】，「文件」）；

定 写者甲 为 SQLite仓储.打开为（数据库路径，程序，30000）；

定 写者乙 为 SQLite仓储.打开为（数据库路径，程序，30000）；

测试.相等（写者甲.读取世界变量（「真实.值」）【「中文」】，「甲'乙」）；

测试.相等（写者甲.读取快照（「真实快照」）【「校验和」】，快照【「校验和」】）；

定 过期事务 为 写者甲.开始事务（）；

过期事务.设置世界变量（「并发.甲」，1）；

写者乙.设置世界变量（「并发.乙」，2）；

法 提交过期事务（） 则
    过期事务.提交（）；
终

应抛冲突（提交过期事务）；

写者甲.关闭（）；

写者乙.关闭（）；

定 已重开 为 SQLite仓储.打开为（数据库路径，程序，30000）；

测试.相等（已重开.读取世界变量（「并发.甲」），空）；

测试.相等（已重开.读取世界变量（「并发.乙」），2）；

测试.断言（（已重开.恢复报告（）【「SQLite版本」】 是 文），「恢复报告必须记录真实SQLite版本」）；

已重开.关闭（）；

文件.删除（根，真）；
