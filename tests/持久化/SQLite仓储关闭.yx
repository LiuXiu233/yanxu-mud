引「../../src/持久化/SQLite仓储.yx」为 SQLite仓储；

引「../../src/测试支持/SQLite内存执行器.yx」为 支持；

引「标准:测试」为 测试；

引「标准:文字」为 文字；

法 应抛已关闭（操作：法）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，SQLite仓储.已关闭错误）；
    终
    测试.断言（命中，「关闭后的SQLite仓储必须拒绝读取」）；
终

定 执行器 为 支持.新执行器（）；

定 仓储 为 SQLite仓储.以执行器（执行器.执行请求）；

仓储.关闭（）；

仓储.关闭（）；

测试.相等（仓储.是否关闭（），真）；

法 关闭后读取（） 则
    仓储.读取世界变量（「不存在」）；
终

应抛已关闭（关闭后读取）；
