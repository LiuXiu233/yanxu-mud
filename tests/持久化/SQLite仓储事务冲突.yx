引「../../src/持久化/SQLite仓储.yx」为 SQLite仓储；

引「../../src/持久化/仓储协议.yx」为 协议；

引「../../src/测试支持/SQLite内存执行器.yx」为 支持；

引「标准:测试」为 测试；

引「标准:文字」为 文字；

法 应抛冲突（操作：法）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，协议.事务冲突错误）；
    终
    测试.断言（命中，「并发SQLite写入必须返回仓储事务冲突」）；
终

定 执行器 为 支持.新执行器（）；

定 写者甲 为 SQLite仓储.以执行器（执行器.执行请求）；

定 写者乙 为 SQLite仓储.以执行器（执行器.执行请求）；

定 过期事务 为 写者甲.开始事务（）；

过期事务.设置世界变量（「竞争.甲」，1）；

写者乙.设置世界变量（「竞争.乙」，2）；

法 提交过期事务（） 则
    过期事务.提交（）；
终

应抛冲突（提交过期事务）；

测试.相等（过期事务.状态（），「已失败」）；

定 已重开 为 SQLite仓储.以执行器（执行器.执行请求）；

测试.相等（已重开.读取世界变量（「竞争.甲」），空）；

测试.相等（已重开.读取世界变量（「竞争.乙」），2）；
