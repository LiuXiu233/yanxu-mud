引「../../src/持久化/数据迁移.yx」为 迁移；

引「../../src/持久化/SQLite仓储.yx」为 SQLite仓储；

引「../../src/测试支持/SQLite内存执行器.yx」为 支持；

引「标准:测试」为 测试；

法 应用迁移（事务，上下文：典）：典 则
    事务.设置世界变量（「迁移.SQLite」，上下文【「值」】）；
    归 {「后端」：「SQLite」}；
终

定 执行器 为 支持.新执行器（）；

定 定义 为 迁移.简单迁移（「测试:迁移/SQLite」，「1.0.0」，「验证SQLite迁移持久化」，应用迁移）；

定 仓储 为 SQLite仓储.以执行器（执行器.执行请求）；

迁移.执行（仓储，【定义】，{「值」：「已完成」}）；

定 已重开 为 SQLite仓储.以执行器（执行器.执行请求）；

测试.相等（已重开.读取世界变量（「迁移.SQLite」），「已完成」）；

测试.相等（长度（迁移.计划（已重开，【定义】）），0）；
