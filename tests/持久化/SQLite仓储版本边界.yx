引「../../src/持久化/SQLite仓储.yx」为 SQLite仓储；

引「../../src/测试支持/SQLite内存执行器.yx」为 支持；

引「标准:测试」为 测试；

引「标准:文字」为 文字；

法 应抛结构（操作：法）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，SQLite仓储.结构错误）；
    终
    测试.断言（命中，「不受支持的SQLite版本必须返回结构错误」）；
终

定 执行器 为 支持.新执行器（）；

执行器.设置SQLite版本（「3.37.2」）；

法 打开旧SQLite（） 则
    SQLite仓储.以执行器（执行器.执行请求）；
终

应抛结构（打开旧SQLite）；
