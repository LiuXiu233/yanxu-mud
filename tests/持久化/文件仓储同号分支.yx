引「../../src/持久化/文件仓储.yx」为 文件仓储；

引「包:言据」为 言据；

引「标准:文件」为 文件；

引「标准:哈希」为 哈希；

引「标准:JSON」为 JSON；

引「标准:路径」为 路径；

引「标准:测试」为 测试；

引「标准:文字」为 文字；

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.断言（命中，「同号有效分支必须返回稳定错误码」）；
终

法 世代本体（世代：典）：典 则
    归 {「协议」：世代【「协议」】，「格式版本」：世代【「格式版本」】，「世代」：世代【「世代」】，「父校验和」：世代【「父校验和」】，「状态」：世代【「状态」】，「快照」：世代【「快照」】，「快照序号」：世代【「快照序号」】，「事件日志」：世代【「事件日志」】，「扩展」：世代【「扩展」】}；
终

法 世代文件名（世代：典）：文 则
    定 世代号：文 为 文字.替换（JSON.序列化（世代【「世代」】），「.0」，「」）；
    归 （（（（「世代-」 加 世代号） 加 「-」） 加 世代【「校验和」】） 加 「.yj」）；
终

定 根：文 为 「tests/.文件仓储同号分支」；

若 文件.存在（根） 则
    文件.删除（根，真）；
终

定 仓储 为 文件仓储.打开（根）；

仓储.设置世界变量（「分支」，「甲」）；

定 分支：典 为 言据.读取据（路径.合并（根，仓储.当前文件（）））；

置 分支【「扩展」】【「分支」】 为 「乙」；

置 分支【「校验和」】 为 哈希.SHA256（JSON.序列化（世代本体（分支）））；

言据.美写（路径.合并（根，世代文件名（分支）），分支）；

法 打开同号分支（） 则
    文件仓储.打开（根）；
终

应抛（打开同号分支，文件仓储.恢复歧义错误）；

文件.删除（根，真）；
