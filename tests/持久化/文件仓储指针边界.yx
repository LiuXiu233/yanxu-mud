引「../../src/持久化/文件仓储.yx」为 文件仓储；

引「包:言据」为 言据；

引「标准:文件」为 文件；

引「标准:哈希」为 哈希；

引「标准:JSON」为 JSON；

引「标准:路径」为 路径；

引「标准:测试」为 测试；

引「标准:文字」为 文字；

法 应抛损坏（指针：典）：空 则
    令 命中：理 为 假；
    试 则
        文件仓储.验证指针（指针）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，文件仓储.损坏错误）；
    终
    测试.断言（命中，「越界指针文件名必须在读取目标前被拒绝」）；
终

法 指针本体（指针：典）：典 则
    归 {「协议」：指针【「协议」】，「格式版本」：指针【「格式版本」】，「世代」：指针【「世代」】，「文件」：指针【「文件」】，「世代校验和」：指针【「世代校验和」】}；
终

定 根：文 为 「tests/.文件仓储指针边界」；

若 文件.存在（根） 则
    文件.删除（根，真）；
终

定 仓储 为 文件仓储.打开（根）；

仓储.设置世界变量（「已提交」，真）；

定 指针路径：文 为 路径.合并（根，文件仓储.指针文件名）；

定 越界指针：典 为 言据.读取据（指针路径）；

置 越界指针【「文件」】 为 （「../」 加 仓储.当前文件（））；

置 越界指针【「校验和」】 为 哈希.SHA256（JSON.序列化（指针本体（越界指针）））；

应抛损坏（越界指针）；

言据.美写（指针路径，越界指针）；

定 已恢复 为 文件仓储.打开（根）；

测试.相等（已恢复.恢复报告（）【「指针已修复」】，真）；

测试.相等（已恢复.读取世界变量（「已提交」），真）；

测试.断言（（文件仓储.指针文件字节上限 小于 文件仓储.世代文件字节上限），「指针必须使用更小的独立读取上限」）；

文件.删除（根，真）；
