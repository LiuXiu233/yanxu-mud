引「../../src/持久化/文件仓储.yx」为 文件仓储；

引「标准:文件」为 文件；

引「标准:路径」为 路径；

引「标准:测试」为 测试；

引「标准:文字」为 文字；

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.断言（命中，「缺失父世代必须返回稳定错误码」）；
终

定 根：文 为 「tests/.文件仓储缺少父世代」；

若 文件.存在（根） 则
    文件.删除（根，真）；
终

定 仓储 为 文件仓储.打开（根）；

仓储.设置世界变量（「第一步」，1）；

定 中间文件：文 为 仓储.当前文件（）；

仓储.设置世界变量（「第二步」，2）；

文件.删除（路径.合并（根，中间文件），假）；

法 打开缺少父世代（） 则
    文件仓储.打开（根）；
终

应抛（打开缺少父世代，文件仓储.损坏错误）；

文件.删除（根，真）；
