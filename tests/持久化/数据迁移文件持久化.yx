引「../../src/持久化/数据迁移.yx」为 迁移；

引「../../src/持久化/文件仓储.yx」为 文件仓储；

引「标准:文件」为 文件；

引「标准:测试」为 测试；

法 应用迁移（事务，上下文：典）：典 则
    事务.设置世界变量（「迁移.文件」，上下文【「值」】）；
    归 {「后端」：「文件」}；
终

定 根：文 为 「tests/.数据迁移文件持久化」；

若 文件.存在（根） 则
    文件.删除（根，真）；
终

定 定义 为 迁移.简单迁移（「测试:迁移/文件」，「1.0.0」，「验证文件迁移持久化」，应用迁移）；

定 仓储 为 文件仓储.打开（根）；

迁移.执行（仓储，【定义】，{「值」：「已完成」}）；

定 已重开 为 文件仓储.打开（根）；

测试.相等（已重开.读取世界变量（「迁移.文件」），「已完成」）；

测试.相等（长度（迁移.计划（已重开，【定义】）），0）；

文件.删除（根，真）；
