引「../../src/持久化/SQLite仓储.yx」为 SQLite仓储；

引「../../src/测试支持/SQLite内存执行器.yx」为 支持；

引「标准:测试」为 测试；

定 执行器 为 支持.新执行器（）；

定 仓储 为 SQLite仓储.以执行器（执行器.执行请求）；

仓储.设置世界变量（「阶段」，「快照前」）；

定 快照：典 为 仓储.创建快照（「基线」，{「原因」：「恢复测试」}）；

仓储.设置世界变量（「阶段」，「快照后」）；

仓储.恢复快照（「基线」）；

测试.相等（仓储.世代（），4）；

测试.相等（仓储.读取世界变量（「阶段」），「快照前」）；

定 已重开 为 SQLite仓储.以执行器（执行器.执行请求）；

测试.相等（已重开.读取快照（「基线」）【「校验和」】，快照【「校验和」】）；

测试.相等（已重开.读取世界变量（「阶段」），「快照前」）；

测试.相等（长度（已重开.各快照（）），1）；
