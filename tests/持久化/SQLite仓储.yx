引「../../src/持久化/SQLite仓储.yx」为 SQLite仓储；

引「../../src/测试支持/SQLite内存执行器.yx」为 支持；

引「../../src/事件/事件总线.yx」为 事件；

引「../../src/实体/实体.yx」为 实体；

引「标准:测试」为 测试；

引「标准:文字」为 文字；

定 执行器 为 支持.新执行器（）；

定 仓储 为 SQLite仓储.以执行器（执行器.执行请求）；

测试.相等（仓储.世代（），0）；

测试.相等（仓储.恢复报告（）【「数据库结构版本」】，1）；

定 事务 为 仓储.开始事务（）；

定 角色 为 实体.新实体（「测试:角色/SQLite甲」，「测试:原型/玩家」）；

角色.添加标签（「玩家」）；

事务.写入实体快照（角色.快照（），空）；

事务.设置世界变量（「天气」，{「状态」：「晴」}）；

事务.保存计划任务（{「编号」：「刷新一」，「到期」：1000，「间隔」：60，「载荷」：{「目标」：「测试:房间/广场」}}，空）；

事务.记录迁移（「迁移一」，「1.0.0」，{「说明」：「初始化」}）；

定 事实：典 为 事件.新事件（「SQLite事件一」，事件.类别事实，「测试:事件/状态提交」，1000，1000，「测试:角色/SQLite甲」，「测试:世界/主世界」，{「状态」：「已提交」}，「命令一」，「事务一」，「追踪一」，「0.4.0」）；

事务.追加事件（事实）；

事务.提交（）；

测试.相等（仓储.世代（），1）；

测试.相等（执行器.世代数（），2）；

定 已重开 为 SQLite仓储.以执行器（执行器.执行请求）；

测试.相等（已重开.读取实体（「测试:角色/SQLite甲」）.编号，「测试:角色/SQLite甲」）；

测试.相等（已重开.读取世界变量（「天气」）【「状态」】，「晴」）；

测试.相等（已重开.读取计划任务（「刷新一」）【「修订」】，0）；

测试.相等（已重开.各迁移（）【0】【「编号」】，「迁移一」）；

测试.相等（已重开.查询事件（{「事务编号」：「事务一」}）【0】【「事件」】【「事件编号」】，「SQLite事件一」）；

令 提交请求 为 空；

逐 请求：典 于 执行器.请求列（） 则
    若 （（文字.始于（请求【「SQL」】，「INSERT INTO yanyu_repository_generations」） 且 （长度（请求【「参数」】） 等于 5）） 且 （请求【「参数」】【0】 等于 1）） 则
        置 提交请求 为 请求；
    终
终

测试.断言（（提交请求 是 典），「SQLite仓储必须使用参数化世代插入」）；

定 已参数化：典 为 提交请求；

测试.相等（长度（文字.分割（已参数化【「SQL」】，「测试:角色/SQLite甲」）），1）；

测试.断言（（已参数化【「参数」】【3】 是 文），「世代载荷必须作为参数传入」）；

仓储.关闭（）；

已重开.关闭（）；
