引「../../src/持久化/事件日志.yx」为 日志模块；

引「../../src/事件/事件总线.yx」为 事件；

引「标准:测试」为 测试；

引「标准:文字」为 文字；

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.断言（命中，「应抛出指定事件日志错误码」）；
终

法 新事实（编号：文，种类：文，现实时间：数）：典 则
    归 事件.新事件（编号，事件.类别事实，种类，现实时间，现实时间，「测试:角色/甲」，「测试:角色/乙」，{「数量」：1}，「命令一」，「事务一」，「追踪一」，「0.4.0」）；
终

定 日志 为 日志模块.内存事件日志（）；

定 事实一：典 为 新事实（「事件一」，「测试:事件/物品转移」，1000）；

定 记录一：典 为 日志.追加（事实一）；

测试.相等（记录一【「日志序号」】，1）；

测试.相等（记录一【「事件」】【「全局序号」】，1）；

测试.相等（记录一【「前项校验和」】，「」）；

测试.断言（日志模块.验证记录列（日志.全部（）），「首项事件日志应通过哈希链验证」）；

定 事实二：典 为 新事实（「事件二」，「测试:事件/任务完成」，2000）；

置 事实二【「全局序号」】 为 5；

定 记录二：典 为 日志.追加（事实二）；

测试.相等（记录二【「日志序号」】，2）；

测试.相等（记录二【「前项校验和」】，记录一【「校验和」】）；

测试.相等（日志.计数（），2）；

测试.相等（日志.末事件序号（），5）；

测试.相等（日志.末校验和（），记录二【「校验和」】）；

测试.相等（日志.查询（{「事件种类」：「测试:事件/任务完成」}）【0】【「事件」】【「事件编号」】，「事件二」）；

测试.相等（长度（日志.查询（{「起始序号」：2，「最大数量」：1}）），1）；

定 导出记录：列 为 日志.全部（）；

置 导出记录【0】【「事件」】【「载荷」】【「数量」】 为 999；

测试.相等（日志.全部（）【0】【「事件」】【「载荷」】【「数量」】，1）；

定 被篡改：列 为 日志.全部（）；

置 被篡改【1】【「事件」】【「载荷」】【「数量」】 为 2；

法 验证篡改（） 则
    日志模块.验证记录列（被篡改）；
终

应抛（验证篡改，日志模块.记录无效错误）；

法 重复事件（） 则
    日志.追加（事实一）；
终

应抛（重复事件，日志模块.事件重复错误）；

定 旧序事件：典 为 新事实（「事件三」，「测试:事件/物品转移」，3000）；

置 旧序事件【「全局序号」】 为 3；

法 写入旧序（） 则
    日志.追加（旧序事件）；
终

应抛（写入旧序，日志模块.序号冲突错误）；

定 意图：典 为 事件.新事件（「意图一」，事件.类别意图，「测试:事件/尝试移动」，3000，3000，「测试:角色/甲」，「测试:房间/乙」，{}，「命令二」，「事务二」，「追踪二」，「0.4.0」）；

法 写入意图（） 则
    日志.追加（意图）；
终

应抛（写入意图，日志模块.记录无效错误）；

法 非法查询（） 则
    日志.查询（{「未知」：真}）；
终

应抛（非法查询，日志模块.记录无效错误）；

定 已恢复 为 日志模块.从记录（日志.全部（））；

测试.相等（已恢复.计数（），2）；

令 已重放：列<文> 为 【】；

法 接收（事件值：典） 则
    追加（已重放，事件值【「事件编号」】）；
    归 空；
终

定 总线 为 事件.总线（8，100）；

总线.订阅（「」，接收，0，空，假）；

定 重放结果：列 为 已恢复.重放到（总线，1，10）；

测试.相等（长度（重放结果），2）；

测试.相等（长度（已重放），2）；

测试.相等（已重放【0】，「事件一」）；

测试.相等（已重放【1】，「事件二」）；
