引「../../src/持久化/文件仓储.yx」为 文件仓储；

引「../../src/持久化/仓储协议.yx」为 协议；

引「标准:文件」为 文件；

引「标准:路径」为 路径；

引「标准:测试」为 测试；

引「标准:文字」为 文字；

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.断言（命中，「应抛出指定文件仓储错误码」）；
终

定 根：文 为 「tests/.文件仓储损坏恢复」；

若 文件.存在（根） 则
    文件.删除（根，真）；
终

定 仓储 为 文件仓储.打开（根）；

仓储.设置世界变量（「已提交」，真）；

文件.写入（路径.合并（根，文件仓储.指针文件名），「损坏的指针」）；

定 已恢复 为 文件仓储.打开（根）；

测试.相等（已恢复.恢复报告（）【「指针已修复」】，真）；

测试.相等（已恢复.读取世界变量（「已提交」），真）；

定 假校验和：文 为 「aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa」；

文件.写入（路径.合并（根，（（「世代-999-」 加 假校验和） 加 「.yj」）），「损坏世代」）；

定 忽略孤儿 为 文件仓储.打开（根）；

测试.相等（忽略孤儿.恢复报告（）【「忽略文件数」】，1）；

文件.写入（路径.合并（根，忽略孤儿.当前文件（）），「损坏当前世代」）；

法 打开损坏当前世代（） 则
    文件仓储.打开（根）；
终

应抛（打开损坏当前世代，文件仓储.损坏错误）；

文件.删除（根，真）；

定 文件根：文 为 「tests/.文件仓储根文件」；

若 文件.存在（文件根） 则
    文件.删除（文件根，真）；
终

文件.写入（文件根，「不是目录」）；

法 打开文件根（） 则
    文件仓储.打开（文件根）；
终

应抛（打开文件根，协议.参数无效错误）；

文件.删除（文件根，假）；
