引「../../src/持久化/文件仓储.yx」为 文件仓储；

引「../../src/持久化/仓储协议.yx」为 协议；

引「标准:文件」为 文件；

引「标准:测试」为 测试；

引「标准:文字」为 文字；

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.断言（命中，「应抛出指定文件仓储错误码」）；
终

定 根：文 为 「tests/.文件仓储事务冲突」；

若 文件.存在（根） 则
    文件.删除（根，真）；
终

定 写者甲 为 文件仓储.打开（根）；

定 写者乙 为 文件仓储.打开（根）；

定 过期事务 为 写者甲.开始事务（）；

过期事务.设置世界变量（「竞争.甲」，1）；

写者乙.设置世界变量（「竞争.乙」，2）；

法 提交过期事务（） 则
    过期事务.提交（）；
终

应抛（提交过期事务，协议.事务冲突错误）；

测试.相等（过期事务.状态（），「已失败」）；

定 竞争后 为 文件仓储.打开（根）；

测试.相等（竞争后.读取世界变量（「竞争.甲」），空）；

测试.相等（竞争后.读取世界变量（「竞争.乙」），2）；

文件.删除（根，真）；
