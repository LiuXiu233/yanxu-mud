引「../../src/系统/时间系统.yx」为 时间；

引「../../src/测试支持/虚拟时钟.yx」为 测试时间；

引「../../src/持久化/内存仓储.yx」为 内存；

引「../../src/世界/世界循环.yx」为 世界；

引「../../src/消息/消息协议.yx」为 消息；

引「标准:测试」为 测试；

法 公告行为（事务，任务：典，上下文：典）：典 则
    归 {「数据」：{「公告」：真}，「消息」：【消息.新消息（【消息.系统提示（「信息」，【消息.文字（「钟声响起」）】）】，{}）】}；
终

法 任务上下文（任务）：典 则
    归 {「事务编号」：「tx-world-persistent-task」，「操作者编号」：「测试:系统/时间」，「权限」：【时间.时间管理权限】}；
终

法 空处理（项目） 则
    归 空；
终

令 已输出：列 为 【】；

法 输出处理（所消息） 则
    追加（已输出，所消息）；
    归 空；
终

定 仓储 为 内存.新仓储（）；

定 时钟 为 测试时间.虚拟时钟（0）；

定 注册 为 时间.新任务行为注册表（）；

注册.注册（「测试:计划行为/公告」，公告行为）；

定 服务 为 时间.时间服务（仓储，时钟，注册）；

服务.计划（「测试.公告」，10，0，「测试:计划行为/公告」，{}，{「事务编号」：「tx-world-persistent-schedule」，「操作者编号」：「测试:系统/时间」，「权限」：【时间.时间管理权限】}）；

定 循环 为 世界.世界循环（服务，服务.调度器（），空处理，时间.建立任务处理器（服务，任务上下文），空处理，空处理，空处理，空处理，输出处理）；

测试.相等（循环.是否有工作（），假）；

时钟.推进（10）；

测试.相等（循环.是否有工作（），真）；

定 结果：典 为 循环.轮转（世界.默认预算（））；

测试.相等（结果【「计数」】【「任务」】，1）；

测试.相等（结果【「计数」】【「事件」】，1）；

测试.相等（结果【「计数」】【「输出」】，1）；

测试.相等（长度（结果【「错误」】），0）；

测试.相等（长度（已输出），1）；

测试.相等（服务.读取任务（「测试.公告」），空）；

测试.相等（结果【「仍有工作」】，假）；
