引「../../src/事件/事件总线.yx」为 事件；

引「标准:测试」为 测试；

引「标准:JSON」为 JSON；

定 总线 为 事件.总线（3，10）；

令 次数：数 为 0；

法 递归处理（所事件） 则
    置 次数 为 （次数 加 1）；
    定 下一：典 为 事件.新事件（（「evt-r-」 加 JSON.序列化（次数）），事件.类别系统，「言域:事件/递归」，0，0，「系统」，「」，{}，「」，「」，「trace」，「基础@1.0.0」）；
    归 总线.发布（下一）；
终

总线.订阅（「言域:事件/递归」，递归处理，10，空，假）；

定 根：典 为 事件.新事件（「evt-root」，事件.类别系统，「言域:事件/递归」，0，0，「系统」，「」，{}，「」，「」，「trace」，「基础@1.0.0」）；

定 结果：列 为 总线.发布（根）；

测试.相等（次数，3）；

测试.相等（长度（总线.历史（）），3）；

测试.相等（结果【0】【「成功」】，真）；

令 有递归错误：理 为 假；

逐 项：典 于 总线.踪迹（） 则
    若 （项【「阶段」】 等于 「处理错误」） 则
        置 有递归错误 为 真；
    终
终

测试.相等（有递归错误，真）；
