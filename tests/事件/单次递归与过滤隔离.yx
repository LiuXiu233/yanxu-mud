引「../../src/事件/事件总线.yx」为 事件；

引「标准:测试」为 测试；

定 单次总线 为 事件.总线（8，20）；

令 单次次数：数 为 0；

令 递归结果：列 为 【】；

法 单次递归处理（所事件） 则
    置 单次次数 为（单次次数 加 1）；
    定 内层：典 为 事件.新事件（「evt-once-inner」，事件.类别系统，「言域:测试/单次递归」，0，0，「测试」，「」，{}，「」，「」，「trace-once-inner」，「测试@1.0.0」）；
    置 递归结果 为 单次总线.发布（内层）；
    归 空；
终

单次总线.订阅（「言域:测试/单次递归」，单次递归处理，10，空，真）；

定 外层：典 为 事件.新事件（「evt-once-outer」，事件.类别系统，「言域:测试/单次递归」，0，0，「测试」，「」，{}，「」，「」，「trace-once-outer」，「测试@1.0.0」）；

定 外层结果：列 为 单次总线.发布（外层）；

测试.相等（单次次数，1）；

测试.相等（长度（外层结果），1）；

测试.相等（长度（递归结果），0）；

令 观察：列 为 【】；

法 篡改过滤（所事件）：理 则
    置 所事件【「目标」】 为 「被篡改」；
    置 所事件【「载荷」】【「值」】 为 99；
    归 真；
终

法 记录首处理（所事件） 则
    追加（观察，（「首处理:」 加 所事件【「目标」】））；
    归 所事件【「载荷」】【「值」】；
终

法 观察过滤（所事件）：理 则
    追加（观察，（「后过滤:」 加 所事件【「目标」】））；
    归（所事件【「载荷」】【「值」】 等于 1）；
终

法 记录后处理（所事件） 则
    追加（观察，（「后处理:」 加 所事件【「目标」】））；
    归 所事件【「载荷」】【「值」】；
终

定 隔离总线 为 事件.总线（8，20）；

隔离总线.订阅（「言域:测试/过滤隔离」，记录首处理，10，篡改过滤，假）；

隔离总线.订阅（「言域:测试/过滤隔离」，记录后处理，20，观察过滤，假）；

定 隔离事件：典 为 事件.新事件（「evt-filter-isolation」，事件.类别事实，「言域:测试/过滤隔离」，10，20，「测试」，「原目标」，{「值」：1}，「」，「」，「trace-filter」，「测试@1.0.0」）；

定 隔离结果：列 为 隔离总线.发布（隔离事件）；

测试.相等（长度（隔离结果），2）；

测试.相等（观察【0】，「首处理:原目标」）；

测试.相等（观察【1】，「后过滤:原目标」）；

测试.相等（观察【2】，「后处理:原目标」）；

测试.相等（隔离结果【0】【「结果」】，1）；

测试.相等（隔离结果【1】【「结果」】，1）；

测试.相等（隔离事件【「目标」】，「原目标」）；

测试.相等（隔离事件【「载荷」】【「值」】，1）；
