引「../../src/事件/事件总线.yx」为 事件；

引「标准:测试」为 测试；

令 顺序：列<数> 为 【】；

法 记一（所事件） 则
    追加（顺序，1）；
    归 空；
终

法 记二（所事件） 则
    追加（顺序，2）；
    归 空；
终

法 记三（所事件） 则
    追加（顺序，3）；
    归 空；
终

法 记四（所事件） 则
    追加（顺序，4）；
    归 空；
终

法 记五（所事件） 则
    追加（顺序，5）；
    归 空；
终

法 记六（所事件） 则
    追加（顺序，6）；
    归 空；
终

法 记七（所事件） 则
    追加（顺序，7）；
    归 空；
终

法 记八（所事件） 则
    追加（顺序，8）；
    归 空；
终

法 记九（所事件） 则
    追加（顺序，9）；
    归 空；
终

法 记十（所事件） 则
    追加（顺序，10）；
    归 空；
终

法 记十一（所事件） 则
    追加（顺序，11）；
    归 空；
终

法 记十二（所事件） 则
    追加（顺序，12）；
    归 空；
终

定 总线 为 事件.总线（8，100）；

总线.订阅（「言域:测试/顺序」，记一，10，空，假）；

总线.订阅（「言域:测试/顺序」，记二，10，空，假）；

总线.订阅（「言域:测试/顺序」，记三，10，空，假）；

总线.订阅（「言域:测试/顺序」，记四，10，空，假）；

总线.订阅（「言域:测试/顺序」，记五，10，空，假）；

总线.订阅（「言域:测试/顺序」，记六，10，空，假）；

总线.订阅（「言域:测试/顺序」，记七，10，空，假）；

总线.订阅（「言域:测试/顺序」，记八，10，空，假）；

总线.订阅（「言域:测试/顺序」，记九，10，空，假）；

总线.订阅（「言域:测试/顺序」，记十，10，空，假）；

总线.订阅（「言域:测试/顺序」，记十一，10，空，假）；

总线.订阅（「言域:测试/顺序」，记十二，10，空，假）；

定 所事件：典 为 事件.新事件（「evt-order」，事件.类别系统，「言域:测试/顺序」，0，0，「测试」，「」，{}，「」，「」，「trace-order」，「测试@1.0.0」）；

总线.发布（所事件）；

测试.相等（长度（顺序），12）；

逐 次：数 于 范围（0，12） 则
    测试.相等（顺序【次】，（次 加 1））；
终
