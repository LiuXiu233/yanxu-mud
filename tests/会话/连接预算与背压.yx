引「../../src/会话/会话服务.yx」为 会话；

引「标准:测试」为 测试；

引「标准:文字」为 文字；

引「标准:JSON」为 JSON；

令 编号序号：数 为 0；

法 生成编号（类别：文）：文 则
    置 编号序号 为 （编号序号 加 1）；
    归 （（「测试:」 加 类别） 加 （「/」 加 JSON.序列化（编号序号）））；
终

令 令牌序号：数 为 0；

法 生成令牌（）：文 则
    置 令牌序号 为 （令牌序号 加 1）；
    归 （「token-」 加 JSON.序列化（令牌序号））；
终

法 摘要令牌（令牌：文）：文 则
    归 （「digest-」 加 令牌）；
终

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.断言（命中，「应抛出指定连接错误码」）；
终

定 服务 为 会话.会话服务（生成编号，生成令牌，摘要令牌，{「发送队列项」：2，「发送队列字节」：10，「单条输出字节」：8，「单条输入字节」：5，「输入窗口毫秒」：1000，「输入窗口条数」：2，「输入窗口字节」：8，「空闲超时毫秒」：100}）；

服务.打开连接（「conn-queue」，「Telnet」，「127.0.0.1:20001」，0）；

定 已创建：典 为 服务.创建会话（「conn-queue」，「测试:账户/甲」，「测试:角色/甲」，【】，1）；

定 会话编号：文 为 已创建【「会话」】【「会话编号」】；

服务.入队（会话编号，{「消息」：「甲」}，4，2）；

服务.入队（会话编号，{「消息」：「乙」}，5，3）；

定 一项：列 为 服务.出队（「conn-queue」，1，4）；

测试.相等（长度（一项），1）；

测试.相等（一项【0】【「序号」】，1）；

测试.相等（服务.读取连接（「conn-queue」）【「排队字节」】，5）；

服务.入队（会话编号，{「消息」：「丙」}，5，4）；

法 队列过载（） 则
    服务.入队（会话编号，{「消息」：「丁」}，1，5）；
终

应抛（队列过载，会话.背压错误）；

测试.相等（服务.读取连接（「conn-queue」）【「状态」】，「关闭」）；

测试.相等（服务.读取会话（会话编号）【「状态」】，「已断开」）；

服务.打开连接（「conn-rate」，「WebSocket」，「127.0.0.1:20002」，10）；

服务.接受输入（「conn-rate」，4，11）；

服务.接受输入（「conn-rate」，4，12）；

法 输入超限（） 则
    服务.接受输入（「conn-rate」，1，13）；
终

应抛（输入超限，会话.速率限制错误）；

测试.相等（服务.读取连接（「conn-rate」）【「状态」】，「关闭」）；

服务.打开连接（「conn-size」，「Telnet」，「127.0.0.1:20003」，20）；

法 输入过大（） 则
    服务.接受输入（「conn-size」，6，21）；
终

应抛（输入过大，会话.速率限制错误）；

服务.打开连接（「conn-idle」，「Telnet」，「127.0.0.1:20004」，30）；

测试.相等（长度（服务.检查空闲（129）），0）；

定 已空闲：列 为 服务.检查空闲（130）；

测试.相等（长度（已空闲），1）；

测试.相等（已空闲【0】，「conn-idle」）；

测试.相等（服务.清理关闭连接（），4）；
