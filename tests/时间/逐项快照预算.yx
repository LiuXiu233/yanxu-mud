引「../../src/测试支持/虚拟时钟.yx」为 时间；

引「标准:测试」为 测试；

令 深载荷：任意 为 0；

令 深度：数 为 0；

当 （深度 小于 63） 则
    置 深载荷 为 【深载荷】；
    置 深度 为 （深度 加 1）；
终

定 边界调度 为 时间.调度器（）；

边界调度.计划（「深任务」，1，0，深载荷）；

定 边界快照：列 为 边界调度.快照（）；

测试.相等（长度（边界快照），1）；

测试.相等（边界快照【0】【「编号」】，「深任务」）；

定 边界恢复 为 时间.调度器（）；

边界恢复.恢复（边界快照）；

测试.相等（边界恢复.下一到期（），1）；

测试.相等（边界恢复.快照（）【0】【「编号」】，「深任务」）；
