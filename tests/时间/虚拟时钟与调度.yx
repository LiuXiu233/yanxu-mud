引「../../src/测试支持/虚拟时钟.yx」为 时间；

引「标准:测试」为 测试；

定 时钟 为 时间.虚拟时钟（1000）；

定 调度 为 时间.调度器（）；

调度.计划（「公告」，2000，1000，{「文字」：「整点公告」}）；

调度.计划（「刷新」，1500，0，{「原型」：「测试:NPC/守卫」}）；

时钟.推进（500）；

定 第一批：列 为 调度.到期（时钟.毫秒（），10）；

测试.相等（长度（第一批），1）；

测试.相等（第一批【0】【「编号」】，「刷新」）；

时钟.推进（2500）；

定 第二批：列 为 调度.到期（时钟.毫秒（），10）；

测试.相等（长度（第二批），3）；

测试.相等（第二批【0】【「编号」】，「公告」）；

测试.相等（调度.下一到期（），5000）；

定 快照：列 为 调度.快照（）；

定 已恢复 为 时间.调度器（）；

已恢复.恢复（快照）；

测试.相等（已恢复.下一到期（），5000）；
