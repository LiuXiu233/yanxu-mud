引「../../src/世界/世界事务.yx」为 世界事务；

引「../../src/持久化/内存仓储.yx」为 内存；

引「../../src/持久化/仓储协议.yx」为 协议；

引「../../src/实体/实体.yx」为 实体；

引「../../src/事件/事件总线.yx」为 事件协议；

引「../../src/消息/消息协议.yx」为 消息协议；

引「标准:测试」为 测试；

引「标准:文字」为 文字；

法 应抛（操作：法，代码：文）：空 则
    令 命中：理 为 假；
    试 则
        操作（）；
    救 所误 则
        置 命中 为 文字.始于（所误.消息，代码）；
    终
    测试.断言（命中，「应抛出指定事务错误码」）；
终

法 加在线（所实体） 则
    所实体.添加标签（「在线」）；
    归 空；
终

定 仓储 为 内存.新仓储（）；

定 角色 为 实体.新实体（「测试:角色/甲」，「」）；

仓储.写入实体快照（角色.快照（），空）；

定 事务 为 世界事务.开始（仓储，「tx-1」）；

事务.修改实体（「测试:角色/甲」，加在线）；

事务.设置世界变量（「世界.轮次」，1）；

定 事件：典 为 事件协议.新事件（「evt-1」，事件协议.类别事实，「测试:事件/提交」，1，2，「测试:角色/甲」，「」，{「值」：1}，「测试:命令/修改」，「tx-1」，「trace-1」，「测试@0.2.0」）；

定 消息：典 为 消息协议.新消息（【消息协议.文字（「提交后可见」）】，{}）；

事务.暂存事件（事件）.暂存消息（消息）；

置 事件【「载荷」】【「值」】 为 99；

置 消息【「节点」】【0】【「内容」】 为 「外部篡改」；

测试.相等（仓储.读取实体（「测试:角色/甲」）.有标签（「在线」），假）；

测试.相等（仓储.读取世界变量（「世界.轮次」），空）；

测试.相等（事务.待提交计数（）【「事件」】，1）；

定 输出：典 为 事务.提交（）；

测试.相等（输出【「已提交」】，真）；

测试.相等（输出【「事件」】【0】【「载荷」】【「值」】，1）；

测试.相等（输出【「消息」】【0】【「节点」】【0】【「内容」】，「提交后可见」）；

测试.相等（仓储.读取实体（「测试:角色/甲」）.有标签（「在线」），真）；

测试.相等（仓储.读取世界变量（「世界.轮次」），1）；

置 输出【「消息」】【0】【「节点」】【0】【「内容」】 为 「改提交结果」；

测试.相等（事务.已提交输出（）【「消息」】【0】【「节点」】【0】【「内容」】，「提交后可见」）；

定 回滚 为 世界事务.开始（仓储，「tx-rollback」）；

回滚.设置世界变量（「不应存在」，真）；

回滚.暂存事件（事件协议.新事件（「evt-rollback」，事件协议.类别事实，「测试:事件/回滚」，2，3，「」，「」，{}，「」，「tx-rollback」，「trace-rollback」，「测试@0.2.0」））；

回滚.暂存消息（消息协议.新消息（【消息协议.文字（「不应发送」）】，{}））；

回滚.回滚（）；

测试.相等（仓储.读取世界变量（「不应存在」），空）；

测试.相等（回滚.已提交输出（）【「已提交」】，假）；

测试.相等（长度（回滚.已提交输出（）【「事件」】），0）；

定 并发甲 为 世界事务.开始（仓储，「tx-a」）；

定 并发乙 为 世界事务.开始（仓储，「tx-b」）；

并发甲.设置世界变量（「并发甲」，1）；

并发乙.设置世界变量（「并发乙」，2）；

并发乙.暂存消息（消息协议.新消息（【消息协议.文字（「不可泄漏」）】，{}））；

并发甲.提交（）；

法 冲突提交（） 则
    并发乙.提交（）；
终

应抛（冲突提交，协议.事务冲突错误）；

测试.相等（并发乙.状态（），「已失败」）；

测试.相等（仓储.读取世界变量（「并发乙」），空）；

测试.相等（长度（并发乙.已提交输出（）【「消息」】），0）；

定 非法发件箱 为 世界事务.开始（仓储，「tx-invalid-outbox」）；

法 暂存非法事件（） 则
    非法发件箱.暂存事件（{「事件编号」：「不完整」}）；
终

法 暂存非法消息（） 则
    非法发件箱.暂存消息（{「种类」：「文字」，「内容」：「裸节点」}）；
终

应抛（暂存非法事件，世界事务.参数错误）；

应抛（暂存非法消息，世界事务.参数错误）；

定 意图事件：典 为 事件协议.新事件（「evt-intent」，事件协议.类别意图，「测试:事件/意图」，3，4，「」，「」，{}，「」，「tx-invalid-outbox」，「trace-intent」，「测试@0.2.0」）；

法 暂存意图事件（） 则
    非法发件箱.暂存事件（意图事件）；
终

应抛（暂存意图事件，世界事务.参数错误）；

定 已编号事件：典 为 事件协议.新事件（「evt-numbered」，事件协议.类别事实，「测试:事件/已编号」，3，4，「」，「」，{}，「」，「tx-invalid-outbox」，「trace-numbered」，「测试@0.2.0」）；

置 已编号事件【「全局序号」】 为 9；

法 暂存已编号事件（） 则
    非法发件箱.暂存事件（已编号事件）；
终

应抛（暂存已编号事件，世界事务.参数错误）；

定 重放标记事件：典 为 事件协议.新事件（「evt-replay-flag」，事件协议.类别事实，「测试:事件/重放标记」，3，4，「」，「」，{}，「」，「tx-invalid-outbox」，「trace-replay-flag」，「测试@0.2.0」）；

置 重放标记事件【「重放」】 为 真；

法 暂存重放标记事件（） 则
    非法发件箱.暂存事件（重放标记事件）；
终

应抛（暂存重放标记事件，世界事务.参数错误）；

定 错事务事件：典 为 事件协议.新事件（「evt-wrong-tx」，事件协议.类别事实，「测试:事件/错事务」，3，4，「」，「」，{}，「」，「别的事务」，「trace-wrong-tx」，「测试@0.2.0」）；

法 暂存错事务事件（） 则
    非法发件箱.暂存事件（错事务事件）；
终

应抛（暂存错事务事件，世界事务.参数错误）；

测试.相等（非法发件箱.待提交计数（）【「事件」】，0）；

测试.相等（非法发件箱.待提交计数（）【「消息」】，0）；

非法发件箱.回滚（）；

法 建立空白事务（） 则
    世界事务.开始（仓储，「   」）；
终

应抛（建立空白事务，世界事务.参数错误）；

定 坏修改器事务 为 世界事务.开始（仓储，「tx-bad-modifier」）；

法 使用坏修改器（） 则
    坏修改器事务.修改实体（「测试:角色/甲」，「不是函数」）；
终

应抛（使用坏修改器，世界事务.参数错误）；

坏修改器事务.回滚（）；

仓储.设故障（「提交事务」，「注入提交失败」，1）；

定 故障事务 为 世界事务.开始（仓储，「tx-fault」）；

故障事务.设置世界变量（「故障不应写入」，真）；

故障事务.暂存事件（事件协议.新事件（「evt-fault」，事件协议.类别事实，「测试:事件/故障」，4，5，「」，「」，{}，「」，「tx-fault」，「trace-fault」，「测试@0.2.0」））；

故障事务.暂存消息（消息协议.新消息（【消息协议.文字（「不可泄漏」）】，{}））；

法 提交故障事务（） 则
    故障事务.提交（）；
终

应抛（提交故障事务，协议.测试故障错误）；

测试.相等（故障事务.状态（），「已失败」）；

测试.相等（仓储.读取世界变量（「故障不应写入」），空）；

测试.相等（长度（故障事务.已提交输出（）【「事件」】），0）；

测试.相等（长度（故障事务.已提交输出（）【「消息」】），0）；

定 聚合事务 为 世界事务.开始（仓储，「tx-large-outbox」）；

聚合事务.设置世界变量（「大聚合已提交」，真）；

令 大数据：列<数> 为 【】；

逐 序号：数 于 范围（0，100） 则
    追加（大数据，序号）；
终

定 大消息甲：典 为 消息协议.新消息（【消息协议.自定义（「测试:消息/大甲」，大数据，【消息协议.文字（「甲」）】）】，{}）；

定 大消息乙：典 为 消息协议.新消息（【消息协议.自定义（「测试:消息/大乙」，大数据，【消息协议.文字（「乙」）】）】，{}）；

聚合事务.暂存消息（大消息甲）.暂存消息（大消息乙）；

定 聚合输出：典 为 聚合事务.提交（）；

测试.相等（聚合事务.状态（），「已提交」）；

测试.相等（仓储.读取世界变量（「大聚合已提交」），真）；

测试.相等（长度（聚合输出【「消息」】），2）；

测试.相等（长度（聚合事务.已提交输出（）【「消息」】【1】【「节点」】【0】【「数据」】），100）；
