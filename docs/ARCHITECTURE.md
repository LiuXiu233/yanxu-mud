# 架构

## 原则

言域以协议隔离变化频率不同的部分：具体游戏依赖引擎，接入和持久化实现依赖稳定协议，世界内核不反向依赖边界实现。

```text
客户端与工具
    ↓ 结构化输入/输出
接入层（连接、编码、限流、恢复）
    ↓ 命令请求/会话上下文
应用层（账户、角色、权限、频道、管理）
    ↓ 世界事务
世界内核（实体、组件、系统、事件、查询、调度）
    ↓ 变更集/仓储协议
内容运行时与持久化
```

## 关键边界

1. 引擎与游戏：引擎只提供通用组件、系统和协议；青石镇通过独立清单、源码和内容包使用它们。
2. 逻辑与内容：规则由言序模块注册，文本、房间、原型、任务与掉落由言据定义；言据不执行任意代码。
3. 静态与动态：静态原型来自内容包；角色、物品实例、任务进度和计划任务进入状态仓储。
4. 接入与世界：网络输入转换为命令请求；世界层只返回结构化消息，渲染器在边界产生纯文本、ANSI 或 Web 数据。
5. 持久化与领域：世界提交变更集；内存、言据文件和 SQLite 仓储实现同一契约。

## 内容运行时

文件加载器先读取所有包头并解析依赖，再在统一预算内读取 Schema、内容和本地化。内容图按清单、Schema、文档、引用、原型、本地化、行为的顺序完成验证，对最终语义生成确定性指纹和总摘要。内容差异沿引用反向传播影响；热重载只在候选图完整验证、静态策略、现场影响和代数检查全部通过后替换当前图。

内容数据只引用稳定行为编号，不能携带可执行回调。静态内容留在内容图，实体、任务进度和计划任务等动态状态进入仓储。

## 账户边界

账户是应用层实体，不等同于连接、会话、角色或世界实体的玩家化身。账户服务以大小写不敏感登录键维护原子索引，并把账户状态、索引变更、事实事件和结构化消息放在同一世界事务中。公开账户投影只包含稳定编号、显示登录名、状态、角色编号和时间，不暴露凭据。

密码算法位于可信接入适配边界。账户服务只接受注入的摘要生成与验证函数，不实现密码学算法；因此测试可以使用确定性替身，生产适配器必须使用专用密码哈希库。会话令牌、连接限制与统一登录响应时间属于后续接入层，不进入世界内核。

角色是受账户控制的持久世界实体，但不是账户本身。角色创建把账户的角色编号列和完整角色快照原子提交；受保护删除则把账户索引移除和实体删除原子提交。角色服务只接受稳定的静态房间与原型编号，不把静态内容复制进动态状态。删除截止时间来自注入的世界时间，测试可以确定性推进。

权限服务从仓储中的权限组件重新计算角色组和显式授权，不信任接入请求携带的权限列。命令上下文在执行前投影权威权限；管理员引导、授权和角色组轮换都通过世界事务提交并产生事实事件。权限系统只是服务的注册表适配器，不拥有独立状态。

空间服务通过注入解析器读取内容图中的静态房间，仓储只保存实体当前位置。移动更新、事实事件和目标场景消息共享世界事务；观察只读查询同房间实体。移动与观察以两个独立系统注册，接入层和渲染器不进入空间规则。

物品服务同样通过注入解析器读取静态定义，动态物品实体只保存定义引用、数量、绑定和位置等状态。拾取、放下与容器转移在单个世界事务中维护来源列表、目标列表和位置组件，提交失败不会留下半转移状态。静态房间编号可以直接作为地面位置；动态背包和容器使用显式实例编号列并在读取时校验双向一致性。

装备槽保存物品实例编号，不复制物品静态属性。穿戴和卸下在角色世界事务中切换背包/槽位引用，并调用通用属性来源聚合器重新生成当前属性；状态组件同样只保存定义引用与动态层数/时间。聚合器按稳定提供器及来源编号组合装备、状态和插件数据，任一子系统触发重算都不会覆盖其他来源。战斗组件中的攻击、防御和最大生命是同事务更新的运行投影；内容变更后的再次重算会读取最新静态定义，基础角色属性仍保持独立。

技能定义留在内容图，通过稳定行为编号绑定启动时注册的言序规则；实体只保存已学技能和到期时间。技能使用把冷却写入与行为对世界事务的修改合并提交。通用伤害行为在同一事务修改双方战斗投影，致死时写入死亡标签、时间和击败者；复活服务再原子清除这些动态状态，并可迁移已有位置组件。静态技能参数、显示文字和行为实现都不会复制进动态存档。

任务模板、对话节点和掉落表同样留在内容图。任务组件只保存定义编号、状态、每步计数和接受/完成时间；完成事实携带静态奖励描述，具体物品或货币授予由后续组合规则在自己的可信事务边界处理。对话组件只保存当前节点、定义、交互对象、索引和开始时间；内容关联任务或商品仍由上层命令组合，不复制为动态权威配置。掉落组件只保存表引用、抽取次数和幂等结算状态，随机源及实例编号生成器由组合层注入，全部生成物品与结算事实共享一个世界事务。

## 世界轮次

世界循环由事件唤醒，不高频空转。单轮按网络输入、到期任务、命令、世界事务、领域事件、持久化、结构化输出的顺序处理。每一阶段使用有限非负整数硬预算；所有时钟与随机种子可注入，确保测试和重放确定。持久调度器只读领取候选，时间服务把任务行为效果与删除或重排确认原子提交，因此失败任务可重试且成功效果不会被旧修订重复执行。

一轮只读取一次世界时间。处理器、时钟或调度器故障会转换为不含原始消息和源码位置的结构化阶段错误；唤醒计算失败时采用“仍有工作”的保守结果，已发送的结构化输出不会从轮次报告中丢失。运行锁在全部成功与失败路径上释放，下一轮可以继续恢复处理。

## 稳定编号

内容编号统一为 `内容包名:类别/标识`。协议编号、错误代码和行为编号是存档与插件边界的一部分，不能以显示文字代替。显示文字和本地化键可以演进，稳定编号不得在兼容版本中改义。
