引「../src/游戏.yx」为 游戏；

引「../src/服务器.yx」为 服务器；

引「包:言据」为 言据；

引「包:言域/内存仓储」为 内存；

引「包:言域/虚拟时钟」为 时间；

引「标准:测试」为 测试；

定 所时钟 为 时间.虚拟时钟（3000）；

法 当前（）：数 则
    归 所时钟.毫秒（）；
终

定 所游戏 为 游戏.以仓储（「内容」，内存.新仓储（），所时钟）；

定 环境值 为 服务器.网络环境（所游戏，当前，{「内容根」：「内容」，「客户端根」：「web」，「会话密钥」：「0123456789abcdef0123456789abcdef」，「允许游客」：真}）；

测试.相等（环境值.内容状态（）【「内容图摘要」】，所游戏.信息（）【「内容图摘要」】）；

定 运行时索引：典 为 言据.读取据（「运行时世界/索引.yj」）；

测试.相等（环境值.内容状态（）【「内容图摘要」】，运行时索引【「内容图摘要」】）；

定 重载：典 为 环境值.热重载（{}）；

测试.相等（重载【「已验证」】，真）；

测试.相等（重载【「已应用」】，真）；

测试.相等（重载【「变化」】，假）；
