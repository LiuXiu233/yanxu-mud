引「../src/游戏.yx」为 游戏；

引「../src/服务器.yx」为 服务器；

引「包:言域/内存仓储」为 内存；

引「包:言域/虚拟时钟」为 时间；

引「标准:JSON」为 JSON；

引「标准:测试」为 测试；

定 所时钟 为 时间.虚拟时钟（1000）；

法 当前（）：数 则
    归 所时钟.毫秒（）；
终

定 所游戏 为 游戏.以仓储（「内容」，内存.新仓储（），所时钟）；

定 环境值 为 服务器.网络环境（所游戏，当前，{「内容根」：「内容」，「客户端根」：「web」，「会话密钥」：「0123456789abcdef0123456789abcdef」，「登录名」：「qingstone」，「登录密钥」：「only-for-test」，「管理令牌」：「admin-only-for-test」，「允许游客」：真}）；

定 信息：典 为 环境值.服务器信息（）；

测试.相等（信息【「游戏」】【「名称」】，「青石镇」）；

测试.相等（信息【「登录已配置」】，真）；

测试.断言（非 包含（JSON.序列化（信息），「only-for-test」），「服务器投影不得包含登录密钥」）；

测试.相等（环境值.管理认证（「Bearer admin-only-for-test」，「查看诊断」），真）；

测试.相等（环境值.管理认证（「Bearer wrong」，「查看诊断」），假）；

定 网关 为 环境值.网关（）；

网关.打开连接（「test-login」，「WebSocket」，「loopback」，1000）；

定 错误登录：典 为 网关.处理请求（「test-login」，{「种类」：「登录」，「请求编号」：「bad-login」，「登录名」：「qingstone」，「密码」：「wrong」}，1001）；

测试.相等（错误登录【「成功」】，假）；

测试.相等（错误登录【「错误」】【「代码」】，服务器.登录拒绝错误）；

测试.断言（非 包含（JSON.序列化（错误登录），「wrong」），「登录失败响应不得回显密钥」）；

定 已登录：典 为 网关.处理请求（「test-login」，{「种类」：「登录」，「请求编号」：「login」，「登录名」：「qingstone」，「密码」：「only-for-test」}，1002）；

测试.相等（已登录【「成功」】，真）；

测试.相等（已登录【「数据」】【「会话」】【「角色编号」】，「青石镇:角色/旅人」）；

定 观察：典 为 网关.处理请求（「test-login」，{「种类」：「命令」，「请求编号」：「look」，「命令」：「观察」}，1003）；

测试.相等（观察【「成功」】，真）；

测试.断言（（长度（观察【「消息」】） 大于 0），「真实游戏命令必须返回结构化消息」）；

网关.处理请求（「test-login」，{「种类」：「退出」，「请求编号」：「logout」}，1004）；

网关.断开连接（「test-login」，1005，「测试完成」）；

网关.打开连接（「test-guest」，「WebSocket」，「loopback」，1006）；

定 游客：典 为 网关.处理请求（「test-guest」，{「种类」：「游客」，「请求编号」：「guest」}，1007）；

测试.相等（游客【「成功」】，真）；

测试.相等（游客【「数据」】【「会话」】【「游客」】，真）；

定 无管理环境 为 服务器.网络环境（所游戏，当前，{「内容根」：「内容」，「客户端根」：「web」，「会话密钥」：「abcdef0123456789abcdef0123456789」，「允许游客」：真}）；

测试.相等（无管理环境.管理认证（「Bearer anything」，「查看诊断」），假）；
