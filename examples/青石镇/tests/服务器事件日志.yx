引「../src/游戏.yx」为 游戏；

引「../src/服务器.yx」为 服务器；

引「包:言域/内存仓储」为 内存；

引「包:言域/虚拟时钟」为 时间；

引「标准:测试」为 测试；

定 所时钟 为 时间.虚拟时钟（4000）；

法 当前（）：数 则
    归 所时钟.毫秒（）；
终

定 所游戏 为 游戏.以仓储（「内容」，内存.新仓储（），所时钟）；

定 环境值 为 服务器.网络环境（所游戏，当前，{「内容根」：「内容」，「客户端根」：「web」，「会话密钥」：「0123456789abcdef0123456789abcdef」，「允许游客」：真}）；

定 网关 为 环境值.网关（）；

网关.打开连接（「event-guest」，「WebSocket」，「loopback」，4000）；

定 游客：典 为 网关.处理请求（「event-guest」，{「种类」：「游客」，「请求编号」：「guest」}，4001）；

测试.相等（游客【「成功」】，真）；

定 移动：典 为 网关.处理请求（「event-guest」，{「种类」：「命令」，「请求编号」：「move」，「命令」：「北」}，4002）；

测试.相等（移动【「成功」】，真）；

定 日志：典 为 环境值.日志查询（{「游标」：1，「上限」：10}）；

测试.断言（（长度（日志【「项目」】） 大于 0），「真实游戏事实必须进入服务器事件日志」）；

测试.相等（日志【「项目」】【0】【「事件」】【「类别」】，「事实」）；

测试.相等（环境值.诊断（）【「事件日志数」】，长度（日志【「项目」】））；
