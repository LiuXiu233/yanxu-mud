引「../src/游戏.yx」为 游戏；

引「../src/服务器.yx」为 服务器；

引「包:言域/内存仓储」为 内存；

引「包:言域/虚拟时钟」为 时间；

引「标准:测试」为 测试；

定 所时钟 为 时间.虚拟时钟（2000）；

法 当前（）：数 则
    归 所时钟.毫秒（）；
终

定 所游戏 为 游戏.以仓储（「内容」，内存.新仓储（），所时钟）；

定 环境值 为 服务器.网络环境（所游戏，当前，{「内容根」：「内容」，「客户端根」：「web」，「会话密钥」：「0123456789abcdef0123456789abcdef」，「允许游客」：真}）；

定 快照：典 为 环境值.创建快照（{「原因」：「测试」}）；

测试.相等（快照【「元数据」】【「来源」】，「HTTP管理」）；

定 日志：典 为 环境值.日志查询（{「游标」：「1」，「上限」：「10」}）；

测试.相等（日志【「游标」】，1）；

测试.相等（日志【「上限」】，10）；

测试.相等（日志【「下一游标」】，1）；

测试.相等（环境值.诊断（）【「快照数」】，1）；
