引「../src/轻量存档.yx」为 轻量存档；

引「包:言域/内存仓储」为 内存；

引「包:言域/实体」为 实体；

引「标准:文件」为 文件；

引「标准:测试」为 测试；

定 存档路径：文 为 「.yanxu/测试-轻量存档重启.json」；

若 文件.存在（存档路径） 则
    文件.删除（存档路径，假）；
终

定 仓储 为 内存.新仓储（）；

定 角色 为 实体.新实体（「测试:角色/重启」，「测试:原型/角色」）；

角色.添加标签（「存档」）；

仓储.写入实体快照（角色.快照（），空）；

仓储.设置世界变量（「测试.重启」，{「轮次」：1}）；

仓储.保存计划任务（{「编号」：「测试.重启任务」，「到期」：100，「间隔」：0，「载荷」：{「实体编号」：「测试:角色/重启」}}，空）；

仓储.创建快照（「重启前」，{「阶段」：1}）；

定 首次保存：典 为 轻量存档.保存（仓储，存档路径）；

测试.相等（首次保存【「实体数量」】，1）；

测试.相等（首次保存【「任务数量」】，1）；

仓储.设置世界变量（「测试.重启」，{「轮次」：99}）；

定 首次重启 为 轻量存档.打开（存档路径）；

测试.相等（首次重启.读取实体（「测试:角色/重启」）.有标签（「存档」），真）；

测试.相等（首次重启.读取世界变量（「测试.重启」）【「轮次」】，1）；

测试.相等（首次重启.读取计划任务（「测试.重启任务」）【「到期」】，100）；

测试.相等（长度（首次重启.各快照（）），1）；

首次重启.设置世界变量（「测试.重启」，{「轮次」：2}）；

轻量存档.保存（首次重启，存档路径）；

定 二次重启 为 轻量存档.打开（存档路径）；

测试.相等（二次重启.读取世界变量（「测试.重启」）【「轮次」】，2）；

测试.相等（二次重启.读取实体（「测试:角色/重启」）.编号，「测试:角色/重启」）；

文件.删除（存档路径，假）；
