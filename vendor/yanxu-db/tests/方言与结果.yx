引「../src/言库.yx」为 言库；

引「标准:测试」为 测试；

定 轻舟 为 言库.SQLite方言（）；

定 象库 为 言库.PostgreSQL方言（）；

定 海豚 为 言库.MySQL方言（）；

测试.相等（轻舟.占位符（1），「?」）；

测试.相等（象库.占位符（2），「$2」）；

测试.相等（海豚.占位符（3），「%s」）；

测试.相等（轻舟.引名（「用户.姓名」），「"用户"."姓名"」）；

测试.相等（海豚.引名（「用户.姓名」），「`用户`.`姓名`」）；

测试.相等（轻舟.分页（「SELECT * FROM t」，10，20），「SELECT * FROM t LIMIT 10 OFFSET 20」）；

定 参数 为 言库.参数表（象库）；

测试.相等（参数.加入（「甲」），「$1」）；

测试.相等（参数.加入（42），「$2」）；

测试.相等（长度（参数.参数（）），2）；

定 结果 为 言库.查询结果（【「编号」，「姓名」】，【{「编号」：1，「姓名」：「子衿」}】，1，1，{}）；

测试.相等（结果.数量（），1）；

测试.相等（结果.首行（）【「姓名」】，「子衿」）；

测试.相等（结果.标量（），1）；
