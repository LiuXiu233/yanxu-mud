引「../src/言库.yx」为 言库；

引「标准:测试」为 测试；

令 请求列：列 为 【】；

法 执行器（请求：典）：典 则
    追加（请求列，请求）；
    若 （请求【「动作」】 等于 「查询」） 则
        归 {「成功」：真，「列名」：【「值」】，「行」：【{「值」：请求【「参数」】【0】}】，「影响行数」：0}；
    终
    归 {「成功」：真，「行」：【】，「影响行数」：1}；
终

定 连接 为 言库.函数连接（「测试」，言库.SQLite方言（），执行器）；

定 结果 为 连接.查询（「SELECT ?」，【42】）；

测试.相等（结果.标量（），42）；

定 语句 为 连接.预编译（「SELECT ?」）；

测试.相等（语句.查询（【7】）.标量（），7）；

定 外层 为 连接.开始事务（）；

外层.执行（「UPDATE t SET v = ?」，【1】）；

定 内层 为 外层.保存点（）；

内层.执行（「UPDATE t SET v = ?」，【2】）；

内层.回滚（）；

外层.提交（）；

测试.相等（请求列【2】【「SQL」】，「BEGIN」）；

测试.相等（请求列【4】【「动作」】，「保存点」）；

测试.相等（请求列【6】【「动作」】，「回滚保存点」）；

测试.相等（请求列【7】【「SQL」】，「COMMIT」）；

法 新连接（） 则
    归 言库.函数连接（「测试」，言库.SQLite方言（），执行器）；
终

定 池 为 言库.连接池（新连接，2）；

定 租用甲 为 池.取得（）；

定 租用乙 为 池.取得（）；

测试.相等（池.状态（）【「借出」】，2）；

租用甲.释放（）；

测试.相等（池.状态（）【「空闲」】，1）；

租用乙.释放（）；

池.关闭（）；

测试.断言（池.状态（）【「已关闭」】，「连接池应关闭」）；
