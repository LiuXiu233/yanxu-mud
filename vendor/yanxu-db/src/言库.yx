引「标准:JSON」为 JSON；

引「标准:文字」为 文字；

公 定 版本：文 为 「0.1.0」；

法 有所键（所典：典，键）：理 则
    归 包含（键列（所典），键）；
终

法 复制列（来源：列）：列 则
    令 结果：列 为 【】；
    逐 项 于 来源 则
        追加（结果，项）；
    终
    归 结果；
终

法 复制典（来源：典）：典 则
    令 结果：典 为 {}；
    逐 键 于 来源 则
        置 结果【键】 为 来源【键】；
    终
    归 结果；
终

法 整数文（值：数）：文 则
    归 文字.替换（JSON.序列化（值），「.0」，「」）；
终

公 协 方言协议 则
    法 名称（）：文；
    法 占位符（序号：数）：文；
    法 引名（名称：文）：文；
    法 分页（SQL：文，限量：数，偏移：数）：文；
    法 布尔字面（值：理）：文；
终

公 协 查询结果协议 则
    法 全部（）：列；
    法 首行（）；
    法 标量（）；
    法 数量（）：数；
终

公 协 预编译协议 则
    法 执行（参数：列）；
    法 查询（参数：列）；
终

公 协 事务协议 则
    法 执行（SQL：文，参数：列）；
    法 查询（SQL：文，参数：列）；
    法 提交（）：空；
    法 回滚（）：空；
终

公 协 连接协议 则
    法 执行（SQL：文，参数：列）；
    法 查询（SQL：文，参数：列）；
    法 预编译（SQL：文）；
    法 开始事务（）；
    法 关闭（）：空；
    法 是否关闭（）：理；
终

公 协 连接池协议 则
    法 取得（）；
    法 关闭（）：空；
    法 状态（）：典；
终

公 协 驱动协议 则
    法 名称（）：文；
    法 连接（配置：典）；
终

公 类 数据库错误 则
    公 只 域 代码：文；
    公 只 域 消息：文；
    公 只 域 操作：文；
    公 只 域 SQL：文；
    公 只 域 参数：列；
    公 只 域 驱动：文；
    公 只 域 原因：典；
    公 法 初始化（代码：文，消息：文，操作：文，SQL：文，参数：列，驱动：文，原因：典） 则
        置 此.代码 为 代码；
        置 此.消息 为 消息；
        置 此.操作 为 操作；
        置 此.SQL 为 SQL；
        置 此.参数 为 复制列（参数）；
        置 此.驱动 为 驱动；
        置 此.原因 为 复制典（原因）；
    终
    公 法 转典（）：典 则
        归 {「代码」：此.代码，「消息」：此.消息，「操作」：此.操作，「SQL」：此.SQL，「参数」：复制列（此.参数），「驱动」：此.驱动，「原因」：复制典（此.原因）}；
    终
    公 法 抛出（）：空 则
        抛 （（（「YANKU_」 加 此.代码） 加 「：」） 加 JSON.序列化（此.转典（）））；
    终
终

法 新错误（代码：文，消息：文，操作：文，SQL：文，参数：列，驱动：文）：数据库错误 则
    归 数据库错误（代码，消息，操作，SQL，参数，驱动，{}）；
终

公 类 数据库方言 纳 方言协议 则
    私 域 名称值：文；
    私 域 占位风格：文；
    私 域 引号：文；
    私 域 真值：文；
    私 域 假值：文；
    公 法 初始化（名称：文，占位风格：文，引号：文，真值：文，假值：文） 则
        置 此.名称值 为 名称；
        置 此.占位风格 为 占位风格；
        置 此.引号 为 引号；
        置 此.真值 为 真值；
        置 此.假值 为 假值；
    终
    公 法 名称（）：文 则
        归 此.名称值；
    终
    公 法 占位符（序号：数）：文 则
        若 （序号 小于 1） 则
            抛 「YANKU_PLACEHOLDER：占位序号从一开始」；
        终
        若 （此.占位风格 等于 「问号」） 则
            归 「?」；
        终
        若 （此.占位风格 等于 「编号」） 则
            归 （「$」 加 整数文（序号））；
        终
        若 （此.占位风格 等于 「百分号」） 则
            归 「%s」；
        终
        抛 「YANKU_DIALECT：未知占位风格」；
    终
    公 法 引名（名称：文）：文 则
        若 （文字.修剪（名称） 等于 「」） 则
            抛 「YANKU_IDENTIFIER：标识名不可为空」；
        终
        定 各段：列<文> 为 文字.分割（名称，「.」）；
        令 已引：列<文> 为 【】；
        逐 段：文 于 各段 则
            若 （段 等于 「*」） 则
                追加（已引，「*」）；
            否则
                若 （段 等于 「」） 则
                    抛 「YANKU_IDENTIFIER：标识名不可含空段」；
                终
                定 已逃：文 为 文字.替换（段，此.引号，（此.引号 加 此.引号））；
                追加（已引，（（此.引号 加 已逃） 加 此.引号））；
            终
        终
        归 文字.联结（已引，「.」）；
    终
    公 法 分页（SQL：文，限量：数，偏移：数）：文 则
        若 （限量 小于 0） 则
            抛 「YANKU_PAGE：限量不可为负数」；
        终
        若 （偏移 小于 0） 则
            抛 「YANKU_PAGE：偏移不可为负数」；
        终
        归 （（（（SQL 加 「 LIMIT 」） 加 整数文（限量）） 加 「 OFFSET 」） 加 整数文（偏移））；
    终
    公 法 布尔字面（值：理）：文 则
        若 值 则
            归 此.真值；
        终
        归 此.假值；
    终
终

公 法 SQLite方言（）：数据库方言 则
    归 数据库方言（「sqlite」，「问号」，「"」，「1」，「0」）；
终

公 法 PostgreSQL方言（）：数据库方言 则
    归 数据库方言（「postgresql」，「编号」，「"」，「TRUE」，「FALSE」）；
终

公 法 MySQL方言（）：数据库方言 则
    归 数据库方言（「mysql」，「百分号」，「`」，「TRUE」，「FALSE」）；
终

公 类 参数表 则
    私 域 方言值：方言协议；
    私 域 参数列：列；
    公 法 初始化（方言：方言协议） 则
        置 此.方言值 为 方言；
        置 此.参数列 为 【】；
    终
    公 法 加入（值）：文 则
        追加（此.参数列，值）；
        归 此.方言值.占位符（长度（此.参数列））；
    终
    公 法 加入列（各值：列）：列<文> 则
        令 各占位：列<文> 为 【】；
        逐 值 于 各值 则
            追加（各占位，此.加入（值））；
        终
        归 各占位；
    终
    公 法 参数（）：列 则
        归 复制列（此.参数列）；
    终
    公 法 清空（）：参数表 则
        置 此.参数列 为 【】；
        归 此；
    终
终

公 类 查询结果 纳 查询结果协议 则
    公 只 域 列名：列<文>；
    公 只 域 行列：列；
    公 只 域 影响行数：数；
    公 只 域 末插入号：任意；
    公 只 域 元数据：典；
    公 法 初始化（列名：列<文>，行列：列，影响行数：数，末插入号，元数据：典） 则
        置 此.列名 为 复制列（列名）；
        置 此.行列 为 复制列（行列）；
        置 此.影响行数 为 影响行数；
        置 此.末插入号 为 末插入号；
        置 此.元数据 为 复制典（元数据）；
    终
    公 法 全部（）：列 则
        归 复制列（此.行列）；
    终
    公 法 首行（） 则
        若 （长度（此.行列） 等于 0） 则
            归 空；
        终
        归 此.行列【0】；
    终
    公 法 标量（） 则
        定 首项 为 此.首行（）；
        若 （首项 等于 空） 则
            归 空；
        终
        若 （首项 是 典） 则
            定 各键：列 为 键列（首项）；
            若 （长度（各键） 等于 0） 则
                归 空；
            终
            归 首项【各键【0】】；
        终
        若 （首项 是 列） 则
            若 （长度（首项） 等于 0） 则
                归 空；
            终
            归 首项【0】；
        终
        归 首项；
    终
    公 法 数量（）：数 则
        归 长度（此.行列）；
    终
    公 法 是否为空（）：理 则
        归 （长度（此.行列） 等于 0）；
    终
    公 法 映射（变换：法）：列 则
        令 结果：列 为 【】；
        逐 行 于 此.行列 则
            追加（结果，变换（行））；
        终
        归 结果；
    终
    公 法 转典（）：典 则
        归 {「列名」：复制列（此.列名），「行」：复制列（此.行列），「影响行数」：此.影响行数，「末插入号」：此.末插入号，「元数据」：复制典（此.元数据）}；
    终
终

法 响应结果（响应）：查询结果 则
    若 （响应 是 查询结果） 则
        归 响应；
    终
    若 非 （响应 是 典） 则
        抛 「YANKU_RESPONSE：执行器必须返回查询结果或典」；
    终
    令 列名：列<文> 为 【】；
    令 行：列 为 【】；
    令 影响：数 为 0；
    令 末号 为 空；
    令 元数据：典 为 {}；
    若 有所键（响应，「列名」） 则
        置 列名 为 响应【「列名」】；
    终
    若 有所键（响应，「行」） 则
        置 行 为 响应【「行」】；
    终
    若 有所键（响应，「影响行数」） 则
        置 影响 为 响应【「影响行数」】；
    终
    若 有所键（响应，「末插入号」） 则
        置 末号 为 响应【「末插入号」】；
    终
    若 有所键（响应，「元数据」） 则
        置 元数据 为 响应【「元数据」】；
    终
    归 查询结果（列名，行，影响，末号，元数据）；
终

公 类 预编译语句 纳 预编译协议 则
    公 只 域 SQL：文；
    私 域 连接值：任意；
    公 法 初始化（连接，SQL：文） 则
        置 此.连接值 为 连接；
        置 此.SQL 为 SQL；
    终
    公 法 执行（参数：列） 则
        归 此.连接值.执行（此.SQL，参数）；
    终
    公 法 查询（参数：列） 则
        归 此.连接值.查询（此.SQL，参数）；
    终
    公 法 安全执行（参数：列）：典 则
        归 此.连接值.安全执行（此.SQL，参数）；
    终
终

公 类 事务 纳 事务协议 则
    私 域 连接值：任意；
    私 域 标记：文；
    私 域 深度：数；
    私 域 活跃：理；
    公 法 初始化（连接，标记：文，深度：数） 则
        置 此.连接值 为 连接；
        置 此.标记 为 标记；
        置 此.深度 为 深度；
        置 此.活跃 为 真；
    终
    私 法 确保活跃（）：空 则
        若 非 此.活跃 则
            抛 「YANKU_TRANSACTION_STATE：事务已经结束」；
        终
        归 空；
    终
    公 法 执行（SQL：文，参数：列） 则
        此.确保活跃（）；
        归 此.连接值.执行（SQL，参数）；
    终
    公 法 查询（SQL：文，参数：列） 则
        此.确保活跃（）；
        归 此.连接值.查询（SQL，参数）；
    终
    公 法 预编译（SQL：文）：预编译语句 则
        此.确保活跃（）；
        归 预编译语句（此，SQL）；
    终
    公 法 保存点（）：事务 则
        此.确保活跃（）；
        归 此.连接值.开始事务（）；
    终
    公 法 提交（）：空 则
        此.确保活跃（）；
        此.连接值.结束事务（此.标记，此.深度，真）；
        置 此.活跃 为 假；
        归 空；
    终
    公 法 回滚（）：空 则
        此.确保活跃（）；
        此.连接值.结束事务（此.标记，此.深度，假）；
        置 此.活跃 为 假；
        归 空；
    终
    公 法 是否活跃（）：理 则
        归 此.活跃；
    终
终

公 类 函数连接 纳 连接协议 则
    私 域 驱动名：文；
    私 域 方言值：方言协议；
    私 域 执行器：法；
    私 域 已关闭：理；
    私 域 事务深度：数；
    公 法 初始化（驱动名：文，方言：方言协议，执行器：法） 则
        置 此.驱动名 为 驱动名；
        置 此.方言值 为 方言；
        置 此.执行器 为 执行器；
        置 此.已关闭 为 假；
        置 此.事务深度 为 0；
    终
    公 法 方言（）：方言协议 则
        归 此.方言值；
    终
    公 法 名称（）：文 则
        归 此.驱动名；
    终
    私 法 确保开放（）：空 则
        若 此.已关闭 则
            新错误（「CONNECTION_CLOSED」，「连接已经关闭」，「连接」，「」，【】，此.驱动名）.抛出（）；
        终
        归 空；
    终
    私 法 调用（动作：文，SQL：文，参数：列）：查询结果 则
        此.确保开放（）；
        定 请求：典 为 {「动作」：动作，「SQL」：SQL，「参数」：复制列（参数），「驱动」：此.驱动名}；
        试 则
            定 响应 为 此.执行器（请求）；
            若 （响应 是 典） 则
                若 （有所键（响应，「成功」） 且 非 响应【「成功」】） 则
                    令 代码：文 为 「DRIVER」；
                    令 消息：文 为 「驱动执行失败」；
                    若 有所键（响应，「代码」） 则
                        置 代码 为 响应【「代码」】；
                    终
                    若 有所键（响应，「消息」） 则
                        置 消息 为 响应【「消息」】；
                    终
                    新错误（代码，消息，动作，SQL，参数，此.驱动名）.抛出（）；
                终
            终
            归 响应结果（响应）；
        救 所误 则
            定 原因：典 为 {「代码」：所误.代码，「类别」：所误.类别，「消息」：所误.消息，「踪迹」：所误.踪迹}；
            数据库错误（「EXECUTION」，所误.消息，动作，SQL，参数，此.驱动名，原因）.抛出（）；
        终
    终
    公 法 执行（SQL：文，参数：列） 则
        归 此.调用（「执行」，SQL，参数）；
    终
    公 法 查询（SQL：文，参数：列） 则
        归 此.调用（「查询」，SQL，参数）；
    终
    公 法 安全执行（SQL：文，参数：列）：典 则
        试 则
            归 {「成功」：真，「结果」：此.执行（SQL，参数）.转典（），「错误」：空}；
        救 所误 则
            归 {「成功」：假，「结果」：空，「错误」：{「代码」：所误.代码，「类别」：所误.类别，「消息」：所误.消息，「踪迹」：所误.踪迹}}；
        终
    终
    公 法 预编译（SQL：文） 则
        此.确保开放（）；
        归 预编译语句（此，SQL）；
    终
    公 法 开始事务（） 则
        此.确保开放（）；
        置 此.事务深度 为 （此.事务深度 加 1）；
        令 标记：文 为 「」；
        若 （此.事务深度 等于 1） 则
            此.调用（「事务」，「BEGIN」，【】）；
        否则
            置 标记 为 （「yanxu_」 加 整数文（此.事务深度））；
            此.调用（「保存点」，（「SAVEPOINT 」 加 标记），【】）；
        终
        归 事务（此，标记，此.事务深度）；
    终
    公 法 结束事务（标记：文，深度：数，提交：理）：空 则
        此.确保开放（）；
        若 （深度 不等于 此.事务深度） 则
            新错误（「TRANSACTION_ORDER」，「事务必须按后进先出顺序结束」，「事务」，「」，【】，此.驱动名）.抛出（）；
        终
        若 （深度 等于 1） 则
            若 提交 则
                此.调用（「提交」，「COMMIT」，【】）；
            否则
                此.调用（「回滚」，「ROLLBACK」，【】）；
            终
        否则
            若 提交 则
                此.调用（「释放保存点」，（「RELEASE SAVEPOINT 」 加 标记），【】）；
            否则
                此.调用（「回滚保存点」，（（（「ROLLBACK TO SAVEPOINT 」 加 标记） 加 「; RELEASE SAVEPOINT 」） 加 标记），【】）；
            终
        终
        置 此.事务深度 为 （此.事务深度 减 1）；
        归 空；
    终
    公 法 是否关闭（）：理 则
        归 此.已关闭；
    终
    公 法 关闭（）：空 则
        若 非 此.已关闭 则
            若 （此.事务深度 大于 0） 则
                此.调用（「回滚」，「ROLLBACK」，【】）；
                置 此.事务深度 为 0；
            终
            此.执行器（{「动作」：「关闭」，「SQL」：「」，「参数」：【】，「驱动」：此.驱动名}）；
            置 此.已关闭 为 真；
        终
        归 空；
    终
终

公 类 驱动适配器 纳 驱动协议 则
    私 域 名称值：文；
    私 域 方言值：方言协议；
    私 域 执行器工厂：法；
    公 法 初始化（名称：文，方言：方言协议，执行器工厂：法） 则
        置 此.名称值 为 名称；
        置 此.方言值 为 方言；
        置 此.执行器工厂 为 执行器工厂；
    终
    公 法 名称（）：文 则
        归 此.名称值；
    终
    公 法 方言（）：方言协议 则
        归 此.方言值；
    终
    公 法 连接（配置：典） 则
        定 执行器：法 为 此.执行器工厂（配置）；
        归 函数连接（此.名称值，此.方言值，执行器）；
    终
终

公 法 SQLite驱动（执行器工厂：法）：驱动适配器 则
    归 驱动适配器（「sqlite」，SQLite方言（），执行器工厂）；
终

公 法 PostgreSQL驱动（执行器工厂：法）：驱动适配器 则
    归 驱动适配器（「postgresql」，PostgreSQL方言（），执行器工厂）；
终

公 法 MySQL驱动（执行器工厂：法）：驱动适配器 则
    归 驱动适配器（「mysql」，MySQL方言（），执行器工厂）；
终

公 类 租用连接 则
    私 域 池值：任意；
    私 域 连接值：连接协议；
    私 域 已释放：理；
    公 法 初始化（池，连接：连接协议） 则
        置 此.池值 为 池；
        置 此.连接值 为 连接；
        置 此.已释放 为 假；
    终
    私 法 确保租用（）：空 则
        若 此.已释放 则
            抛 「YANKU_LEASE：连接已经归还」；
        终
        归 空；
    终
    公 法 连接（）：连接协议 则
        此.确保租用（）；
        归 此.连接值；
    终
    公 法 执行（SQL：文，参数：列） 则
        归 此.连接（）.执行（SQL，参数）；
    终
    公 法 查询（SQL：文，参数：列） 则
        归 此.连接（）.查询（SQL，参数）；
    终
    公 法 开始事务（） 则
        归 此.连接（）.开始事务（）；
    终
    公 法 释放（）：空 则
        若 非 此.已释放 则
            此.池值.归还（此.连接值）；
            置 此.已释放 为 真；
        终
        归 空；
    终
终

公 类 连接池 纳 连接池协议 则
    私 域 连接工厂：法；
    私 域 最大连接：数；
    私 域 空闲列：列<连接协议>；
    私 域 已借数：数；
    私 域 已关闭：理；
    公 法 初始化（连接工厂：法，最大连接：数） 则
        若 （最大连接 小于 1） 则
            抛 「YANKU_POOL_SIZE：最大连接须为正数」；
        终
        置 此.连接工厂 为 连接工厂；
        置 此.最大连接 为 最大连接；
        置 此.空闲列 为 【】；
        置 此.已借数 为 0；
        置 此.已关闭 为 假；
    终
    公 法 取得（） 则
        若 此.已关闭 则
            抛 「YANKU_POOL_CLOSED：连接池已经关闭」；
        终
        令 所连：任意 为 空；
        若 （长度（此.空闲列） 大于 0） 则
            置 所连 为 弹出（此.空闲列）；
        否则
            若 （此.已借数 不小于 此.最大连接） 则
                抛 「YANKU_POOL_EXHAUSTED：连接池已耗尽」；
            终
            置 所连 为 此.连接工厂（）；
        终
        若 非 （所连 是 连接协议） 则
            抛 「YANKU_POOL_FACTORY：连接工厂未返回连接协议实例」；
        终
        置 此.已借数 为 （此.已借数 加 1）；
        归 租用连接（此，所连）；
    终
    公 法 归还（连接：连接协议）：空 则
        若 （此.已借数 小于 1） 则
            抛 「YANKU_POOL_STATE：归还数超过借出数」；
        终
        置 此.已借数 为 （此.已借数 减 1）；
        若 此.已关闭 则
            连接.关闭（）；
        否则
            若 连接.是否关闭（） 则
                归 空；
            终
            追加（此.空闲列，连接）；
        终
        归 空；
    终
    公 法 状态（）：典 则
        归 {「最大」：此.最大连接，「空闲」：长度（此.空闲列），「借出」：此.已借数，「已关闭」：此.已关闭}；
    终
    公 法 关闭（）：空 则
        若 非 此.已关闭 则
            逐 连接：连接协议 于 此.空闲列 则
                连接.关闭（）；
            终
            置 此.空闲列 为 【】；
            置 此.已关闭 为 真；
        终
        归 空；
    终
终
