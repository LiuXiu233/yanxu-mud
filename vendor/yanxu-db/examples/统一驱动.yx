引「包:言库」为 言库；

法 执行器工厂（配置：典）：法 则
    法 执行器（请求：典）：典 则
        归 {「成功」：真，「列名」：【「问候」】，「行」：【{「问候」：「你好，言库」}】，「影响行数」：0}；
    终
    归 执行器；
终

定 驱动 为 言库.PostgreSQL驱动（执行器工厂）；

定 连接 为 驱动.连接（{「地址」：「postgres://localhost/示例」}）；

言 连接.查询（「SELECT $1 AS 问候」，【「你好」】）.首行（）；

连接.关闭（）；
