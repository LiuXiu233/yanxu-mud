引「../src/言时.yx」为 言时；

引「标准:测试」为 测试；

定 时钟 为 言时.虚拟时钟（1000）；

言时.等待（言时.毫秒段（250），时钟）；

测试.相等（时钟.毫秒（），1250）；

法 所作（）：文 则
    时钟.推进（75）；
    归 「完成」；
终

定 计时结果：典 为 言时.计时（所作，时钟）；

测试.相等（计时结果【「值」】，「完成」）；

测试.相等（计时结果【「耗时毫秒」】，75）；

法 限时作业（截止）：文 则
    时钟.推进（20）；
    截止.检查（）；
    归 「及时」；
终

测试.相等（言时.限时（言时.毫秒段（30），限时作业，时钟），「及时」）；

定 一次 为 言时.一次定时器（言时.毫秒段（100），时钟）；

测试.相等（一次.剩余（）.总毫秒，100）；

时钟.推进（99）；

测试.断言（非 一次.是否到期（），「一次定时器不应提前到期」）；

时钟.推进（1）；

测试.断言（一次.触发（），「一次定时器应在截止点触发」）；

测试.断言（非 一次.是否活跃（），「一次定时器触发后应停止」）；

一次.重置（）.等待（）；

测试.断言（非 一次.是否活跃（），「等待完成后一次定时器应停止」）；

定 重复 为 言时.重复定时器（言时.毫秒段（50），时钟）；

时钟.推进（125）；

测试.断言（重复.触发（），「重复定时器应触发并跨过错过的周期」）；

测试.相等（重复.剩余（）.总毫秒，25）；

重复.等待（）；

测试.断言（重复.是否活跃（），「重复定时器等待后仍应活跃」）；

重复.取消（）；

测试.断言（非 重复.是否活跃（），「取消后定时器应停止」）；

定 甲 为 言时.解析RFC3339（「2026-07-15T00:00:00Z」）；

定 乙 为 甲.加时（2）；

定 丙 为 甲.加时（1）；

定 已排 为 言时.排序日期时间（【乙，甲，丙】，真）；

测试.相等（已排【0】.Unix毫秒（），甲.Unix毫秒（））；

测试.相等（已排【2】.Unix毫秒（），乙.Unix毫秒（））；

测试.相等（言时.相对时间（甲.加分（5），甲），「5分钟后」）；
