引「../src/言验.yx」为 言验；

引「标准:测试」为 测试；

定 用户规则 为 言验.对象型（{「姓名」：言验.文本（）.至少（2）.至多（20），「年龄」：言验.整数（）.至少（0）.至多（150），「邮箱」：言验.文本（）.是邮件（）.可选（），「角色」：言验.枚举（【「读者」，「作者」】）.默认（「读者」）}）.拒绝未知（）；

定 正确 为 用户规则.安全解析（{「姓名」：「子衿」，「年龄」：18}）；

测试.断言（正确.成功，「正确用户应通过」）；

测试.相等（正确.值【「角色」】，「读者」）；

定 错误 为 用户规则.安全解析（{「姓名」：「甲」，「年龄」：18.5，「邮箱」：「坏地址」，「多余」：真}）；

测试.断言（非 错误.成功，「多个错误应一起返回」）；

测试.相等（长度（错误.问题列），4）；

测试.相等（错误.问题列【0】【「路径」】，「$.姓名」）；

定 可空文字 为 言验.文本（）.可空（）；

测试.断言（可空文字.安全解析（空）.成功，「可空规则接受空」）；

定 严格文字 为 言验.文本（）；

测试.断言（非 严格文字.安全解析（空）.成功，「普通规则拒绝空」）；
