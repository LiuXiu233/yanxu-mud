引「../src/言验.yx」为 言验；

引「标准:测试」为 测试；

引「标准:文字」为 文字；

定 标签规则 为 言验.列表（言验.文本（）.至少（1））.至少（1）.至多（3）；

定 标签结果 为 标签规则.安全解析（【「言序」，「中文」】）；

测试.断言（标签结果.成功，「标签列应通过」）；

定 坏标签 为 标签规则.安全解析（【「」】）；

测试.断言（非 坏标签.成功，「空标签应失败」）；

测试.相等（坏标签.问题列【0】【「路径」】，「$[0]」）；

定 标识规则 为 言验.联合（【言验.整数（）.至少（1），言验.文本（）.匹配（「^[a-z]+$」）】）；

测试.断言（标识规则.安全解析（7）.成功，「正整数分支应通过」）；

测试.断言（标识规则.安全解析（「yanxu」）.成功，「文字分支应通过」）；

测试.断言（非 标识规则.安全解析（「123」）.成功，「两个分支均不接受」）；

定 数字结果 为 言验.数字自文（）.至少（10）.安全解析（「42」）；

测试.断言（数字结果.成功，「数值文字应被转换」）；

测试.相等（数字结果.值，42）；

法 大写（值：文）：文 则
    归 文字.大写（值）；
终

定 已转 为 言验.文本（）.转化（大写）.解析（「yanxu」）；

测试.相等（已转，「YANXU」）；
