引「标准:JSON」为 JSON；

引「标准:数学」为 数学；

引「标准:文字」为 文字；

引「标准:正则」为 正则；

引「标准:校验」为 标准校验；

引「标准:URL」为 URL；

引「标准:日期」为 日期；

公 定 版本：文 为 「0.1.0」；

法 有所键（所典：典，键）：理 则
    归 包含（键列（所典），键）；
终

法 复制列（来源：列）：列 则
    令 结果：列 为 【】；
    逐 项 于 来源 则
        追加（结果，项）；
    终
    归 结果；
终

法 复制典（来源：典）：典 则
    令 结果：典 为 {}；
    逐 键 于 来源 则
        置 结果【键】 为 来源【键】；
    终
    归 结果；
终

法 问题（路径：文，代码：文，消息：文，期望：文，实际）：典 则
    归 {「路径」：路径，「代码」：代码，「消息」：消息，「期望」：期望，「实际类型」：类型（实际）}；
终

法 子键路径（路径：文，键）：文 则
    若 （键 是 文） 则
        归 （（路径 加 「.」） 加 键）；
    终
    归 （（（路径 加 「[」） 加 JSON.序列化（键）） 加 「]」）；
终

法 子位路径（路径：文，位：数）：文 则
    定 位文：文 为 文字.替换（JSON.序列化（位），「.0」，「」）；
    归 （（（路径 加 「[」） 加 位文） 加 「]」）；
终

公 类 验证结果 则
    公 只 域 成功：理；
    公 只 域 值：任意；
    公 只 域 问题列：列；
    公 法 初始化（成功：理，值，问题列：列） 则
        置 此.成功 为 成功；
        置 此.值 为 值；
        置 此.问题列 为 问题列；
    终
    公 法 是否成功（）：理 则
        归 此.成功；
    终
    公 法 首项问题（）：典? 则
        若 （长度（此.问题列） 等于 0） 则
            归 空；
        终
        归 此.问题列【0】；
    终
    公 法 转典（）：典 则
        若 此.成功 则
            归 {「成功」：真，「值」：此.值，「问题」：【】}；
        终
        归 {「成功」：假，「值」：空，「问题」：复制列（此.问题列）}；
    终
终

法 通过（值）：验证结果 则
    归 验证结果（真，值，【】）；
终

法 未过（各问题：列）：验证结果 则
    归 验证结果（假，空，各问题）；
终

法 单错（路径：文，代码：文，消息：文，期望：文，实际）：验证结果 则
    归 未过（【问题（路径，代码，消息，期望，实际）】）；
终

法 恒等（值） 则
    归 值；
终

公 类 规则 则
    私 域 名称值：文；
    私 域 基础检查：法；
    私 域 约束列：列<法>；
    私 域 预处理法：法?；
    私 域 转化法：法?；
    私 域 可空值：理；
    私 域 可选值：理；
    私 域 有默认值：理；
    私 域 默认内容：任意；
    私 域 配置值：典；
    公 法 初始化（名称：文，基础检查：法，配置：典） 则
        置 此.名称值 为 名称；
        置 此.基础检查 为 基础检查；
        置 此.约束列 为 【】；
        置 此.预处理法 为 空；
        置 此.转化法 为 空；
        置 此.可空值 为 假；
        置 此.可选值 为 假；
        置 此.有默认值 为 假；
        置 此.默认内容 为 空；
        置 此.配置值 为 配置；
    终
    公 法 名称（）：文 则
        归 此.名称值；
    终
    公 法 是否可空（）：理 则
        归 此.可空值；
    终
    公 法 是否可选（）：理 则
        归 此.可选值；
    终
    公 法 是否有默认（）：理 则
        归 此.有默认值；
    终
    公 法 取默认（） 则
        归 此.默认内容；
    终
    公 法 配置（）：典 则
        归 此.配置值；
    终
    公 法 可空（）：规则 则
        置 此.可空值 为 真；
        归 此；
    终
    公 法 可选（）：规则 则
        置 此.可选值 为 真；
        归 此；
    终
    公 法 默认（值）：规则 则
        置 此.有默认值 为 真；
        置 此.默认内容 为 值；
        置 此.可选值 为 真；
        归 此；
    终
    公 法 预处理（处理：法）：规则 则
        置 此.预处理法 为 处理；
        归 此；
    终
    公 法 转化（处理：法）：规则 则
        置 此.转化法 为 处理；
        归 此；
    终
    私 法 加约束（检查：法）：规则 则
        追加（此.约束列，检查）；
        归 此；
    终
    公 法 至少（下界：数）：规则 则
        法 检查（值，路径：文）：典? 则
            若 （值 是 数） 则
                若 （值 小于 下界） 则
                    归 问题（路径，「MIN」，「数值小于允许下界」，JSON.序列化（下界），值）；
                终
                归 空；
            终
            若 （（（值 是 文） 或 （值 是 列）） 或 （值 是 典）） 则
                若 （长度（值） 小于 下界） 则
                    归 问题（路径，「MIN_LENGTH」，「长度小于允许下界」，JSON.序列化（下界），值）；
                终
                归 空；
            终
            归 问题（路径，「CONSTRAINT_TYPE」，「此值不支持至少约束」，「数、文、列或典」，值）；
        终
        归 此.加约束（检查）；
    终
    公 法 至多（上界：数）：规则 则
        法 检查（值，路径：文）：典? 则
            若 （值 是 数） 则
                若 （值 大于 上界） 则
                    归 问题（路径，「MAX」，「数值大于允许上界」，JSON.序列化（上界），值）；
                终
                归 空；
            终
            若 （（（值 是 文） 或 （值 是 列）） 或 （值 是 典）） 则
                若 （长度（值） 大于 上界） 则
                    归 问题（路径，「MAX_LENGTH」，「长度大于允许上界」，JSON.序列化（上界），值）；
                终
                归 空；
            终
            归 问题（路径，「CONSTRAINT_TYPE」，「此值不支持至多约束」，「数、文、列或典」，值）；
        终
        归 此.加约束（检查）；
    终
    公 法 匹配（模式：文）：规则 则
        法 检查（值，路径：文）：典? 则
            若 非 （值 是 文） 则
                归 问题（路径，「PATTERN_TYPE」，「模式约束只适用于文字」，「文」，值）；
            终
            若 非 正则.匹配（模式，值） 则
                归 问题（路径，「PATTERN」，「文字不符合指定模式」，模式，值）；
            终
            归 空；
        终
        归 此.加约束（检查）；
    终
    公 法 是邮件（）：规则 则
        法 检查（值，路径：文）：典? 则
            若 （非 （值 是 文） 或 非 标准校验.电子邮件（值）） 则
                归 问题（路径，「EMAIL」，「不是受支持的电子邮件地址」，「电子邮件」，值）；
            终
            归 空；
        终
        归 此.加约束（检查）；
    终
    公 法 是网址（）：规则 则
        法 检查（值，路径：文）：典? 则
            若 （非 （值 是 文） 或 非 URL.是否合法（值）） 则
                归 问题（路径，「URL」，「不是合法网址」，「URL」，值）；
            终
            归 空；
        终
        归 此.加约束（检查）；
    终
    公 法 是日期（）：规则 则
        法 检查（值，路径：文）：典? 则
            若 （非 （值 是 文） 或 非 日期.是否合法（值）） 则
                归 问题（路径，「DATE」，「不是合法公历日期」，「YYYY-MM-DD」，值）；
            终
            归 空；
        终
        归 此.加约束（检查）；
    终
    公 法 其中（候选：列）：规则 则
        法 检查（值，路径：文）：典? 则
            若 非 包含（候选，值） 则
                归 问题（路径，「ENUM」，「值不在允许集合中」，JSON.序列化（候选），值）；
            终
            归 空；
        终
        归 此.加约束（检查）；
    终
    公 法 自定义（判定：法，代码：文，消息：文）：规则 则
        法 检查（值，路径：文）：典? 则
            若 非 判定（值） 则
                归 问题（路径，代码，消息，「自定义约束」，值）；
            终
            归 空；
        终
        归 此.加约束（检查）；
    终
    公 法 保留未知（）：规则 则
        置 此.配置值【「未知键」】 为 「保留」；
        归 此；
    终
    公 法 拒绝未知（）：规则 则
        置 此.配置值【「未知键」】 为 「拒绝」；
        归 此；
    终
    公 法 移除未知（）：规则 则
        置 此.配置值【「未知键」】 为 「移除」；
        归 此；
    终
    公 法 验证于（值，路径：文）：验证结果 则
        令 当前 为 值；
        若 （此.预处理法 是 法） 则
            试 则
                置 当前 为 此.预处理法（当前）；
            救 所误 则
                归 单错（路径，「PREPROCESS」，所误.消息，此.名称值，值）；
            终
        终
        若 （当前 等于 空） 则
            若 此.可空值 则
                归 通过（空）；
            终
            归 单错（路径，「NULL」，「此处不可为空」，此.名称值，当前）；
        终
        定 基础：验证结果 为 此.基础检查（当前，路径）；
        若 非 基础.成功 则
            归 基础；
        终
        置 当前 为 基础.值；
        令 各问题：列 为 【】；
        逐 检查：法 于 此.约束列 则
            定 所问题：典? 为 检查（当前，路径）；
            若 （所问题 是 典） 则
                追加（各问题，所问题）；
            终
        终
        若 （长度（各问题） 大于 0） 则
            归 未过（各问题）；
        终
        若 （此.转化法 是 法） 则
            试 则
                置 当前 为 此.转化法（当前）；
            救 所误 则
                归 单错（路径，「TRANSFORM」，所误.消息，「可转化的值」，当前）；
            终
        终
        归 通过（当前）；
    终
    公 法 安全解析（值）：验证结果 则
        归 此.验证于（值，「$」）；
    终
    公 法 解析（值） 则
        定 结果：验证结果 为 此.安全解析（值）；
        若 非 结果.成功 则
            定 首项：典 为 结果.问题列【0】；
            抛 （（（「YANYAN_」 加 首项【「代码」】） 加 「：」） 加 JSON.序列化（首项））；
        终
        归 结果.值；
    终
终

法 检查任意（值，路径：文）：验证结果 则
    归 通过（值）；
终

法 检查文字（值，路径：文）：验证结果 则
    若 （值 是 文） 则
        归 通过（值）；
    终
    归 单错（路径，「TYPE_TEXT」，「需要文字」，「文」，值）；
终

法 检查数字（值，路径：文）：验证结果 则
    若 （值 是 数） 则
        归 通过（值）；
    终
    归 单错（路径，「TYPE_NUMBER」，「需要数值」，「数」，值）；
终

法 检查整数（值，路径：文）：验证结果 则
    若 （值 是 数） 则
        若 （数学.下取整（值） 等于 值） 则
            归 通过（值）；
        终
        归 单错（路径，「INTEGER」，「需要整数」，「整数」，值）；
    终
    归 单错（路径，「TYPE_NUMBER」，「需要整数数值」，「数」，值）；
终

法 检查布尔（值，路径：文）：验证结果 则
    若 （值 是 理） 则
        归 通过（值）；
    终
    归 单错（路径，「TYPE_BOOL」，「需要真或假」，「理」，值）；
终

法 检查典（值，路径：文）：验证结果 则
    若 （值 是 典） 则
        归 通过（值）；
    终
    归 单错（路径，「TYPE_OBJECT」，「需要典」，「典」，值）；
终

公 法 任选（）：规则 则
    归 规则（「任意值」，检查任意，{}）；
终

公 法 文本（）：规则 则
    归 规则（「文字」，检查文字，{}）；
终

公 法 数字（）：规则 则
    归 规则（「数值」，检查数字，{}）；
终

公 法 整数（）：规则 则
    归 规则（「整数」，检查整数，{}）；
终

公 法 布尔（）：规则 则
    归 规则（「布尔值」，检查布尔，{}）；
终

公 法 典型（）：规则 则
    归 规则（「典」，检查典，{}）；
终

公 法 字面（期望）：规则 则
    法 检查（值，路径：文）：验证结果 则
        若 （值 等于 期望） 则
            归 通过（值）；
        终
        归 单错（路径，「LITERAL」，「值与指定字面量不同」，JSON.序列化（期望），值）；
    终
    归 规则（「字面量」，检查，{}）；
终

公 法 枚举（候选：列）：规则 则
    若 （长度（候选） 等于 0） 则
        抛 「YANYAN_SCHEMA：枚举候选不可为空」；
    终
    法 检查（值，路径：文）：验证结果 则
        若 包含（候选，值） 则
            归 通过（值）；
        终
        归 单错（路径，「ENUM」，「值不在允许集合中」，JSON.序列化（候选），值）；
    终
    归 规则（「枚举」，检查，{}）；
终

公 法 列表（项规则：规则）：规则 则
    法 检查（值，路径：文）：验证结果 则
        若 非 （值 是 列） 则
            归 单错（路径，「TYPE_LIST」，「需要列」，「列」，值）；
        终
        令 结果列：列 为 【】；
        令 各问题：列 为 【】；
        令 位：数 为 0；
        逐 项 于 值 则
            定 结果：验证结果 为 项规则.验证于（项，子位路径（路径，位））；
            若 结果.成功 则
                追加（结果列，结果.值）；
            否则
                逐 所问题 于 结果.问题列 则
                    追加（各问题，所问题）；
                终
            终
            置 位 为 （位 加 1）；
        终
        若 （长度（各问题） 大于 0） 则
            归 未过（各问题）；
        终
        归 通过（结果列）；
    终
    归 规则（「列表」，检查，{}）；
终

公 法 对象型（形状：典）：规则 则
    逐 键 于 形状 则
        若 非 （形状【键】 是 规则） 则
            抛 （（「YANYAN_SCHEMA：字段“」 加 JSON.序列化（键）） 加 「”不是规则」）；
        终
    终
    定 配置：典 为 {「未知键」：「移除」}；
    法 检查（值，路径：文）：验证结果 则
        若 非 （值 是 典） 则
            归 单错（路径，「TYPE_OBJECT」，「需要典」，「典」，值）；
        终
        令 输出：典 为 {}；
        令 各问题：列 为 【】；
        逐 键 于 形状 则
            定 子规：规则 为 形状【键】；
            若 有所键（值，键） 则
                定 子结果：验证结果 为 子规.验证于（值【键】，子键路径（路径，键））；
                若 子结果.成功 则
                    置 输出【键】 为 子结果.值；
                否则
                    逐 所问题 于 子结果.问题列 则
                        追加（各问题，所问题）；
                    终
                终
            否则
                若 子规.是否有默认（） 则
                    置 输出【键】 为 子规.取默认（）；
                否则
                    若 非 子规.是否可选（） 则
                        追加（各问题，问题（子键路径（路径，键），「REQUIRED」，「缺少必需字段」，子规.名称（），空））；
                    终
                终
            终
        终
        逐 键 于 值 则
            若 非 有所键（形状，键） 则
                若 （配置【「未知键」】 等于 「保留」） 则
                    置 输出【键】 为 值【键】；
                终
                若 （配置【「未知键」】 等于 「拒绝」） 则
                    追加（各问题，问题（子键路径（路径，键），「UNKNOWN_KEY」，「不允许未知字段」，「已声明字段」，值【键】））；
                终
            终
        终
        若 （长度（各问题） 大于 0） 则
            归 未过（各问题）；
        终
        归 通过（输出）；
    终
    归 规则（「对象」，检查，配置）；
终

公 法 联合（各规则：列<规则>）：规则 则
    若 （长度（各规则） 等于 0） 则
        抛 「YANYAN_SCHEMA：联合至少需要一个规则」；
    终
    法 检查（值，路径：文）：验证结果 则
        令 分支问题：列 为 【】；
        逐 所规：规则 于 各规则 则
            定 结果：验证结果 为 所规.验证于（值，路径）；
            若 结果.成功 则
                归 结果；
            终
            追加（分支问题，结果.问题列）；
        终
        定 所问题：典 为 问题（路径，「UNION」，「没有联合分支接受此值」，「联合规则」，值）；
        置 所问题【「分支问题」】 为 分支问题；
        归 未过（【所问题】）；
    终
    归 规则（「联合」，检查，{}）；
终

公 法 数字自文（）：规则 则
    法 检查（值，路径：文）：验证结果 则
        若 （值 是 数） 则
            归 通过（值）；
        终
        若 （值 是 文） 则
            试 则
                定 已解 为 JSON.解析（文字.修剪（值））；
                若 （已解 是 数） 则
                    归 通过（已解）；
                终
            救 所误 则
                归 单错（路径，「COERCE_NUMBER」，「文字不能转为数值」，「数值文字」，值）；
            终
        终
        归 单错（路径，「COERCE_NUMBER」，「值不能转为数值」，「数或数值文字」，值）；
    终
    归 规则（「可转数值」，检查，{}）；
终

公 法 验证（所规：规则，值）：验证结果 则
    归 所规.安全解析（值）；
终

公 法 断验（所规：规则，值） 则
    归 所规.解析（值）；
终
