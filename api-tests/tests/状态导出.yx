引「包:言域/状态」为 状态；

引「包:言域/属性」为 属性；

引「包:言域/内存仓储」为 内存；

引「标准:测试」为 测试；

法 定义解析（编号：文） 则
    归 {「名称」：「测试状态」，「属性」：{「攻击」：1}}；
终

定 定义：典 为 状态.读取状态定义（定义解析，「测试:状态/增益」）；

测试.相等（定义【「属性」】【「攻击」】，1）；

定 聚合器 为 属性.新修正源聚合器（）；

状态.注册状态属性提供器（聚合器，定义解析）；

定 服务 为 状态.状态服务（内存.新仓储（），定义解析，聚合器）；

测试.相等（状态.建立状态系统（服务）.编号，「言域:系统/状态」）；
