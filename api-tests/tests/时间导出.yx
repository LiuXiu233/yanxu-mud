引「包:言域/时间」为 时间；

引「包:言域/内存仓储」为 内存；

引「标准:测试」为 测试；

公 类 时钟 则
    公 法 初始化（） 则
    终
    公 法 毫秒（）：数 则
        归 0；
    终
终

定 注册 为 时间.新任务行为注册表（）；

定 服务 为 时间.时间服务（内存.新仓储（），时钟（），注册）；

测试.相等（服务.毫秒（），0）；

测试.相等（服务.调度器（）.下一到期（），空）；

测试.相等（时间.建立时间系统（服务）.编号，「言域:系统/时间」）；
