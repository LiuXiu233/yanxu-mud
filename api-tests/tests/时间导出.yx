引「包:言域/时间」为 时间；

引「包:言域/内存仓储」为 内存；

引「包:言域/世界事务」为 世界事务；

引「标准:测试」为 测试；

公 类 时钟 则
    公 法 初始化（） 则
    终
    公 法 毫秒（）：数 则
        归 0；
    终
终

法 空行为（事务，任务：典，上下文：典）：典 则
    归 {「数据」：{}}；
终

定 注册 为 时间.新任务行为注册表（）；

注册.注册（「契约:计划行为/空」，空行为）；

定 仓储 为 内存.新仓储（）；

定 服务 为 时间.时间服务（仓储，时钟（），注册）；

测试.相等（服务.毫秒（），0）；

测试.相等（服务.调度器（）.下一到期（），空）；

定 事务 为 世界事务.开始（仓储，「tx-api-time-compose」）；

服务.计划到事务（事务，「契约.任务」，10，0，「契约:计划行为/空」，{}，0）；

事务.提交（）；

测试.相等（服务.读取任务（「契约.任务」）【「到期」】，10）；

定 取消事务 为 世界事务.开始（仓储，「tx-api-time-cancel」）；

服务.取消到事务（取消事务，「契约.任务」，0）；

取消事务.提交（）；

测试.相等（服务.读取任务（「契约.任务」），空）；

测试.相等（时间.建立时间系统（服务）.编号，「言域:系统/时间」）；
