引「包:言域/会话」为 会话；

引「标准:测试」为 测试；

引「标准:哈希」为 哈希；

令 编号序号：数 为 0；

法 生成编号（类别：文）：文 则
    置 编号序号 为 （编号序号 加 1）；
    归 （（「契约:」 加 类别） 加 「/一」）；
终

法 生成令牌（）：文 则
    归 「外部消费者令牌」；
终

法 摘要令牌（令牌：文）：文 则
    归 哈希.SHA256（令牌）；
终

定 服务 为 会话.会话服务（生成编号，生成令牌，摘要令牌，{}）；

服务.打开连接（「api-conn」，「本地测试」，「」，1）；

定 已创建：典 为 服务.创建会话（「api-conn」，「契约:账户/一」，「契约:角色/一」，【「会话恢复」】，2）；

测试.相等（会话.协议名称，「言域/会话」）；

测试.相等（会话.协议版本，1）；

测试.相等（已创建【「会话」】【「状态」】，「已连接」）；

测试.相等（已创建【「恢复令牌」】，「外部消费者令牌」）；
