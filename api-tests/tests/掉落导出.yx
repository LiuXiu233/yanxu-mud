引「包:言域/掉落」为 掉落；

引「标准:测试」为 测试；

法 解析（编号：文） 则
    归 {「名称」：「契约掉落」，「条目」：【{「物品」：「契约:物品/一」，「概率」：1，「最少」：1，「最多」：1}】}；
终

法 随机源（请求：典）：数 则
    归 0；
终

法 编号生成器（上下文：典）：文 则
    归 「契约:物品实例/一」；
终

定 定义：典 为 掉落.读取掉落表定义（解析，「契约:掉落表/一」）；

测试.相等（掉落.抽取掉落（定义，1，随机源）【0】【「物品编号」】，「契约:物品/一」）；

定 服务 为 掉落.掉落服务（空，解析，空，随机源，编号生成器）；

测试.相等（掉落.建立掉落系统（服务）.编号，「言域:系统/掉落」）；
