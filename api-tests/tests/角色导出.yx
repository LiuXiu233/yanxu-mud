引「包:言域/角色」为 角色；

引「包:言域/内存仓储」为 内存；

引「包:言域/实体」为 实体；

引「标准:测试」为 测试；

定 仓储 为 内存.新仓储（）；

定 账户实体 为 实体.新实体（「契约:账户/甲」，「言域:原型/账户」）；

账户实体.添加组件（角色.账户组件编号，{「账户编号」：「契约:账户/甲」，「角色」：【】，「状态」：「正常」}，1）；

仓储.写入实体快照（账户实体.快照（），空）；

定 服务 为 角色.角色服务（仓储，{}）；

定 创建：典 为 服务.创建角色（「契约:账户/甲」，「契约:角色/甲」，「远山」，{「初始房间」：「青石镇:房间/中央街」}，{「事务编号」：「tx-api-character-create」，「世界时间」：1，「现实时间」：1，「操作者编号」：「契约:账户/甲」}）；

测试.相等（创建【「成功」】，真）；

测试.相等（创建【「角色」】【「角色编号」】，「契约:角色/甲」）；

测试.相等（仓储.读取实体（「契约:角色/甲」）.有组件（角色.角色组件编号），真）；

测试.相等（角色.默认配置（）【「最大角色数」】，4）；
