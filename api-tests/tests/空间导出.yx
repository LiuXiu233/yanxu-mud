引「包:言域/空间」为 空间；

引「包:言域/内存仓储」为 内存；

引「标准:测试」为 测试；

法 房间解析（编号：文） 则
    归 {「名称」：「契约房间」，「描述」：「用于外部导出验证。」，「出口」：{「北」：「契约:房间/北侧」}}；
终

定 房间：典 为 空间.读取房间（房间解析，「契约:房间/起点」）；

测试.相等（房间【「名称」】，「契约房间」）；

测试.相等（空间.规范方向（「north」），「北」）；

定 服务 为 空间.空间服务（内存.新仓储（），房间解析）；

测试.相等（空间.建立移动系统（服务）.编号，「言域:系统/移动」）；

测试.相等（空间.建立观察系统（服务）.编号，「言域:系统/观察」）；
