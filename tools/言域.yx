引「../src/言域.yx」为 言域；

引「标准:环境」为 环境；

引「标准:文字」为 文字；

定 参数：列<文> 为 环境.参数（）；

若 （长度（参数） 等于 0） 则
    言 （（言域.名称 加 「 」） 加 言域.版本）；
    言 「用法：言域 <新|检查|运行|测试|构建|内容检查|版本>」；
否则
    定 命令：文 为 文字.小写（参数【0】）；
    若 （（命令 等于 「版本」） 或 （命令 等于 「version」）） 则
        言 （（言域.名称 加 「 」） 加 言域.版本）；
        言 （「言序 >= 」 加 言域.最低言序版本）；
    否则
        言 「YANYU_CLI_001：0.1.0 尚未开放此命令」；
    终
终
