引「../实体/值支持.yx」为 值；

引「./内容包协议.yx」为 内容包；

引「./内容图.yx」为 内容图；

引「./内容文档.yx」为 内容；

引「包:言据」为 言据；

引「标准:文件」为 文件；

引「标准:哈希」为 哈希；

引「标准:路径」为 路径；

引「标准:文字」为 文字；

公 定 世界协议：文 为 「言域/文件内容世界」；

公 定 包协议：文 为 「言域/文件内容包」；

公 定 格式版本：数 为 1；

公 定 清单文件名：文 为 「内容包.yj」；

公 定 内容集协议：文 为 「言域/内容文档集」；

公 定 根目录无效错误：文 为 「YANYU_CONTENT_ROOT_INVALID」；

公 定 文件缺失错误：文 为 「YANYU_CONTENT_FILE_MISSING」；

公 定 文件类型错误：文 为 「YANYU_CONTENT_FILE_TYPE」；

公 定 文件超限错误：文 为 「YANYU_CONTENT_FILE_LIMIT」；

公 定 文件读取错误：文 为 「YANYU_CONTENT_FILE_READ」；

公 定 文件变化错误：文 为 「YANYU_CONTENT_FILE_CHANGED」；

公 定 文件解析错误：文 为 「YANYU_CONTENT_FILE_PARSE」；

公 定 文件形状错误：文 为 「YANYU_CONTENT_FILE_SHAPE」；

公 定 路径冲突错误：文 为 「YANYU_CONTENT_PATH_CONFLICT」；

公 定 包归属错误：文 为 「YANYU_CONTENT_PACKAGE_OWNERSHIP」；

公 定 本地化语言错误：文 为 「YANYU_CONTENT_LOCALIZATION_LANGUAGE」；

公 定 清单字节上限：数 为 1048576；

公 定 单文件字节上限：数 为 4194304；

公 定 总文件数上限：数 为 4096；

公 定 总字节上限：数 为 67108864；

公 定 单文件项目上限：数 为 4096；

法 新预算（）：典 则
    归 {「文件数」：0，「字节数」：0}；
终

法 规范根目录（原路径）：文 则
    若 非 （原路径 是 文） 则
        值.抛错（根目录无效错误，「内容包根目录必须是文字路径」，{}）；
    终
    定 原文：文 为 原路径；
    若 （（原文 等于 「」） 或 （文字.修剪（原文） 不等于 原文）） 则
        值.抛错（根目录无效错误，「内容包根目录不可为空或含首尾空白」，{}）；
    终
    若 （长度（原文） 大于 2048） 则
        值.抛错（根目录无效错误，「内容包根目录路径超过 2048 字符」，{}）；
    终
    定 已规范：文 为 路径.规范化（原文）；
    令 状态：典 为 {}；
    试 则
        置 状态 为 文件.状态（已规范）；
    救 所误 则
        值.抛错（根目录无效错误，「内容包根目录不存在或不可访问」，{}）；
    终
    若 （状态【「种类」】 不等于 「目录」） 则
        值.抛错（根目录无效错误，「内容包根路径必须指向目录」，{}）；
    终
    归 已规范；
终

法 取文件状态（完整路径：文，相对路径：文）：典 则
    令 状态：典 为 {}；
    试 则
        置 状态 为 文件.状态（完整路径）；
    救 所误 则
        值.抛错（文件缺失错误，「内容包声明文件不存在或不可访问」，{「路径」：相对路径}）；
    终
    若 （状态【「种类」】 不等于 「文件」） 则
        值.抛错（文件类型错误，「内容包声明路径必须指向普通文件」，{「路径」：相对路径，「种类」：状态【「种类」】}）；
    终
    归 状态；
终

法 占用预算（预算：典，字节数：数，上限：数，相对路径：文）：空 则
    若 （字节数 大于 上限） 则
        值.抛错（文件超限错误，「内容包单个文件超过字节上限」，{「路径」：相对路径，「字节数」：字节数，「上限」：上限}）；
    终
    若 （预算【「文件数」】 不小于 总文件数上限） 则
        值.抛错（文件超限错误，「内容世界声明文件数量超过上限」，{「上限」：总文件数上限}）；
    终
    若 （（预算【「字节数」】 加 字节数） 大于 总字节上限） 则
        值.抛错（文件超限错误，「内容世界声明文件总字节数超过上限」，{「上限」：总字节上限}）；
    终
    置 预算【「文件数」】 为 （预算【「文件数」】 加 1）；
    置 预算【「字节数」】 为 （预算【「字节数」】 加 字节数）；
    归 空；
终

法 读取文本（根目录：文，相对路径：文，类别：文，语言：文，上限：数，预算：典，来源：列）：文 则
    定 完整路径：文 为 路径.合并（根目录，相对路径）；
    定 读取前：典 为 取文件状态（完整路径，相对路径）；
    定 字节数：数 为 读取前【「字节数」】；
    占用预算（预算，字节数，上限，相对路径）；
    令 原文：文 为 「」；
    试 则
        置 原文 为 文件.读取（完整路径）；
    救 所误 则
        值.抛错（文件读取错误，「内容包文件不是可读取的 UTF-8 普通文件」，{「路径」：相对路径}）；
    终
    定 读取后：典 为 取文件状态（完整路径，相对路径）；
    若 （（读取后【「字节数」】 不等于 读取前【「字节数」】） 或 （读取后【「修改毫秒」】 不等于 读取前【「修改毫秒」】）） 则
        值.抛错（文件变化错误，「内容包文件在读取期间发生变化」，{「路径」：相对路径}）；
    终
    追加（来源，{「类别」：类别，「语言」：语言，「相对路径」：相对路径，「字节数」：字节数，「摘要」：哈希.SHA256（原文）}）；
    归 原文；
终

法 简引用列（所值，文档编号：文，相对路径：文）：列 则
    若 （所值 是 列） 则
        归 值.深复制列（所值）；
    终
    若 非 （所值 是 典） 则
        值.抛错（文件形状错误，「内容文档集引用必须是引用列或路径目标据」，{「路径」：相对路径，「编号」：文档编号}）；
    终
    令 结果：列 为 【】；
    定 映射：典 为 所值；
    逐 引用路径：文 于 排序（键列（映射）） 则
        若 非 （映射【引用路径】 是 文） 则
            值.抛错（文件形状错误，「内容文档集简写引用目标必须是文字」，{「路径」：相对路径，「编号」：文档编号}）；
        终
        定 目标：文 为 映射【引用路径】；
        令 目标段：典 为 {}；
        试 则
            置 目标段 为 内容.内容编号各段（目标）；
        救 所误 则
            值.抛错（文件形状错误，「内容文档集简写引用目标编号无效」，{「路径」：相对路径，「编号」：文档编号}）；
        终
        追加（结果，{「路径」：引用路径，「目标」：目标，「类别」：目标段【「类别」】，「可选」：假}）；
    终
    归 结果；
终

法 项字段或（项目：典，字段：文，默认值） 则
    若 值.有键（项目，字段） 则
        归 值.深复制（项目【字段】）；
    终
    归 值.深复制（默认值）；
终

法 展开内容集（文件集：典，相对路径：文）：列 则
    定 必需字段：列<文> 为 【「协议」，「格式版本」，「包编号」，「类别」，「Schema编号」，「Schema版本」，「文档」】；
    若 （长度（文件集） 不等于 长度（必需字段）） 则
        值.抛错（文件形状错误，「内容文档集字段数量无效」，{「路径」：相对路径}）；
    终
    逐 字段：文 于 必需字段 则
        若 非 值.有键（文件集，字段） 则
            值.抛错（文件形状错误，「内容文档集缺少必需字段」，{「路径」：相对路径，「字段」：字段}）；
        终
    终
    若 （文件集【「格式版本」】 不等于 格式版本） 则
        值.抛错（文件形状错误，「内容文档集格式版本不受支持」，{「路径」：相对路径}）；
    终
    若 （非 （文件集【「包编号」】 是 文） 或 非 （文件集【「类别」】 是 文）） 则
        值.抛错（文件形状错误，「内容文档集包编号或类别类型无效」，{「路径」：相对路径}）；
    终
    若 （非 （文件集【「Schema编号」】 是 文） 或 非 （文件集【「Schema版本」】 是 数）） 则
        值.抛错（文件形状错误，「内容文档集 Schema 声明类型无效」，{「路径」：相对路径}）；
    终
    若 非 （文件集【「文档」】 是 列） 则
        值.抛错（文件形状错误，「内容文档集文档字段必须是列表」，{「路径」：相对路径}）；
    终
    定 各项目：列 为 文件集【「文档」】；
    若 （长度（各项目） 大于 单文件项目上限） 则
        值.抛错（文件超限错误，「内容文档集项目数超过上限」，{「路径」：相对路径，「上限」：单文件项目上限}）；
    终
    定 包编号：文 为 文件集【「包编号」】；
    定 类别：文 为 文件集【「类别」】；
    定 Schema编号：文 为 文件集【「Schema编号」】；
    定 Schema版本：数 为 文件集【「Schema版本」】；
    令 结果：列 为 【】；
    逐 原项目 于 各项目 则
        若 非 （原项目 是 典） 则
            值.抛错（文件形状错误，「内容文档集只能包含据项目」，{「路径」：相对路径}）；
        终
        定 项目：典 为 原项目；
        定 允许字段：列<文> 为 【「标识」，「数据」，「原型」，「引用」，「行为编号」，「本地化键」，「扩展」】；
        逐 字段 于 项目 则
            若 非 包含（允许字段，字段） 则
                值.抛错（文件形状错误，「内容文档集项目含未知字段」，{「路径」：相对路径，「字段」：字段}）；
            终
        终
        若 （非 值.有键（项目，「标识」） 或 非 （项目【「标识」】 是 文）） 则
            值.抛错（文件形状错误，「内容文档集项目必须含文字标识」，{「路径」：相对路径}）；
        终
        若 （非 值.有键（项目，「数据」） 或 非 （项目【「数据」】 是 典）） 则
            值.抛错（文件形状错误，「内容文档集项目必须含数据据」，{「路径」：相对路径，「标识」：项目【「标识」】}）；
        终
        定 标识：文 为 项目【「标识」】；
        定 编号：文 为 （（（（包编号 加 「:」） 加 类别） 加 「/」） 加 标识）；
        令 本地化键：列 为 【（（（（包编号 加 「.」） 加 类别） 加 「.」） 加 标识）】；
        若 值.有键（项目，「本地化键」） 则
            若 非 （项目【「本地化键」】 是 列） 则
                值.抛错（文件形状错误，「内容文档集本地化键必须是列表」，{「路径」：相对路径，「编号」：编号}）；
            终
            置 本地化键 为 值.深复制列（项目【「本地化键」】）；
        终
        定 引用：列 为 简引用列（项字段或（项目，「引用」，{}），编号，相对路径）；
        追加（结果，{「协议」：内容.协议名称，「格式版本」：内容.格式版本，「包编号」：包编号，「类别」：类别，「编号」：编号，「Schema编号」：Schema编号，「Schema版本」：Schema版本，「数据」：值.深复制典（项目【「数据」】），「原型」：项字段或（项目，「原型」，空），「引用」：引用，「行为编号」：项字段或（项目，「行为编号」，【】），「本地化键」：本地化键，「扩展」：项字段或（项目，「扩展」，{}）}）；
    终
    归 结果；
终

法 展开项目（所值，相对路径：文，类别：文）：列 则
    令 结果：列 为 【】；
    若 （所值 是 典） 则
        若 （（类别 等于 「内容」） 且 值.有键（所值，「协议」）） 则
            定 所典：典 为 所值；
            若 （所典【「协议」】 等于 内容集协议） 则
                归 展开内容集（所典，相对路径）；
            终
        终
        追加（结果，值.深复制典（所值））；
        归 结果；
    终
    若 非 （所值 是 列） 则
        值.抛错（文件形状错误，「内容包声明文件根值必须是据或据列」，{「路径」：相对路径，「类别」：类别}）；
    终
    定 各项：列 为 所值；
    若 （长度（各项） 大于 单文件项目上限） 则
        值.抛错（文件超限错误，「内容包单个声明文件项目数超过上限」，{「路径」：相对路径，「上限」：单文件项目上限}）；
    终
    逐 原项 于 各项 则
        若 非 （原项 是 典） 则
            值.抛错（文件形状错误，「内容包声明文件列表只能包含据」，{「路径」：相对路径，「类别」：类别}）；
        终
        追加（结果，值.深复制典（原项））；
    终
    归 结果；
终

法 解析项目文件（根目录：文，相对路径：文，类别：文，语言：文，预算：典，来源：列）：列 则
    定 原文：文 为 读取文本（根目录，相对路径，类别，语言，单文件字节上限，预算，来源）；
    令 所值：任意 为 空；
    试 则
        置 所值 为 言据.解析（原文）；
    救 所误 则
        值.抛错（文件解析错误，「内容包声明文件不是合法言据」，{「路径」：相对路径，「类别」：类别}）；
    终
    归 展开项目（所值，相对路径，类别）；
终

法 登记路径（已路径：典，相对路径：文，类别：文）：空 则
    定 键：文 为 文字.小写（相对路径）；
    若 值.有键（已路径，键） 则
        值.抛错（路径冲突错误，「同一内容包不可跨类别重复声明文件路径」，{「路径」：相对路径，「类别」：类别，「已有类别」：已路径【键】}）；
    终
    置 已路径【键】 为 类别；
    归 空；
终

法 读取包头（根目录：文，预算：典）：典 则
    令 来源：列 为 【】；
    定 清单原文：文 为 读取文本（根目录，清单文件名，「清单」，「」，清单字节上限，预算，来源）；
    定 清单：典 为 内容包.自文字（清单原文）；
    令 已路径：典 为 {}；
    登记路径（已路径，清单文件名，「清单」）；
    逐 相对路径：文 于 清单【「Schema」】 则
        登记路径（已路径，相对路径，「Schema」）；
    终
    逐 相对路径：文 于 清单【「入口」】 则
        登记路径（已路径，相对路径，「内容」）；
    终
    逐 语言：文 于 排序（键列（清单【「本地化」】）） 则
        逐 相对路径：文 于 清单【「本地化」】【语言】 则
            登记路径（已路径，相对路径，（「本地化:」 加 语言））；
        终
    终
    归 {「根目录」：根目录，「清单」：清单，「来源」：来源}；
终

法 追加各项（目标：列，来源：列）：空 则
    逐 项 于 来源 则
        追加（目标，项）；
    终
    归 空；
终

法 确保Schema归属（各定义：列，包编号：文，相对路径：文）：空 则
    定 前缀：文 为 （包编号 加 「:」）；
    逐 定义：典 于 各定义 则
        若 （非 值.有键（定义，「编号」） 或 非 （定义【「编号」】 是 文）） 则
            值.抛错（包归属错误，「内容 Schema 必须声明稳定编号」，{「路径」：相对路径，「包编号」：包编号}）；
        终
        若 非 文字.始于（定义【「编号」】，前缀） 则
            值.抛错（包归属错误，「内容 Schema 不可占用其他内容包的编号命名空间」，{「路径」：相对路径，「包编号」：包编号，「Schema编号」：定义【「编号」】}）；
        终
    终
    归 空；
终

法 确保内容归属（各文档：列，包编号：文，相对路径：文）：空 则
    逐 文档：典 于 各文档 则
        若 （非 值.有键（文档，「包编号」） 或 非 （文档【「包编号」】 是 文）） 则
            值.抛错（包归属错误，「内容文档必须声明所属包编号」，{「路径」：相对路径，「包编号」：包编号}）；
        终
        若 （文档【「包编号」】 不等于 包编号） 则
            值.抛错（包归属错误，「内容文档不可冒充其他内容包」，{「路径」：相对路径，「包编号」：包编号，「声明包编号」：文档【「包编号」】}）；
        终
    终
    归 空；
终

法 确保本地化归属（各资源：列，包编号：文，语言：文，相对路径：文）：空 则
    逐 资源：典 于 各资源 则
        若 （非 值.有键（资源，「包编号」） 或 非 （资源【「包编号」】 是 文）） 则
            值.抛错（包归属错误，「本地化资源必须声明所属包编号」，{「路径」：相对路径，「包编号」：包编号}）；
        终
        若 （资源【「包编号」】 不等于 包编号） 则
            值.抛错（包归属错误，「本地化资源不可冒充其他内容包」，{「路径」：相对路径，「包编号」：包编号，「声明包编号」：资源【「包编号」】}）；
        终
        若 （非 值.有键（资源，「语言」） 或 非 （资源【「语言」】 是 文）） 则
            值.抛错（本地化语言错误，「本地化资源必须声明语言」，{「路径」：相对路径，「语言」：语言}）；
        终
        若 （资源【「语言」】 不等于 语言） 则
            值.抛错（本地化语言错误，「本地化资源语言必须与清单路径分组一致」，{「路径」：相对路径，「语言」：语言，「声明语言」：资源【「语言」】}）；
        终
    终
    归 空；
终

法 加载包内容（包头：典，预算：典）：典 则
    定 根目录：文 为 包头【「根目录」】；
    定 清单：典 为 包头【「清单」】；
    定 包编号：文 为 清单【「包编号」】；
    令 各Schema：列 为 【】；
    令 各内容：列 为 【】；
    令 各本地化：列 为 【】；
    令 来源：列 为 值.深复制列（包头【「来源」】）；
    逐 相对路径：文 于 清单【「Schema」】 则
        定 所定义：列 为 解析项目文件（根目录，相对路径，「Schema」，「」，预算，来源）；
        确保Schema归属（所定义，包编号，相对路径）；
        追加各项（各Schema，所定义）；
    终
    逐 相对路径：文 于 清单【「入口」】 则
        定 所文档：列 为 解析项目文件（根目录，相对路径，「内容」，「」，预算，来源）；
        确保内容归属（所文档，包编号，相对路径）；
        追加各项（各内容，所文档）；
    终
    逐 语言：文 于 排序（键列（清单【「本地化」】）） 则
        逐 相对路径：文 于 清单【「本地化」】【语言】 则
            定 所资源：列 为 解析项目文件（根目录，相对路径，「本地化」，语言，预算，来源）；
            确保本地化归属（所资源，包编号，语言，相对路径）；
            追加各项（各本地化，所资源）；
        终
    终
    归 {「协议」：包协议，「格式版本」：格式版本，「根目录」：根目录，「清单」：值.深复制典（清单），「Schema」：各Schema，「内容」：各内容，「本地化」：各本地化，「来源」：来源}；
终

公 法 加载包（原根目录）：典 则
    定 预算：典 为 新预算（）；
    定 根目录：文 为 规范根目录（原根目录）；
    归 加载包内容（读取包头（根目录，预算），预算）；
终

公 法 尝试加载包（原根目录）：典 则
    试 则
        归 {「成功」：真，「值」：加载包（原根目录），「错误」：空}；
    救 所误 则
        归 {「成功」：假，「值」：空，「错误」：值.公开错误典（所误）}；
    终
终

公 法 加载世界（各原根目录：列，引擎版本：文）：典 则
    若 （（长度（各原根目录） 等于 0） 或 （长度（各原根目录） 大于 256）） 则
        值.抛错（根目录无效错误，「内容世界必须包含一至 256 个内容包根目录」，{}）；
    终
    定 预算：典 为 新预算（）；
    令 已根：典 为 {}；
    令 各包头：列 为 【】；
    令 各清单：列 为 【】；
    逐 原根 于 各原根目录 则
        定 根目录：文 为 规范根目录（原根）；
        定 根键：文 为 文字.小写（根目录）；
        若 值.有键（已根，根键） 则
            值.抛错（根目录无效错误，「内容世界不可重复加载同一根目录」，{}）；
        终
        置 已根【根键】 为 真；
        定 包头：典 为 读取包头（根目录，预算）；
        追加（各包头，包头）；
        追加（各清单，包头【「清单」】）；
    终
    定 已排序清单：列 为 内容包.解析加载顺序（各清单，引擎版本）；
    令 包头索引：典 为 {}；
    逐 包头：典 于 各包头 则
        置 包头索引【包头【「清单」】【「包编号」】】 为 包头；
    终
    令 各包：列 为 【】；
    令 全部Schema：列 为 【】；
    令 全部内容：列 为 【】；
    令 全部本地化：列 为 【】；
    令 全部来源：列 为 【】；
    逐 清单：典 于 已排序清单 则
        定 包编号：文 为 清单【「包编号」】；
        定 所包：典 为 加载包内容（包头索引【包编号】，预算）；
        追加（各包，所包）；
        追加各项（全部Schema，所包【「Schema」】）；
        追加各项（全部内容，所包【「内容」】）；
        追加各项（全部本地化，所包【「本地化」】）；
        追加各项（全部来源，所包【「来源」】）；
    终
    归 {「协议」：世界协议，「格式版本」：格式版本，「引擎版本」：引擎版本，「包」：各包，「清单」：已排序清单，「Schema」：全部Schema，「内容」：全部内容，「本地化」：全部本地化，「来源」：全部来源，「统计」：{「包」：长度（各包），「文件」：预算【「文件数」】，「字节」：预算【「字节数」】}}；
终

公 法 尝试加载世界（各原根目录：列，引擎版本：文）：典 则
    试 则
        归 {「成功」：真，「值」：加载世界（各原根目录，引擎版本），「错误」：空}；
    救 所误 则
        归 {「成功」：假，「值」：空，「错误」：值.公开错误典（所误）}；
    终
终

公 法 构建内容图（各原根目录：列，引擎版本：文，默认语言：文，回退语言：列<文>，各行为：列） 则
    定 世界：典 为 加载世界（各原根目录，引擎版本）；
    归 内容图.构建（引擎版本，世界【「清单」】，世界【「Schema」】，世界【「内容」】，世界【「本地化」】，默认语言，回退语言，各行为）；
终

公 法 尝试构建内容图（各原根目录：列，引擎版本：文，默认语言：文，回退语言：列<文>，各行为：列）：典 则
    试 则
        归 {「成功」：真，「值」：构建内容图（各原根目录，引擎版本，默认语言，回退语言，各行为），「错误」：空}；
    救 所误 则
        归 {「成功」：假，「值」：空，「错误」：值.公开错误典（所误）}；
    终
终
