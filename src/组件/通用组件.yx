引「./组件注册表.yx」为 组件；

法 据规则（必需：列<文>，属性：典）：典 则
    归 {「类型」：「据」，「必需」：必需，「允许额外」：真，「属性」：属性}；
终

法 文规则（）：典 则
    归 {「类型」：「文」}；
终

法 数规则（）：典 则
    归 {「类型」：「数」}；
终

法 理规则（）：典 则
    归 {「类型」：「理」}；
终

法 列规则（）：典 则
    归 {「类型」：「列」}；
终

法 典规则（）：典 则
    归 {「类型」：「据」}；
终

法 定义（编号：文，规则：典，默认值：典，文档：文） 则
    归 组件.组件定义（编号，1，规则，默认值，空，文档）；
终

公 法 建立注册表（） 则
    定 注册 为 组件.注册表（）；
    注册.注册（定义（「言域:组件/描述」，据规则（【「名称」，「描述」】，{「名称」：文规则（），「描述」：文规则（）}），{「名称」：「未命名」，「描述」：「」}，「实体显示名称与可本地化描述」））；
    注册.注册（定义（「言域:组件/位置」，据规则（【「所在」，「坐标」】，{「所在」：文规则（），「坐标」：列规则（）}），{「所在」：「」，「坐标」：【】}，「实体所在容器或房间及可选坐标」））；
    注册.注册（定义（「言域:组件/容器」，据规则（【「容量」，「内容」】，{「容量」：数规则（），「内容」：列规则（）}），{「容量」：0，「内容」：【】}，「拥有容量限制和稳定实体编号内容列的容器」））；
    注册.注册（定义（「言域:组件/出口」，据规则（【「方向」】，{「方向」：典规则（）}），{「方向」：{}}，「房间方向到目标房间编号的映射」））；
    注册.注册（定义（「言域:组件/可移动」，据规则（【「可移动」，「重量」】，{「可移动」：理规则（），「重量」：数规则（）}），{「可移动」：真，「重量」：0}，「物体移动许可与通用重量」））；
    注册.注册（定义（「言域:组件/属性」，据规则（【「基础」，「当前」】，{「基础」：典规则（），「当前」：典规则（）}），{「基础」：{}，「当前」：{}}，「可扩展数值属性及计算后当前值」））；
    注册.注册（定义（「言域:组件/状态」，据规则（【「状态」】，{「状态」：列规则（）}），{「状态」：【】}，「带来源和到期时间的通用状态列」））；
    注册.注册（定义（「言域:组件/背包」，据规则（【「容量」，「物品」】，{「容量」：数规则（），「物品」：列规则（）}），{「容量」：20，「物品」：【】}，「角色持有物品实例编号列」））；
    注册.注册（定义（「言域:组件/装备」，据规则（【「槽位」】，{「槽位」：典规则（）}），{「槽位」：{}}，「装备槽稳定编号到物品实例编号的映射」））；
    注册.注册（定义（「言域:组件/战斗」，据规则（【「生命」，「最大生命」，「攻击」，「防御」，「阵营」，「战斗中」】，{「生命」：数规则（），「最大生命」：数规则（），「攻击」：数规则（），「防御」：数规则（），「阵营」：文规则（），「战斗中」：理规则（）}），{「生命」：1，「最大生命」：1，「攻击」：0，「防御」：0，「阵营」：「中立」，「战斗中」：假}，「不绑定职业题材的确定性战斗状态」））；
    注册.注册（定义（「言域:组件/技能」，据规则（【「已学」】，{「已学」：列规则（）}），{「已学」：【】}，「已学习技能编号与熟练数据」））；
    注册.注册（定义（「言域:组件/冷却」，据规则（【「到期」】，{「到期」：典规则（）}），{「到期」：{}}，「行为编号到世界毫秒到期值的映射」））；
    注册.注册（定义（「言域:组件/交互」，据规则（【「行为」】，{「行为」：列规则（）}），{「行为」：【】}，「实体公开的稳定行为编号列」））；
    注册.注册（定义（「言域:组件/对话」，据规则（【「节点」，「进度」】，{「节点」：文规则（），「进度」：典规则（）}），{「节点」：「」，「进度」：{}}，「当前对话节点与参与者进度」））；
    注册.注册（定义（「言域:组件/任务」，据规则（【「进度」】，{「进度」：典规则（）}），{「进度」：{}}，「任务模板编号到角色进度的映射」））；
    注册.注册（定义（「言域:组件/掉落」，据规则（【「表」，「次数」】，{「表」：文规则（），「次数」：数规则（）}），{「表」：「」，「次数」：1}，「内容掉落表引用与抽取次数」））；
    注册.注册（定义（「言域:组件/AI」，据规则（【「行为」，「状态」】，{「行为」：文规则（），「状态」：典规则（）}），{「行为」：「」，「状态」：{}}，「AI 行为编号与可持久行为状态」））；
    注册.注册（定义（「言域:组件/账户」，据规则（【「账户编号」，「角色」】，{「账户编号」：文规则（），「角色」：列规则（）}），{「账户编号」：「」，「角色」：【】}，「角色与账户的显式关系」））；
    注册.注册（定义（「言域:组件/角色」，据规则（【「账户编号」，「名称」，「删除状态」，「删除保护到」】，{「账户编号」：文规则（），「名称」：文规则（），「删除状态」：文规则（），「删除保护到」：数规则（）}），{「账户编号」：「」，「名称」：「未命名」，「删除状态」：「正常」，「删除保护到」：0}，「玩家角色与账户归属及受保护删除状态」））；
    注册.注册（定义（「言域:组件/权限」，据规则（【「角色」，「授权」】，{「角色」：列规则（），「授权」：列规则（）}），{「角色」：【「玩家」】，「授权」：【】}，「角色组和显式权限编号」））；
    注册.注册（定义（「言域:组件/频道」，据规则（【「频道」，「静音」】，{「频道」：列规则（），「静音」：列规则（）}），{「频道」：【「附近」】，「静音」：【】}，「订阅频道和静音列表」））；
    注册.注册（定义（「言域:组件/定时」，据规则（【「任务」】，{「任务」：列规则（）}），{「任务」：【】}，「与实体关联的持久计划任务编号」））；
    注册.注册（定义（「言域:组件/持久化」，据规则（【「保存」，「策略」】，{「保存」：理规则（），「策略」：文规则（）}），{「保存」：真，「策略」：「变更」}，「实体保存许可与仓储策略提示」））；
    归 注册；
终
